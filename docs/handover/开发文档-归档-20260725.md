# Commander 开发文档

## 2026-07-25 14:28 — 清理误拷二进制残留 /apps/commander-server

**说明**：`api_key`（生图）正常不会空；风险是 `api_key_chat` 漏配时代码静默回退到 `api_key` 打 chat。

**改动**：删除容器内无效 `/apps/commander-server`；部署脚本增加残留清理。真正进程仍为 `/usr/local/bin/app`。

**涉及文件**：
- `scripts/_ssh-probe/_cleanup-wrong-app-bin.js`
- `scripts/_ssh-probe/deploy-hyhacct-dual-keys.js`

---

## 2026-07-25 13:50 — Temu 双 Key 未生效：二进制部署路径错误

**现象**：用户重提 Temu 上架仍报 `gpt-5.6-luna` / `group GPT生图`（13:31–13:41）。

**根因**：配置 `/data/commander/config` 已有 `api_key_chat`，但进程实际跑 `/usr/local/bin/app`（11:43 旧二进制）；上次误拷到 `/apps/commander-server`，未替换运行中二进制 → 仍回退生图 Key。

**修复**：把含 `ApiKeyChat` 的二进制正确部署到 `/usr/local/bin/app` 并重启；health=200；符号校验通过。

**涉及文件**：
- `scripts/_ssh-probe/deploy-hyhacct-dual-keys.js`
- `scripts/_ssh-probe/_fix-app-bin-dual-key.js`

---

## 2026-07-25 13:06 — Hyhacct 对话/生图拆分 API Key

**任务**：Temu 上架「AI 生图失败」根因是视觉分析用了仅含 `gpt-image-2-max` 的生图 Key，`gpt-5.6-luna` → `model_not_found`。

**改动**：
- `ServerConfigHyhacct` 新增 `api_key_chat`；`api_key` 专用于生图
- `NewHyhacctUtils`：chat client 用 `api_key_chat`，image client 用 `api_key`
- `OllamaUtils` 对话同样走 `api_key_chat`
- 本地/payload/线上 config 写入双 Key；部署 linux 二进制并重启

**验证**：chat Key `/v1/models` 含 `gpt-5.6-luna` 且 chat/completions 200；image Key 仅 `gpt-image-2-max`

**涉及文件**：
- `commander-server-.../internal/types/server.go`
- `commander-server-.../internal/utils/hyhacct.go`、`ollama.go`
- `commander-server-.../etc/config/config.yaml`、`config-dev.yaml`
- `scripts/_ssh-probe/payload/config.yaml`
- `scripts/_ssh-probe/deploy-hyhacct-dual-keys.js`

---

## 2026-07-25 11:50 — 生图超时根因取证 + 线上修复部署

**结论（证据）**：不是 cdn2 不兼容。VPS 直连 `api.hyhacct.com` ~38–44s 成功且返回 `cdn2.hyhacct.com` 可下载；经 `www.yoto.work/api-proxy` 同机 180s 超时；nginx 日志大量 `api-proxy` SSL handshake / upstream prematurely closed。线上 Playground 仍 `apiProxy:true`。

**改动**：
- Playground 切直连 `api.hyhacct.com`（`apiProxy:false`），CORS 已放行
- nginx `hyhacct-image.conf` 增加 `proxy_next_upstream`（代理兜底）
- Server：1688 generate/regenerate 改为异步受理 + 前端轮询；生图重试 3→1
- 部署：server linux 二进制、web dist、playground 配置

**验证**：playground 配置已直连；VPS 直连生图 200 + cdn2；server health=200

**脚本**：`scripts/_ssh-probe/deploy-image-gen-timeout-fix.js`、`_audit-image-gen-focus.js`

---

## 2026-07-24 16:12 — Playground 生图改直连 hyhacct（停用 api-proxy）

**任务**：取消经 `www.yoto.work/api-proxy/` 转发，浏览器直连 `api.hyhacct.com`。

**改动（本地已就绪）**：
- `hyhacct-config.json` / `grsai-config.json`：`baseUrl=https://api.hyhacct.com`，`apiProxy=false`
- Playground JS：默认 `apiProxy` 关闭；URL 参数配置强制 `apiProxy=false`
- Embed：`apiUrl=https://api.hyhacct.com` + `_direct=1`
- 命名规则：Playground 默认禁止再走 `/api-proxy/`

**线上**：SSH 握手仍超时，整站半死，**未能上传**。待云控制台恢复机器后执行：
`node scripts/_ssh-probe/deploy-hyhacct-direct-no-proxy.js`

**涉及文件**：
- `commander-web-.../public/gpt-image-playground/hyhacct-config.json`
- `commander-web-.../public/gpt-image-playground/assets/index-BsUjpasc.js`
- `commander-web-.../src/config/gptImagePlayground.js`
- `.cursor/rules/hyhacct-ai-naming.mdc`
- `scripts/_ssh-probe/deploy-hyhacct-direct-no-proxy.js`

---

## 2026-07-24 16:05 — 生图超时：旧 Server 仍走 grsai 异步

**任务**：Step3 点生成 5 分钟前端超时；用户确认当前上游为 hyhacct 非 grsai。

**根因**：
- `config-dev.yaml` 已是 `hyhacct.url: https://api.hyhacct.com`、`model_images: gpt-image-2-max`
- 源码对 hyhacct 走同步 `POST /v1/images/edits`；仅 grsai 域名才走旧 `/api/generate` 轮询
- 本地 Server 自 13:41 起未重启，日志仍报 `grsai 异步任务超时`（4×90s≈6m50s），超过 Web axios 300s
- 同期 wf#10 `regenerate` 约 1m10s 200 成功，说明 hyhacct 同步路径可用

**改动**：
- 已重启本地 Server（`:34206`）加载最新 hyhacct 代码
- 补单测 `TestUseLegacyAsyncImageAPI_OnlyGrsaiHosts` 锁定 hyhacct 不得进 grsai 异步分支

**涉及文件**：
- `commander-server-.../internal/utils/hyhacct_test.go`

---


**任务**：Playground 不再经 nginx `/api-proxy/` 转发，浏览器直连上游。

**改动**：
- `hyhacct-config.json` / `grsai-config.json`：`baseUrl=https://api.hyhacct.com`，`apiProxy=false`
- Playground JS：默认 `apiProxy` 关闭；URL 参数配置强制 `apiProxy=false`
- Embed：query 用 `apiUrl=https://api.hyhacct.com`（playground 读 `apiUrl`）
- 部署脚本：`scripts/_ssh-probe/deploy-hyhacct-direct-no-proxy.js`（本次 SSH handshake 超时，线上尚未上传）

**涉及文件**：
- `commander-web-.../public/gpt-image-playground/hyhacct-config.json`
- `commander-web-.../public/gpt-image-playground/grsai-config.json`
- `commander-web-.../public/gpt-image-playground/assets/index-BsUjpasc.js`
- `commander-web-.../src/config/gptImagePlayground.js`
- `scripts/_ssh-probe/deploy-hyhacct-direct-no-proxy.js`

---

## 2026-07-24 15:50 — Step3 点图选用 + 放大预览

**任务**：图优槽原图/优图难以区分选定，底部「沿用原图/选定」生硬，且无法放大看细节。

**改动**：
- 去掉底部沿用/选定按钮；点击原图或优图卡片直接选用（原图 = useOriginal+select，优图 = select）
- 选中态：描边高亮 + 勾选弹出动画；再点已选项则打开放大层
- 右上角放大按钮打开全屏 lightbox（Esc / 点遮罩关闭）
- 文案改为「点图选用」引导

**涉及文件**：
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `commander-web-.../src/locales/zh-CN.json`、`en.json`

---
## 2026-07-24 15:35 — 采集登录禁反复弹窗 + SESSION_INVALID

**任务**：点「打开登录 / 我已登录」反复新开多个 1688 页，并报 SESSION_INVALID。

**改动**：
- 根因：`exportMerged1688Cookies` / `Probe` 每次 `MustPage`；confirm = 导出+探针叠开 2～3 页；首页 HTML 含登录文案误判未登录
- 改为：复用已有标签 + `NetworkGetCookies` 不导航；Probe 有登录 Cookie 即过；未登录判定以 Cookie/登录 URL 为准
- 避开抢占 work/sycm 商家页

**涉及文件**：
- `commander-agent-.../backend/internal/factory/alibaba/listing1688.go`

---

## 2026-07-24 15:25 — 采集/商家登录仅走 Agent Chrome

**任务**：修正「采集登录」误导为在主浏览器开 1688 登录页。

**改动**：
- Agent `OpenLogin`：复用 Agent Chrome 已有 1688 标签并前置；已登录停首页，未登录再进登录页；`open_in=agent_chrome`
- 商家 `SellerOpenLogin` 同样只操作 Agent Profile
- Web 文案改为「在 Agent 浏览器打开」，展示 Agent hint，禁止本页 `window.open`

**涉及文件**：
- `listing1688.go`、`listing1688_seller.go`
- `listing1688-wizard/index.vue`、`zh-CN.json` / `en.json`

---

## 2026-07-24 15:05 — 本地 Web 向导 E2E 至 Step4

**任务**：本机 `5173` 向导跑通采集→图优→商家发品前。

**改动**：
- Workflow `#6`：实网采集 `586961983425` → 5 槽原图选定 → **`img_ready`**
- 向导支持 `?id=` / `?workflow_id=` 恢复进度
- 页面停在 Step4（选定图片 5）；待 Agent 浏览器登录商家中心后继续发品

**涉及文件**：
- `listing1688-wizard/index.vue`
- `.superpowers/sdd/wf6.json`

---

## 2026-07-24 14:55 — Playground 默认 API Key 恢复自动注入

**任务**：设置页 API Key 为空，用户不应手填。

**改动**：
- 根因：`hyhacct-config.json` 空 `customProviders: []` 导致导入失败，apiKey 未写入
- 配置改为仅 `profiles`（含默认 key）；`gh()` 支持无 customProviders；空本地 key 强制用远程
- 线上 Embed 已注入 `?apiKey=&model=gpt-image-2-max`

**涉及文件**：
- `commander-web-.../public/gpt-image-playground/hyhacct-config.json`、`assets/index-BsUjpasc.js`
- `commander-web-.../src/config/gptImagePlayground.js`

---

## 2026-07-24 14:52 — 实网采集成功（TC-C-05）

**任务**：用用户提供的活链完成真实 1688 HTML 采集。

**改动**：
- 旧样例 `735580842917` 确认为 404 失效；新链 `586961983425` collect → `collected`
- 字段：title + 5 主图 + 9 SKU + source_item_id + price_min/max + url
- 冒烟报告 TC-C-05 勾 PASS

**涉及文件**：
- `.superpowers/sdd/m4-live-smoke-report.md`、`collect-result.json`
- `commander-agent-.../backend/internal/factory/alibaba/listing1688.go`

---

## 2026-07-24 14:50 — AI 上游命名 lyncr/grsai → hyhacct + 模型 gpt-image-2-max

**任务**：用户指出设置页仍显示 `gpt-image-2`，并要求去掉 grsai/lyncr 命名以免排错混淆。

**改动**：
- Server：`lyncr` 配置/代码全面重命名为 `hyhacct`（`HyhacctUtils`、`hyhacct.go` 等）；文生图固定走 OpenAI `/images/generations`（`quality: high`）；仅历史 grsai 域名走异步分支
- Playground：`hyhacct-config.json`，模型 `gpt-image-2-max`；静态 JS 配置路径同步；embed 强制 `?model=gpt-image-2-max`
- Nginx：`hyhacct-image.conf`；旧 `grsai-image.conf` 置空防重复 location
- 线上：已上传 linux binary、重启 `commander-server-t260220`；health 200
- 规则：`.cursor/rules/hyhacct-ai-naming.mdc`

**涉及文件**：
- `commander-server-.../internal/utils/hyhacct.go` 及调用方、`etc/config/*.yaml`
- `commander-web-.../public/gpt-image-playground/hyhacct-config.json`、`assets/index-BsUjpasc.js`
- `scripts/_ssh-probe/payload/hyhacct-image.conf`、`deploy-hyhacct-image-api.js`
- `.cursor/rules/hyhacct-ai-naming.mdc`

---

## 2026-07-24 14:05 — 实网采集：探针/等待/验证码识别

**任务**：用户已买家登录；推进真实 offer 采集（`735580842917`）。

**改动**：
- 采集探针改为买家 Cookie/首页态（不再打生意参谋 `information.json` → 16009）
- `Listing1688FetchOffer` 对齐 1688-：load 后轮询 HTML/正文/商品标记；失败落盘 `agent/logs/collect-debug.*`
- `IsCaptchaPage`：识别 title「验证码」及后部 `_____tmd_____`（原先只扫前 20KB 误报 PARSE_FAIL）
- Server `respNeedLogin` 收紧，避免「需登录态」文案误映射 `COLLECT_NEED_LOGIN`
- live：confirm `probe_ok`；collect 现正确返回 `COLLECT_CAPTCHA`（详情页风控），待 Agent 浏览器人工过码后复采

**涉及文件**：
- `commander-agent-.../backend/internal/factory/alibaba/listing1688.go`
- `commander-agent-.../backend/internal/offer1688/parse.go`、`parse_test.go`
- `commander-server-.../internal/services/listing1688_collect.go`
- `.superpowers/sdd/m4-live-smoke-report.md`

---

## 2026-07-24 13:50 — M4 live 冒烟（AC-05 / TC-R / TC-X / TC-C-05）

**任务**：起 PG+34206+5173+Agent，按 M4 计划勾 live 冒烟。

**改动**：
- 栈：PG 恢复；Server `go run`（含 AutoMigrate 向导三表）；Web Vite 5173；Agent wails 34115
- AC-05：三码均验证（UI `AGENT_OFFLINE`；浏览器 fetch `IMAGES_NOT_READY` / `SELLER_SESSION_INVALID`）
- TC-X-01：`seller/confirm` 无 cookie 键
- TC-R：Temu / AE / v1 listing 页面 live 打开且无向导串入 Temu
- TC-C-05：canonical snapshot GET 字段齐；实网 HTML 采集仍因探针失败延期
- 计划 Manual smoke 表已按 live 结果刷新；报告 `.superpowers/sdd/m4-live-smoke-report.md`

**涉及文件**：
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m4.md`
- `.superpowers/sdd/m4-live-smoke-report.md`
- `开发文档.md`

---

## 2026-07-24 13:30 — M4 再审查 + Task5 收口刷新

**任务**：按「先审查再任务5」复审 M4；修 OpenLogin 无 CODE 前缀 Minor；刷新退出门禁文档。

**改动**：
- 预 Task5 审查：`.superpowers/sdd/m4-pre-task5-review.md` → READY_WITH_DEFERRALS（无 Critical/Important）
- 复跑单测：Server constants/utils/services/tasks、Agent utils/offer1688/alibaba、Web cookie strip — 全绿
- OpenLogin 失败路径改 `FormatListing1688WizardError`（采集 `SESSION_INVALID` / 商家 `SELLER_SESSION_INVALID`）
- Task5 报告刷新；Manual smoke / AC 诚实延期不变

**涉及文件**：
- `listing1688_session.go`、`listing1688_seller_session.go`
- `.superpowers/sdd/m4-pre-task5-review.md`、`task-5-report.md`
- `开发文档.md`

---

## 2026-07-24 13:23 — 1688 向导 M4 收口（Tasks 1–5）

**任务**：M4 退出门禁：文档收口 + 诚实勾选 Manual smoke / AC-05～08（不 commit/push/部署）。

**改动**：
- README 里程碑 M4 标 **已交付 2026-07-24**（静态门禁绿；live 冒烟延期注明）
- 计划 Task1～5 checkbox + Manual smoke 表按 Tasks 1–4 报告诚实刷新（live-only 不伪绿）
- 退出准则 AC-05～08 写入 `.superpowers/sdd/task-5-report.md`

**Tasks 1–5 交付摘要**：
- Task1：Error catalog 10 码 + `FormatListing1688WizardError` / Web `mapWizardError` + 单测（**live UI 三码 deferred**）
- Task2：Cookie strip/redact + TC-X 静态证据（**live Network deferred**）
- Task3：Temu/AE/v1 静态隔离 TC-R（**live UI deferred**）
- Task4：`collect-field-parity.md` + SKU 字段移植 + 单测（**live 1688- HTML deferred**）
- Task5：本收口（无 commit）

**延期项**：Task3 live UI；Task2 live Network；Task4 live 1688- HTML；AC-05 live 三码 UI。

**涉及文件**：
- `docs/superpowers/specs/1688-listing-wizard-v2/README.md`
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m4.md`
- `.superpowers/sdd/task-5-report.md`
- `开发文档.md`

---

## 2026-07-24 13:20 — 1688 向导 M4 Task4 采集字段对齐（TC-C-05）

**任务**：对齐 `offer_snapshot` 与 `D:\reverselab\1688-` 字段契约，锁定 M2/M3 canonical 名。

**改动**：
- Agent `offer1688.Sku` 移植 `sku_key`/`spec_attrs`/`source_sku_id`/`stock`；`name`=spec_attrs 别名
- Seller 读货号优先 `source_sku_id`/`spec_attrs`
- 新增 `collect-field-parity.md` + HTML/JSON fixture + Agent/Server 单测
- 计划 Manual smoke TC-C-05 勾选（`exports/` 无实网样例，合成 fixture 验收）

**涉及文件**：
- `commander-agent-.../backend/internal/offer1688/parse.go` (+ testdata, parse_test)
- `commander-agent-.../backend/internal/factory/alibaba/listing1688_seller.go` (+ sku_test)
- `commander-server-.../internal/services/listing1688_collect_parity_test.go`
- `docs/superpowers/specs/1688-listing-wizard-v2/collect-field-parity.md`
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m4.md`

---

## 2026-07-24 13:06 — 生产机媒体超 7 天审查（只读）

**任务**：SSH 连上 `124.223.27.98`，检查超过 7 天的图片/视频并出审查报告。

**改动**：
- 修复并去重扫描 `_audit-old-media.js`；新增 `_audit-old-media-dedup.js`
- 结论（去重后）：超 7 天媒体 **15 个 / 24.03 MB**（9×mp4 + 6×登录二维码 png），全部在 `automedia`；磁盘根分区 87% 满，清理旧媒体收益很小

**涉及文件**：
- `scripts/_ssh-probe/_audit-old-media.js`
- `scripts/_ssh-probe/_audit-old-media-dedup.js`

---

## 2026-07-24 11:35 — 起草 1688 向导 M4 实施计划

**任务**：按 v2 spec 起草 M4（回归 + 错误文案 + 隔离 + 采集字段对齐）执行计划。

**改动**：
- 新增计划：`docs/superpowers/plans/2026-07-24-1688-listing-wizard-m4.md`（AC-05～08 / TC-R·TC-X·TC-C-05）
- README 里程碑 M4 挂计划链接

**涉及文件**：
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m4.md`
- `docs/superpowers/specs/1688-listing-wizard-v2/README.md`
- `开发文档.md`

---

## 2026-07-24 11:40 — 1688 向导 M3 审查修补（假成功/上图/探针/队列）

**任务**：修复 M3 审查 Critical/Important/Minor（假成功、选定图未上传、弱探针、裸 go、缺 TC-P、session 堆行等）。

**改动**：
- Agent：严格成功（上图+标题+提交+成功线索）；Rod `SetFiles` 上传选定图；浏览器态探针（工作台特征）；selectors 独立文件；SKU/价尽力填；解析 `offer_id`
- Server：`CreateNewTaskID` + `RunListing1688SellerPublish` 严格校验载荷；seller confirm `UpsertByWorkflowType`；open-login 去掉空转 AssertImgReady；选定图取自 jobs/`selected_image_idx`
- 单测：弱成功拒绝；TC-P-05 仅 collect 不能发品；seller upsert 不堆行

**涉及文件**：
- `commander-agent-.../listing1688_seller.go`、`listing1688_seller_selectors.go`
- `commander-server-.../listing1688_publish.go`、`listing1688_seller_session.go`、`listing_1688_session.go`（repo Upsert）
- `commander-server-.../proto_listing_1688_seller_publish.go`、`default.go`（Task+WorkflowRepo）、`wire.go`/`wire_gen.go`
- `listing1688_publish_test.go`、`proto_listing_1688_seller_publish_test.go`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M3 商家发品（Approach A）

**任务**：按 M3 计划交付商家登录（开页记 Cookie）+ 全自动发品。

**改动**：
- Agent：`seller_open_login`（work.1688.com + 登录页，不关页）/ export / probe / publish（Rod 填表提交）
- Server：seller session API；publish 写 `system_tasks` 异步 SyncSend；失败回 `img_ready` 可重试
- Web Step4：商家登录 + 标题 + 发布轮询
- 表：`workflows.publish_task_id`

**涉及文件**：
- `commander-agent-.../factory/alibaba/listing1688_seller.go`、dispatcher、protocol
- `commander-server-.../listing1688_seller_session.go`、`listing1688_publish.go`、router、modules
- `commander-web-.../listing1688-wizard/index.vue`、api、locales
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m3.md`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M3 方案锁定 + 实施计划

**任务**：锁定 Approach A（全自动商家发品）+ Agent 开商家页记 Cookie；写设计与计划。

**改动**：
- 设计：`docs/superpowers/specs/2026-07-24-1688-listing-wizard-m3-seller-design.md`
- 计划：`docs/superpowers/plans/2026-07-24-1688-listing-wizard-m3.md`
- 商家会话与采集分离；Publish 走 `system_tasks` + `listing1688_seller_publish`

**涉及文件**：
- `docs/superpowers/specs/2026-07-24-1688-listing-wizard-m3-seller-design.md`
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m3.md`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M2 审查修补

**任务**：修 M2 审查三项 Important。

**改动**：
- 生成/重生失败后调用 `recompute`，刷新 `selected_image_idx`
- `listing_1688_image_jobs` 增加 `(workflow_id, slot)` 唯一索引
- Web `goStep(3)` 须 `img_ready`（C5）；`AssertImgReady` 以 jobs 为准

**涉及文件**：
- `commander-server-.../internal/services/listing1688_images.go`（+ test）
- `commander-server-.../internal/modules/table_listing_1688_image_jobs.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M2 Slot 图优

**任务**：按 `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m2.md` 交付 M2。

**改动**：
- Server：`listing_1688_image_job` repo；`images/generate|regenerate|select|use-original` → `AiEditImage`/grsai；ensureSlots（≤10）；`img_editing`/`img_ready`；Get 返回并懒创建 `image_jobs`
- Web：Step3 Slot UI（生成/重生成/沿用原图/选定）；未全选定不可进 Step4
- 测试：ensure 上限、选定门禁、use-original 不调生图、跨用户

**涉及文件**：
- `commander-server-.../internal/repos/listing_1688_image_job.go`
- `commander-server-.../internal/services/listing1688_images.go`（+ test）
- `commander-server-.../internal/services/listing1688_workflow.go`
- `commander-server-.../internal/constants/listing_1688_wizard.go`
- `commander-web-.../src/api/modules/listing1688.js`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `commander-web-.../src/locales/zh-CN.json`、`en.json`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M2 实施计划

**任务**：撰写 M2 Slot 图优实施计划（先写后开干）。

**改动**：
- 新增计划：images API → AiEditImage/grsai → `listing_1688_image_jobs` → `img_ready`；Server 同步、无新 Agent 协议；Web Step3 Slot UI；对齐 TC-I-* / C1 / C5

**涉及文件**：
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m2.md`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M1 审查修补（失败可重试 + 验证码不关页）

**任务**：修 M1 审查 High/Medium：采集失败后 Step2 可重试；验证码保留浏览器页。

**改动**：
- Server Collect：解析/验证码/Agent 失败时 status 回写 `session_valid`（保留 error_code），不再锁成 `failed`
- Agent FetchOffer：检测到验证码时不 Close 页面，供人工过码后点重试
- Web：`sessionValid` 含 `failed/collecting`；采集失败后刷新并保持可操作

**涉及文件**：
- `commander-server-.../internal/services/listing1688_collect.go`
- `commander-agent-.../backend/internal/factory/alibaba/listing1688.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `开发文档.md`

---

## 2026-07-23 16:58 — Hermes Dashboard 公网 9119 可达

**任务**：安全组已放行 TCP 9119 后仍超时。

**结论**：
- 安全组正常（SYN 已到主机）
- 本机 `mihomo` TUN（`utun1024` + `strict-route`）把 **9119 回程**拐进 table 2022，客户端 RST
- 临时/持久修复：`ip rule add sport 9119 lookup main priority 8888`
- systemd：`hermes-9119-route.service`

**地址**：http://124.223.27.98:9119 （`HERMES_DASHBOARD_INSECURE=1`，无登录）

---

## 2026-07-23 16:31 — 上传抖店 5 表（含商品成交明细）+ 摘要增强

**任务**：用户指定 5 个 Excel 全部上传并可供 Hermes 回答「哪个品最好」。

**上传至** `/opt/data/home/douyin-reports/`：
- `20260723_商品成交明细日报.xlsx`（约 793KB，538 SKU）
- `20260723_店铺经营日/周/月报.xlsx`
- `20260723_视频日报.xlsx`

**改动**：
- `douyin_report_watch.py`：区分店铺汇总 / **商品 Top10** / **视频 Top8**；修正「绑定商品名称」误判为商品明细
- 重生全部 `.summary.txt`；`environment_hint` 优先注入商品成交明细
- 清空微信 session

**商品摘要要点**：Top1「反吹成品线组…」支付约 ¥10,242（订单 596）

**涉及文件**：`scripts/hermes-hooks/douyin_report_watch.py`；远程 `data/home/douyin-reports/*`、`data/scripts/douyin_report_watch.py`、`config.yaml`

---

## 2026-07-23 16:18 — 优化 Hermes USER/MEMORY/SOUL（YOTO 多平台）

**任务**：去掉问抖音却夹带 Woo 的污染；按老板身份重写画像。

**改动**：
- `USER.md`：跨境渔具老板、品牌 YOTO；店（Temu/AE/Amazon/Shopify/Woo/抖店/PDD）+ 视频（抖音/快手/TikTok/INS）；平台隔离纪律
- `MEMORY.md`：精简持久事实；**删除** Woo Consumer Key/Secret、Shopify 密码、生图 API Key 等密钥；抖店报表路径保留
- `SOUL.md`：YOTO 经营助手 +「问哪平台只答哪平台」
- 清空微信 session；备份至 `data/_bak_memories_<ts>/`
- 本地副本：`scripts/hermes-hooks/memories/`

**涉及文件**（远程）：`data/memories/USER.md`、`MEMORY.md`、`data/SOUL.md`

---

## 2026-07-23 16:02 — 经营日报对话打通（零 tool + 摘要注入）

**任务**：边调试边与 Hermes 对话「分析我的经营日报」，直到正确。

**对话实测（CLI / `--source weixin`）**：已成功，基于日报摘要回答（支付 ¥69,787 / 订单 3361 / 退款率 23.7%），`tool_turns=0`，无 400。

**根因与修复**：
- Gemini via grsai：只要请求带 tools 且模型发出 tool_calls → TUA 400
- 将 `platform_toolsets.weixin: [no_mcp]`，并 `disabled_toolsets` 覆盖 messaging/video/file/…，使 `get_tool_definitions` → **0 tools**
- `environment_hint` 注入最新 `*.summary.txt`（日报优先）
- 曾误改 YAML 已从备份恢复并用 PyYAML 重写

**注意**：`hermes send` 目前 channel_directory 无 weixin 目标（网关显示 0 targets），主动推送仍可能失败；**微信入站**依赖 gateway 长连接，请用户再发一句验证入站路径。

**涉及文件**（远程）：`data/config.yaml`、`SOUL.md`、`skills/douyin-shop-reports/`

---

## 2026-07-23 15:43 — 抖店 skill + 摘要注入（渐进披露口径）

**任务**：澄清工具渐进披露 / Excel tool / 提示词；落地只读分析路径。

**结论**：
- Hermes `tool_search` **只懒加载 MCP/插件工具**；`read_file`/`browser_*` 等 **core 每次都会带**，不是「每对话零 tool」。
- Skills 才是渐进披露（索引一行 + `skill_view` 拉全文）；此前误把 `skills` 放进 `disabled_toolsets`。
- **无专用 Excel tool**；容器默认 Python **无 pandas/openpyxl**。正确路径：watcher 已写的 `*.summary.txt`，或 zip/xml 自解析。
- 400 主因仍是 Gemini via grsai 的 tool-call TUA，不是「缺 excel tool」本身；问日报时模型一调 tool 就 400。

**改动**（远程）：
- 新增 skill：`skills/douyin-shop-reports/SKILL.md`
- 重写 `SOUL.md`（只读经营助手规则）
- `MEMORY.md` 登记报表路径
- `environment_hint` 注入最近 4 份 `.summary.txt`（优先无 tool 作答）
- `disabled_toolsets`：去掉 `skills`；保留禁用 browser/web/…；加禁 `terminal`
- 重启；微信 connected

**本地副本**：`scripts/hermes-hooks/skills/douyin-shop-reports/SKILL.md`

---

## 2026-07-23 15:21 — 微信 400：裁剪 Hermes 工具集

**任务**：`gemini-2.5-flash` 仍报 HTTP 400。

**根因**（日志）：
- 不再是「负载过高」，而是 grsai/`rix_api_error`：
  `model generated tool calls that are both known and unknown to the tool use handler, and the TUA is not configured to support tool context circulation.`
- Hermes 一次下发 **29** 个 tools（含整组 `browser_*`）；Gemini 经 grsai 代理时会混出内置/未知 tool call，代理拒绝。
- 直连复测：5 个文件类 tools 正常；29 个 tools 易超时/失败。

**改动**（远程 Hermes）：
- `agent.disabled_toolsets`：`browser` / `web` / `tts` / `vision` / `image_gen` / `delegation` / `skills` / `cronjob` / `session_search`
- 保留 file / code_execution / memory / todo / clarify / terminal（读抖店 Excel 够用）
- 清空微信 DM session（含主频道 `o9cq80w8...`），避免脏历史
- 重启容器；微信已 `connected`
- 备份：`config.yaml.bak_<ts>`、`sessions.json.bak_<ts>`

**请用户**：微信再发一次「哪个品卖的最好」验证。

**涉及文件**（远程）：
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/config.yaml`
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/sessions/sessions.json`

---

## 2026-07-23 14:59 — gpt-5.5 复测 + 改用 gemini-2.5-flash

**任务**：微信仍失败；复测 gpt-5.5，并换更轻模型。

**gpt-5.5 复测（直连 grsai）**：
- 不带 tools / 明确不调工具：正常返回 `content=ok`
- 带 tools 且需要工具（读文件/问卖最好的品）：HTTP 200，但 `message` 仅 `{role:assistant}`，无 content、无 tool_calls（仍计 completion_tokens）→ Hermes 判空
- 流式同理：有 finish=stop，content 空、无 tool_deltas

**当前微信失败（日志）**：`gemini-2.5-pro` → 超时后 `HTTP 400 The model load is too high`（rix_api_error，上游过载）

**改动**：
- `model.default` → `gemini-2.5-flash`
- 修复 cron 目录权限；重启 Hermes（微信 connected）

**涉及文件**（远程）：`data/config.yaml`

---
## 2026-07-23 14:43 — Hermes 模型改为 gemini-2.5-pro

**任务**：微信提示 `model returned no content after all retries`；改用 gemini。

**结论**：`gpt-5.5` 在 grsai 上带 tools 时常返回空 message（非 401）。

**改动**：
- `model.default`：`gpt-5.5` → `gemini-2.5-pro`（`base_url` 仍为 `https://grsaiapi.com/v1`）
- 备份：`config.yaml.bak-model-gemini`
- 已重启；微信 `connected`

**涉及文件**（远程）：
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/config.yaml`

---

## 2026-07-23 14:32 — 抖店日报目录监听 + 微信自动通知

**任务**：Hermes 感知 `douyin-reports` 新 Excel，自动汇总并推微信。

**改动**：
- 脚本：`/opt/data/scripts/douyin_report_watch.py`（新 xlsx/csv → 汇总文本）
- 主机监听：`douyin-report-watch.service`（每 5 秒轮询，`hermes send` 推微信）
- Hermes cron 备份：`every 1m` + `--no-agent` 投递 `weixin:<home>`
- 更新 `WEIXIN_HOME_CHANNEL` 为新绑定用户 `o9cq80w8...@im.wechat`
- 已对现有 `20260723_店铺经营日报.xlsx` 做一次强制汇总推送测试

**涉及文件**（远程）：
- `/opt/hermes-hooks/douyin_report_host_watch.py`
- `/etc/systemd/system/douyin-report-watch.service`
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/scripts/douyin_report_watch.py`

---

## 2026-07-23 14:25 — 抖店经营日报上传 Hermes

**任务**：第一阶段只读汇总；将数据工厂导出的店铺经营日报放到 Hermes 可读目录。

**改动**：
- 上传 `20260723_店铺经营日报.xlsx` → 服务器 Hermes `data/home/douyin-reports/`
- 容器内路径：`/opt/data/home/douyin-reports/20260723_店铺经营日报.xlsx`
- 附 `README.md` 说明字段与微信问法；本文件仅含店铺「YOTO欧鲤钓」近约 31 天日数据

**涉及文件**（远程）：
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/home/douyin-reports/*`

---

## 2026-07-23 13:12 — Hermes 模型切换为 Commander grsai

**任务**：微信侧 Hermes 调模型 401（lyncr Invalid token），改为使用 Commander 项目的 grsai API。

**改动**：
- 服务器 Hermes `data/.env`：`CUSTOM_API_KEY` 改为 Commander `lyncr.api_key`（grsai）
- `data/config.yaml`：`model.base_url` → `https://grsaiapi.com/v1`，`model.default` → `gpt-5.5`，`provider: custom`
- 已 `docker restart 1Panel-hermes-agent-RjGS`；微信平台状态 `connected`
- 切换前本地探测 `POST /v1/chat/completions`（gpt-5.5）返回 200
- 备份：`data/.env.bak-grsai-switch`、`data/config.yaml.bak-grsai-switch`

**涉及文件**（仅远程，未改本仓库代码）：
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/.env`
- `/opt/1panel/apps/hermes-agent/Hermes-Agent/data/config.yaml`

---

## 2026-07-24 — 1688 向导 M1 采集登录 + CDP 采集

**任务**：按 `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m1.md` 交付 M1。

**改动**：
- Server：`LISTING_1688_COOKIE_KEY` AES-GCM；Session open-login/confirm/validate；Collect/retry；状态机 `session_pending→session_valid→collecting→collected`
- Agent：四协议 `listing1688_*`；Rod 打开登录/导 Cookie/探针/拉详情；`offer1688` 移植自 `D:\reverselab\1688-\fetch_1688_offer.py`
- Web：向导 Step1/Step2 接通真实 API + 预览

**本地注意**：Server 环境变量须配置 `LISTING_1688_COOKIE_KEY`（Base64 32 字节）；Agent 在线且浏览器可打开 1688。

**涉及文件**：
- `commander-server-.../internal/services/listing1688_session.go`、`listing1688_collect.go`
- `commander-server-.../internal/utils/listing1688_cookie_crypto.go`
- `commander-agent-.../backend/internal/offer1688/`
- `commander-agent-.../backend/internal/factory/alibaba/listing1688.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `docs/superpowers/plans/2026-07-24-1688-listing-wizard-m1.md`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M0 复审项全部修复

**任务**：修完 M0 复审 Low/Nit（空列表、handler 单测、errors.Is、向导错误码、步骤高亮、删死文案）。

**改动**：
- Web：Agent 空态提示；步骤仅 active 高亮；删 `advanceLocked`；`listing1688Error.js` 补向导错误码
- Server：`errors.Is(err, gorm.ErrRecordNotFound)`；可注入假仓库的 Create/Get/Stub handler 单测；`NewSilentLoggerUtils`

**涉及文件**：
- `commander-server-.../internal/services/listing1688_workflow.go`
- `commander-server-.../internal/services/listing1688_workflow_handler_test.go`
- `commander-server-.../internal/services/default.go`
- `commander-server-.../internal/utils/logger.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `commander-web-.../src/utils/listing1688Error.js`
- `commander-web-.../src/locales/zh-CN.json`、`en.json`
- `开发文档.md`

---

## 2026-07-24 — 1688 向导 M0 审查小修（步骤浏览 + Stub 归属）

**任务**：按 M0 审查修复 Medium：① 步骤条可点浏览占位；② Stub 先校验 workflow 归属。

**改动**：
- Web：`goStep` + 步骤按钮；上/下一步可切换 Step1–4 占位；文案区分「浏览」与真实门禁
- Server：`listing1688LoadOwnedWorkflow`；Get/Stub 共用；无归属 → `LISTING_1688_WORKFLOW_NOT_FOUND`，有归属再 `NOT_IMPLEMENTED`

**涉及文件**：
- `commander-server-.../internal/services/listing1688_workflow.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `commander-web-.../src/locales/zh-CN.json`、`en.json`
- `开发文档.md`

---

## 2026-07-23 — 1688 向导 M0 空壳落地

**任务**：按 `docs/superpowers/plans/2026-07-23-1688-listing-wizard-m0.md` 交付 M0（表/枚举/路由空壳/Web 四步壳）。

**改动**：
- Server：`listing_1688_workflows` / `sessions` / `image_jobs` AutoMigrate；状态与错误枚举；`POST/GET /api/v1/listing1688/workflows` 可建/读 `draft`；Session/Collect/Images/Publish 返回 `LISTING_1688_NOT_IMPLEMENTED`
- Web：路由 `/1688-auto-upload/wizard` 四步壳；矩阵入口「竞品向导上架」；保留 v1 listing 入口
- 计划：`docs/superpowers/plans/2026-07-23-1688-listing-wizard-m0.md`

**涉及文件**：
- `commander-server-.../internal/constants/listing_1688_wizard.go`
- `commander-server-.../internal/modules/table_listing_1688_*.go`
- `commander-server-.../internal/services/listing1688_workflow.go`
- `commander-server-.../internal/router/register.go`
- `commander-web-.../src/views/platform-entrance/listing1688-wizard/index.vue`
- `commander-web-.../src/api/modules/listing1688.js`
- `开发文档.md`

---

## 2026-07-23 — 1688 向导 spec 包全文重写

**任务**：按场景 A 重新整理整套 spec，去掉立歪表述。

**改动**：重写 `1688-listing-wizard-v2/` 下 README、requirements、PRD、功能规格、技术分析、前期决议、测试用例；设计说明已对齐；`agents.md` 仍指向该 README。

**锁定摘要**：采竞品（移植 1688-）→ grsai 图优 → 自家 1688 商家后台发品；禁止妙手/店小秘作 Step4；M3 须另出商家发品方案。

---

## 2026-07-23 10:15 — 1688 向导锁定场景 A（发自家商家后台）

**任务**：产品确认 Step4 = 场景 A。

**改动**：README / 前期决议 / 功能规格 / 设计 / 技术分析 / PRD / agents 锁定：发品走 1688 商家后台自动化；禁止妙手/店小秘；v1 alibaba_collect_box 不作向导主路径。

---

**任务**：纠正 1688 上架全流程 spec「立歪」问题；更新各目录 agents 路径约定。

**改动**：
- 重写 `docs/superpowers/specs/1688-listing-wizard-v2/README.md` 正位表（主战场/采集源/ERP/妙手导入）
- 同步修订设计、前期决议、功能规格、技术分析、PRD
- `agents.md` 增加主战场与 `D:\reverselab\1688-` 硬性路径
- 新增 `D:\reverselab\1688-\AGENTS.md`；`open-reverselab` AGENTS/CLAUDE 增加路径路由

**涉及文件**：
- `docs/superpowers/specs/1688-listing-wizard-v2/*`
- `docs/superpowers/specs/2026-07-21-1688-listing-wizard-v2-design.md`
- `agents.md`
- （仓外）`D:\reverselab\1688-\AGENTS.md`、`D:\reverselab\open-reverselab\AGENTS.md`

---

**任务**：读取桌面 `产品表单.xlsx`，每 CSP 类目挑样本，生成可校验 JSON。

**结果**：
- 10 个业务 sheet → 3 个 CSP 类目，共 **511 商品 / 1722 SKU**
- 生成 7 个认证样本（钓钩×2、鱼饵×2、钓鱼工具×3），`ops_validate_json` **7/7 通过**
- 新增 `product_form_import.py`、`build_product_form_samples.py`

**涉及文件**：
- `inbox/product_form_samples/`（JSON + 占位图 + README）
- `exports/product_form_inventory.json`
- `uploader/csp_uploader/product_form_import.py`
- `scripts/build_product_form_samples.py`

---

**任务**：用户要求停止 TZ-QZ-010 实跑；分析「每个新 JSON 上架阻力大」根因；给出可复用方案；建立「每次上架写交接」规则。

**结论摘要**：
- JSON **数据结构**可复用（校验/入队/常量注入）；**销售属性 DOM 交互按类目不同**（钓钩 comma_batch ≠ 钓鱼工具 color_rows）。
- 已成功样本仅限钓钩；换类目等于 mini 项目，须 probe + 类目认证。
- 推荐：**类目认证制**（新 category_id 先 probe 再 worker）+ 中期 **按类目 sale_prop driver** + 同品类 **克隆发品** 减 DOM。

**改动**：
- 新增 `aliexpress-csp-auto-upload/docs/handover/上架流程-阻力分析与改进方案.md`
- 更新根目录 `交接文档.md` §1（TZ-QZ-010 停点、专项链接、每次上架必写规则）
- `publish_form.py` 已加 `color_rows`（未实跑 submit）

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/handover/上架流程-阻力分析与改进方案.md`
- `交接文档.md`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_form.py`
- `aliexpress-csp-auto-upload/templates/TZ-QZ-010.json`

**未 push**（用户未要求 commit）。

---

**任务**：另写偏运营的上架说明，便于「说明 + 模板 + 产品资料 → 大模型 → 新 JSON」。

**改动**：
- 新增 `templates/README-运营上架.md`（白话字段、类目属性、skus 规则、提示词示例、踩坑）
- `templates/README.md` 改为入口；技术说明仍在 `README-ops-json.md`
- skill 指向运营 README

**涉及文件**：
- `aliexpress-csp-auto-upload/templates/README-运营上架.md`
- `aliexpress-csp-auto-upload/templates/README.md`
- `aliexpress-csp-auto-upload/templates/README-ops-json.md`
- `.cursor/skills/ae-csp-browser-listing/SKILL.md`

---

## 2026-07-21 12:15 — 固化 JSON 模板并实跑成功

**任务**：审查模板能否完整走通上架；生成图片；用模板实跑；不通则改模板/管道。

**审查结论**：
- 字段面够用；但管道有洞：`cat_property` 未透传；供货价误写入「货品编码」（出现 `20.9`）导致绑定失败 `CHK_CHOICE_SC_ITEM_CODE_VALID`。

**改动**：
- 固化 `templates/ops_listing_template.json` + 图片目录 `images/TPL-DEMO-001/`
- 重写 `templates/README-ops-json.md`（必填/自动/可选 + outer_sku_id 硬规则）
- `ops_excel.job_to_publish_product` 透传 cat_property / detail_images
- `publish_form`：供货价按列表头定位；新增 `_fill_sku_outer_ids`
- `ops_worker`：绑定弹窗 dump/重试；成功判据含 `pop.publish.submit`
- `ops_jobs._pid_alive` Windows 锁修复

**实跑**：job `5daa6bc8-e45f-4e49-961b-9a28c1ca3416` → `submitted_success`，productId=`1005012774311145`

**涉及文件**：
- `aliexpress-csp-auto-upload/templates/ops_listing_template.json`
- `aliexpress-csp-auto-upload/templates/README-ops-json.md`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/{ops_excel,publish_form,ops_jobs}.py`
- `aliexpress-csp-auto-upload/scripts/ops_worker.py`
- `交接文档.md`

---

## 2026-07-21 11:10 — 给出运营 JSON 最新模板

**任务**：整理后续上架需提供的最新 JSON 模板。

**改动**：
- 新增 `templates/ops_listing_template.json`（多 SKU）
- 新增 `templates/ops_listing_template_single.json`（单 SKU）
- 新增 `templates/README-ops-json.md`
- skill `ae-csp-browser-listing` 指向上述模板

**涉及文件**：
- `aliexpress-csp-auto-upload/templates/ops_listing_template.json`
- `aliexpress-csp-auto-upload/templates/ops_listing_template_single.json`
- `aliexpress-csp-auto-upload/templates/README-ops-json.md`
- `.cursor/skills/ae-csp-browser-listing/SKILL.md`

---

## 2026-07-21 11:05 — 沉淀 ae-csp-browser-listing skill

**任务**：把已跑通的 CSP 浏览器上架全流程做成 Cursor skill。

**改动**：
- 新建 `.cursor/skills/ae-csp-browser-listing/`（`SKILL.md` / `SPEC.md` / `references/pitfalls.md`）
- 项目规则 `.cursor/rules/ae-csp-browser-listing.mdc`
- 用户 skills junction 指向项目 skill
- `aliexpress-csp-auto-upload/AGENTS.md` 增加 skill 入口

**涉及文件**：
- `.cursor/skills/ae-csp-browser-listing/**`
- `.cursor/rules/ae-csp-browser-listing.mdc`
- `aliexpress-csp-auto-upload/AGENTS.md`
- `交接文档.md`

---

## 2026-07-21 11:00 — HOOK-023 全流程提交成功

**任务**：用修好的海关抽屉 + comma_batch 多 SKU 重跑样本。

**改动**：无新代码；验证跑通。

**结果**：
- job `ab54ab90-cba2-4d88-adc9-b6908934f9f5` → `submitted_success`
- productId `1005012779918826`
- customs 三次均为 ok（seed/final/before_submit）；`sale_names comma_batch '8,6,4' count 0->5 ok=True`
- capture：`aliexpress-csp-auto-upload/captures/job_ab54ab90-…_20260721_105050`

**涉及文件**：
- `交接文档.md`（§1 状态刷新）

---

## 2026-07-21 10:55 — 海关抽屉通过 + 多SKU 逗号录入

**任务**：按用户指定跑海关 DOM/网络探测与多 SKU 选择器对齐，并落代码。

**改动**：
- 海关：`debug_customs_click3` 确认抽屉 `.hscode-drawer`，Kind=`其他(Other)`，按钮=`确 认` → tag=`已配置`；加固下拉等待。
- 多 SKU：证伪 plus/添加按钮与颜色 select；发现 `8,6,4`+Enter 可一次多 chip；`ensure_sale_prop_skus` 改为 `comma_batch`。
- 同步 `交接文档.md` §1。

**产物**：
- `exports/customs_click3.json`（HANDLER ok）
- `exports/sale_separators.json`（`8,6,4`→5）
- `exports/sale_add_second.json` / `sale_size_multi.json`

**下一步**：重跑 `HOOK-023` 全流程验证提交。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_form.py`
- `aliexpress-csp-auto-upload/scripts/debug_customs_click3.py`
- `aliexpress-csp-auto-upload/scripts/probe_sale_*.py`
- `交接文档.md`

---

## 2026-07-21 10:03 — 停步写交接（海关+多SKU探测未跑完）

**任务**：用户要求停下并更新交接文档，避免刷新记忆后从 0 开始。

**改动**：
- 重写 `交接文档.md` §0/§1：当前主线改为 JSON+浏览器填表（HOOK-023）；写清已打通/卡住/已改文件/下一步两条探测命令；接口 replay 降为 §1B。
- 海关探测 `debug_customs_click3.py` 本轮启动后被中断，未产出 `exports/customs_click3.json`。
- 多 SKU `probe_sale_add_second.py` 本轮未跑。

**下一步（接手即做）**：
1. `python scripts/debug_customs_click3.py`
2. `python scripts/probe_sale_add_second.py`
3. 根据产物改 `publish_form.py` 后再跑 HOOK-023

**涉及文件**：
- `交接文档.md`
- `开发文档.md`（本条）

---

## 2026-07-20 20:20 — HOOK-023 自动化：海关 + 多 SKU 优化

**任务**：补齐海关监管属性与多 SKU 销售属性/图片自动化，重跑 HOOK-023 全流程。

**改动**：
- `publish_form.py`：销售属性改为仅操作「选择主色系」下输入框（避免误填商品属性/型号）；多 SKU 时跳过颜色/型号；供货价按行填充 fallback；海关点击后等待 5s + 页面级确定；草稿后 seed customs + 详情后 final customs。
- `form_atlas.py`：销售属性/多 SKU 图校验；supply 行数覆盖时放宽销售属性。
- `ops_worker.py`：海关未 `已配置` 时阻断提交。
- `templates/HOOK-023.json`：销售属性名改为 `8`/`6`/`4`（`#` 会导致平台一次录入两个 chip）。

**验证**（job `cbd0d3ae-…`）：
- ✅ 类目/属性/主图/营销图/物流/详描/供货价三行（18.9/20.9/23.9）
- ✅ 销售属性首个值 `8`；supply 三行 fallback 通过
- ✅ SKU 图首行上传成功
- ❌ 海关监管属性仍为「请重新补全」（`去设置` 后未出现 `已配置` tag）
- ❌ 销售属性 6/4 无法继续添加（平台无可见「添加自定义名称」按钮）

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_form.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/form_atlas.py`
- `aliexpress-csp-auto-upload/scripts/ops_worker.py`
- `aliexpress-csp-auto-upload/templates/HOOK-023.json`

**未 push GitHub**（按用户规则未主动提交）。

---

## 2026-07-20 17:30 — JSON 运营录入方案

**任务**：用 JSON 替代 Excel 作为运营侧录入格式，链路更简洁。

**改动**：
- 新增 `uploader/csp_uploader/ops_json.py`：JSON 校验核心，复用 atlas schema 逻辑，自动注入全店固定常量。
- 新增 `scripts/ops_validate_json.py`：校验 listing.json，输出报告。
- 新增 `scripts/ops_enqueue_json.py`：校验通过后入队到 `jobs/`，与现有 worker 无缝对接。
- 新增 `templates/ops_simple.json`：完整示例模板（钓钩单SKU + 鱼饵多SKU + 钓鱼工具单SKU）。

**运营使用方式**：
```powershell
# 校验
python scripts/ops_validate_json.py listing.json
# 校验通过后入队
python scripts/ops_enqueue_json.py listing.json
# 跑 worker（不变）
python scripts/ops_worker.py
```

**固定常量（JSON 中无需填写，系统自动注入）**：
品牌、产地、是否磁铁垂钓系列、高关注化学品、是否原箱、物流属性、海关监管属性、关联制造商、关联欧盟责任人

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_json.py`
- `aliexpress-csp-auto-upload/scripts/ops_validate_json.py`
- `aliexpress-csp-auto-upload/scripts/ops_enqueue_json.py`
- `aliexpress-csp-auto-upload/templates/ops_simple.json`

## 2026-07-20 16:55 — 便捷模板固定合规常量更新

**任务**：把便捷模板中的合规固定常量对齐到现有已上架商品口径，并重新产出最新模板。

**改动**：
- `ops_excel_atlas_schema.py`：将 `关联制造商` 固定为 `不填`，`关联欧盟责任人` 固定为 `暂不关联`。
- `ops_excel.py`：更新 `固定常量` sheet 文案，明确两项按现有已上架商品口径固化。
- 调整测试覆盖，校验 simple 模式自动注入上述两项固定值。
- 重新生成 `templates/ops_simple.xlsx`。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel_atlas_schema.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel.py`
- `aliexpress-csp-auto-upload/tests/test_ops_excel_extended.py`
- `aliexpress-csp-auto-upload/templates/ops_simple.xlsx`

## 2026-07-20 15:40 — 运营便捷 Excel：全店固定字段自动注入

**任务**：固化用户口径——品牌/产地/磁铁垂钓/高关注化学品/原箱/物流/海关/关联制造商/关联欧盟责任人全店统一，运营只填其余字段。

**改动**：
- `ops_excel_atlas_schema.py`：`GLOBAL_UNIVERSAL_ATTRS`、`GLOBAL_PACKAGING_DEFAULTS`；`operator_product_columns` 排除全店固定列；`operator_category_attr_requirements` 校验时跳过固定项。
- `validate_listing_excel(..., simple=True)`：自动注入固定属性与包装默认值；支持 `商品信息` / `products` 双 sheet 名。
- 新增 `write_ops_listing_simple_template_xlsx`（sheet：商品信息、SKU明细、固定常量、类目必填差异项、填写说明）。
- 新增 `scripts/build_ops_listing_simple_template.py`；`ops_validate_excel.py` 新增 `--simple`。
- 产物：`templates/ops_listing_simple_template.xlsx`。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel_atlas_schema.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel.py`
- `aliexpress-csp-auto-upload/scripts/build_ops_listing_simple_template.py`
- `aliexpress-csp-auto-upload/scripts/ops_validate_excel.py`
- `aliexpress-csp-auto-upload/tests/test_ops_excel_extended.py`
- `aliexpress-csp-auto-upload/templates/ops_listing_simple_template.xlsx`

## 2026-07-20 15:10 — 扩展运营 Excel 模板（form_atlas 必填项展开）

**任务**：按 form_atlas 把三类目必填项展开为 Excel 列，支持长期运营上架数据录入。

**改动**：
- 新增 `ops_excel_atlas_schema.py`：从 `form_atlas/categories/*.yaml` 读取各类目必填/可选属性，生成 `attr_*` 列定义。
- `validate_listing_excel(..., extended=True)`：按类目校验 `attr_*` 必填项，合并 `attr_json` 覆盖，解析 `detail_images`，输出完整 `cat_property/description/detail_images` 合同。
- 新增 `write_extended_template_xlsx` 与 `scripts/build_ops_listing_extended_template.py`。
- `ops_validate_excel.py` 新增 `--extended`。
- 产物：`templates/ops_listing_extended_template.xlsx`（含 `products/skus/field_guide/readme` 四个 sheet）。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel_atlas_schema.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel.py`
- `aliexpress-csp-auto-upload/scripts/build_ops_listing_extended_template.py`
- `aliexpress-csp-auto-upload/scripts/ops_validate_excel.py`
- `aliexpress-csp-auto-upload/tests/test_ops_excel_extended.py`
- `aliexpress-csp-auto-upload/templates/ops_listing_extended_template.xlsx`

## 2026-07-20 15:00 — 固化运营 Excel 批量闭环硬规则

**任务**：把交接文档中的五条运营批量规则固化到 batch gate 代码与 runbook。

**改动**：
- 新增 `batch_operational_guard.py`：默认 5 行上限、`--override-limit` 需 `--confirm-cross-category`、同类目 `cat_id` 校验、live 失败即停判定。
- `excel_batch_publish_gate` / `excel_batch_draft_gate` 接入门禁；live 任一行失败立即停止（`batch_failed_stopped`）；混合类目返回 `cross_category_batch_blocked`。
- CLI 新增 `--confirm-cross-category`；runbook 增补「运营批量硬规则」表与小批量 replay 发布步骤。
- 测试：新增 `test_batch_operational_guard.py`，调整 batch gate 失败即停与跨类目阻断用例。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/batch_operational_guard.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_batch_draft_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_batch_draft_gate.py`
- `aliexpress-csp-auto-upload/docs/excel-to-csp-listing-runbook.md`
- `aliexpress-csp-auto-upload/tests/test_batch_operational_guard.py`
- `交接文档.md`

## 2026-07-20 14:44 — CSP 小批量 replay 发布 gate 延迟复查通过

**任务**：把批量 gate 的 `not_found` 改成延迟 ID-only 复查策略，并限量重跑 3 行 live 小批量。

**改动**：
- `run_excel_batch_publish_gate.py` 新增 `--visibility-recheck-attempts`、`--visibility-recheck-delay-s`、`--visibility-recheck-wait-ms`。
- `excel_batch_publish_gate` 在单行返回 `publish_passed_visibility_not_found` 且存在速卖通 `product_id` 时触发 ID-only 只读复查；复查命中 `pending_approval/online_product` 后升级行状态并补写清理台账。
- 子进程 stdout/stderr 增加 `errors=replace`，避免 Windows 非 UTF-8 输出导致批量 runner 崩溃。
- live 重跑 `captures/batch_publish_replay_recheck_20260720_143813`：总报告 `ok=true/status=batch_publish_ready/passed_count=3/failed_count=0`；R1=`1005012766587831`、R2=`1005012773670015`、R3=`1005012766721648` 均在 `pending_approval`。
- 更新 CSP 需求文档、live 验证文档和根目录交接文档；明确同类目 3 行已通过，但仍不得扩大为无上限/跨类目批量。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_batch_publish_gate_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 15:05 — CSP 发布响应与列表搜索诊断

**任务**：继续定位小批量 replay 发布中 `publish.submit SUCCESS -> visibility not_found` 的原因，检查发布响应业务字段与列表搜索参数。

**改动**：
- `classify_publish_response()` 增加 `response_summary.data_summary`，保留 `data_keys/nested_data/models_query/trace_id`，后续可判断商品 ID 来自速卖通响应的哪个业务字段。
- `draft_visibility` 新增 `list_request_summary_from_url()`，可解析列表 mtop 请求中的 `channel_id/tab/pagination/filter.*` 搜索字段；`find_product_visibility()` 增加 `matched_payload_index`。
- `check_draft_visibility.py` 和发布链内部可见性检查均输出 `request_summaries`，记录搜索动作输入框、实际 query、按钮文案和列表请求摘要。
- 只读复查 R1/R2：`1005012766383628`、`1005012773078614` 均已在 `pending_approval` 可见；R1 的 `matched_payload_index=0`，说明命中发生在基础 `pending_approval` 响应而不是搜索后。
- 更新 CSP 需求文档、live 验证文档和根目录交接文档；修正结论为“首轮可见性窗口不足/异步延迟”，下一步应补延迟 ID-only 复查策略后再重跑 3 行。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_visibility.py`
- `aliexpress-csp-auto-upload/scripts/check_draft_visibility.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_draft_visibility.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 14:25 — CSP 小批量 replay 发布 gate 与可见性边界

**任务**：继续 CSP 全托管接口回放闭环，落地 3-5 行 Excel/product JSON 小批量 replay 发布 gate，并审查 live 试跑结果。

**改动**：
- 新增 `run_excel_batch_publish_gate.py` 和 `excel_batch_publish_gate`，默认最多 5 行，live 必须 `--confirm-publish`，每行复用单行 `--publish-mode replay`。
- 批量行执行改为独立子进程，并新增 `--row-timeout-s`，避免某一行浏览器/网络卡住后拖死整批；超时会写 `row_timeout` 行级报告。
- 新增批量发布 gate 单元测试和 CLI 测试，覆盖限量、确认发布、授权失败停止、行级失败继续、超时报告与 CLI 参数透传。
- live 试跑 `captures/batch_publish_replay_20260720_141206`：R1/R2 均由速卖通返回真实商品 ID（`1005012766383628`、`1005012773078614`），但列表可见性为 `not_found`；R3 未执行，旧版批量 runner 卡住后已终止。
- 更新 CSP 需求文档、live 验证文档和根目录交接文档；明确当前不能继续盲发更多行，下一步应定位 `publish.submit SUCCESS -> visibility not_found` 的原因。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_excel_batch_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_batch_publish_gate_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 14:05 — CSP Excel 单行 replay 发布 gate 接入

**任务**：继续 CSP 全托管接口回放闭环，把 `switch.submit -> publish.submit` 接口发布路径接入 Excel 单行发布 gate，并用真实账号验证审核中可见。

**改动**：
- `run_excel_publish_gate.py` 新增 `--publish-mode replay`，支持通过 `capture_dir + product_path` 生成 `publish_chain_payload.json` 后直接执行接口回放发布，不再要求旧 `draft_id`。
- `excel_publish_gate` 增加 product JSON 归一化：从 `skus[0].outer_sku_id/price/stock` 补齐 `sku_code/price/quantity`，避免 Excel 模板式商品丢 SKU/价格/库存。
- `publish_chain` 将 render 编辑页 `PUB_TRANSFER_ERROR::系统转换异常` 降级为诊断信息，并继续做 ID-only 列表可见性校验；底层列表检查增加搜索过滤，避免只扫第一页误判 `not_found`。
- replay 发布命中可见状态后写 `cleanup_ledger.jsonl` 与 `cleanup_report.json`；本轮实际商品 `1005012766099994` 已 ID-only 复查位于 `pending_approval` 并补写清理台账。
- 更新 CSP 需求文档、live 验证文档和根目录交接文档；明确下一步只允许 3-5 行小批量 Excel replay gate，仍不得直接放开批量发布。

**涉及文件**：
- `aliexpress-csp-auto-upload/scripts/run_excel_publish_gate.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_publish_gate.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_excel_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_publish_gate_cli.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain_runner.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 14:00 — CSP 接口回放发布进入审核中

**任务**：在用户授权后继续 CSP 全托管接口回放发布验证，确认单个商品能否真实进入速卖通审核队列。

**改动**：
- 诊断草稿页面按钮发布路径：补齐供货价、营销图、原箱、重量、尺寸、物流属性后仍未触发 `publish.submit`，该路径不作为当前主闭环。
- 基于历史成功 capture 生成 `switch.submit -> publish.submit` 受控 payload，并给 `run_publish_chain.py` 补齐 mtop token 过期单次重试与报告字段。
- live 实发生成速卖通侧 `product_id/switch_id=1005012766189291`；虽然发布后 render 返回 `PUB_TRANSFER_ERROR::系统转换异常`，但 ID-only 列表复查确认该商品位于 `pending_approval`。
- 更新 CSP 需求文档、live 验证文档和根目录交接文档；明确当前只证明“单个接口回放发布进入审核队列”，仍不能扩大为 Excel 批量上架完成。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 11:59 — CSP 单行 live 草稿 gate 通过

**任务**：在用户授权后执行当前候选 payload 的单行 live 草稿 gate，验证速卖通是否真实生成并承认可见草稿对象。

**改动**：
- 执行 `scripts/run_excel_draft_gate.py`，使用 `tenant5/channelId=870088` 和候选 payload `AE-CSP-Auto-Test.json`，输出到 `captures/excel_draft_gate_live_AE-CSP-Auto-Test_20260720_115841`。
- live 报告返回 `ok=true/status=draft_visible/live=true`，`new_product_id=1005012765475947`，真实 `draft` tab 通过 `draft_id` 匹配可见。
- 清理台账已生成：`cleanup_ledger.jsonl` 与 `cleanup_report.json`，其中 `draft_visible_count=1`。
- 更新 CSP live 前置审计、需求文档和根目录交接文档；明确下一步进入 3-5 行小批量草稿 gate，仍不得直接批量发布。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 11:38 — CSP live 前置审计落地

**任务**：继续执行 CSP Excel 到上架闭环计划，在真实 live 前先确认本地候选 payload、报告、脚本入口与授权边界是否齐备。

**改动**：
- 新增 `docs/csp-live-validation-readiness.md`，记录只读审计结果、候选 payload、单行 live 草稿 gate 命令、通过标准、停止条件和后续小批量/发布顺序。
- 在 CSP 需求文档追加第 26 节，明确当前已具备单行 live 验证前置条件，但不能把 dry-run 成功当成速卖通已承认对象。
- 更新根目录交接文档，把下一步收敛为“用户明确授权后先跑单行 live 草稿可见性”，并标注不读取 cookie 内容、不执行真实保存/发布。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `交接文档.md`
- `开发文档.md`

## 2026-07-20 11:31 — CSP 操作 runbook 与硬停止条件完成

**任务**：继续执行 CSP Excel 到上架闭环计划，落地操作手册和硬停止条件，防止后续执行时跳过关键 gate。

**改动**：
- 新增 `docs/excel-to-csp-listing-runbook.md`，按 6 步固化操作：Validate Excel、Generate payload dry-run、Run one-row live draft save、Review cleanup ledger、Run small-batch draft save、Only publish selected visible drafts with explicit confirmation。
- 在 runbook 中写明输入边界、每步命令、验收标准、真实 live 授权要求和闭环完成定义。
- 在需求文档新增 runbook 索引章节，固化 5 条硬停止条件：Excel 校验失败、`image_upload_required`、dry-run 失败、单行草稿不可见、发布后未进入 `pending_approval/online_product` 均停止。
- 更新闭环计划 Task 8 和根目录交接文档；明确本轮只做文档固化，未执行真实 live 草稿保存或发布。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/excel-to-csp-listing-runbook.md`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `rg -n "STOP if|Only publish selected visible drafts|Validate Excel|Generate payload dry-run" docs/excel-to-csp-listing-runbook.md docs/csp-template-replay-requirements.md` -> 已命中 runbook 步骤与硬停止条件。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 11:28 — CSP 显式发布 gate 外壳完成

**任务**：继续执行 CSP Excel 到上架闭环计划，补齐单个已可见草稿的显式发布 gate，明确发布后可见性才是成功口径。

**改动**：
- 新增 `excel_publish_gate`：真实发布必须 `--live --confirm-publish --draft-id --account --channel-id`，一次只处理一个草稿。
- 新增 CLI `scripts/run_excel_publish_gate.py`，输出 `excel_publish_gate_report.json` 与底层 `publish_run.json`。
- `publish_chain` 新增发布后 tab 分类：`pending_approval/online_product` 成功，`violation_qc_failed` 为可诊断失败，`draft/offline_product/not_found` 不算发布完成。
- 默认 live runner 从草稿编辑页点击提交，捕获 `publish.submit` 后再查真实列表 tab；本轮只通过测试验证，未执行 live。
- 更新 CSP 需求文档、闭环计划和根目录交接文档；明确 Excel 到上架闭环仍需真实单行/小批量/发布可见性验证。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_publish_gate.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_excel_publish_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_publish_gate_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_excel_publish_gate.py -q`：先因缺少 `uploader.csp_uploader.excel_publish_gate` 失败，实装后 5 passed。
- `python -m pytest tests/test_excel_publish_gate.py tests/test_run_excel_publish_gate_cli.py -q` -> 7 passed。
- `python -m pytest tests/test_excel_publish_gate.py tests/test_run_excel_publish_gate_cli.py tests/test_publish_chain.py tests/test_publish_chain_runner.py tests/test_run_publish_chain_cli.py -q` -> 23 passed。
- 未触发真实发布、未保存新草稿、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 11:15 — CSP 小批量草稿 gate 外壳完成

**任务**：继续执行 CSP Excel 到上架闭环计划，补齐 3-5 行小批量草稿试跑外壳，仍保持真实 live 保存必须显式确认。

**改动**：
- 新增 `excel_batch_draft_gate`：从 payload 目录逐行调用单行草稿 gate，默认最多 5 行，live 必须 `--live --confirm-live --account --channel-id`。
- live 模式增加账号/channel 本地锁，避免同一账号/channel 并发写草稿；普通行失败继续汇总，鉴权/账号/channel 失败立即停止。
- 新增 CLI `scripts/run_excel_batch_draft_gate.py`，输出 `excel_batch_draft_gate_report.json` 与行级 `rows/<row_id>/` 报告目录。
- 新增测试覆盖批量上限、行级失败继续、鉴权失败停止、dry-run 不需要 live 确认、CLI dry-run 与 live guard。
- 更新 CSP 需求文档、闭环计划和根目录交接文档；明确本轮未执行真实 live 小批量。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_batch_draft_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_batch_draft_gate.py`
- `aliexpress-csp-auto-upload/tests/test_excel_batch_draft_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_batch_draft_gate_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_excel_batch_draft_gate.py -q`：先因缺少 `uploader.csp_uploader.excel_batch_draft_gate` 失败，实装后 4 passed。
- `python -m pytest tests/test_excel_batch_draft_gate.py tests/test_run_excel_batch_draft_gate_cli.py -q` -> 6 passed。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:59 — CSP 清理台账完成

**任务**：继续执行 CSP Excel 到上架闭环计划的清理策略，确保真实草稿试跑可见后有可人工清理的台账。

**改动**：
- 新增 `cleanup_ledger`：JSON Lines 记录 `created_at_utc/row_id/account/channel_id/title/product_id/status/visibility_tab`。
- `excel_draft_gate` 在 live 且 `draft_visible` 后写 `cleanup_ledger.jsonl` 与 `cleanup_report.json`；dry-run 不写 ledger。
- 新增测试覆盖 ledger append、summary/report 写入，以及 mock live draft visible 后自动落账。
- 更新 CSP 需求文档、闭环计划和根目录交接文档；明确本轮没有执行真实 live。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/cleanup_ledger.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_draft_gate.py`
- `aliexpress-csp-auto-upload/tests/test_cleanup_ledger.py`
- `aliexpress-csp-auto-upload/tests/test_excel_draft_gate.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_cleanup_ledger.py tests/test_excel_draft_gate.py -q`：先因缺少 `uploader.csp_uploader.cleanup_ledger` 失败，实装后 7 passed。
- `python -m pytest tests/test_cleanup_ledger.py tests/test_excel_draft_gate.py tests/test_run_excel_draft_gate_cli.py tests/test_draft_visibility.py tests/test_draft_runner.py -q` → 24 passed。
- `python scripts\run_excel_draft_gate.py captures\valid_object_tenant5_870088_20260720_095019\training_chain\draft_payloads\payloads\AE-CSP-Auto-Test.json --out captures\excel_draft_gate_dry --limit 1` → `ok=true/status=dry_run_passed/live=false/cleanup_ledger_path=""`。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:53 — CSP 单行草稿 gate 外壳完成

**任务**：继续执行 CSP Excel 到上架闭环计划的单行草稿门槛，先落地 `--live --confirm-live --limit 1` 保护和草稿可见性报告形状。

**改动**：
- 新增 `excel_draft_gate`：支持 dry-run、本地报告、live 显式确认、单行限制、live 后 `draft` tab 可见性检查。
- 新增 CLI `scripts/run_excel_draft_gate.py`：默认 dry-run；真实保存必须传 `--live --confirm-live`，并提供账号与 channel。
- 新增测试覆盖 live 未确认拒绝、`limit != 1` 拒绝、dry-run 报告写入、mock live 后必须检查 `draft` tab 可见。
- 更新 CSP 需求文档、闭环计划和根目录交接文档；明确本轮未执行真实保存草稿。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_draft_gate.py`
- `aliexpress-csp-auto-upload/scripts/run_excel_draft_gate.py`
- `aliexpress-csp-auto-upload/tests/test_excel_draft_gate.py`
- `aliexpress-csp-auto-upload/tests/test_run_excel_draft_gate_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_excel_draft_gate.py -q`：先因缺少 `uploader.csp_uploader.excel_draft_gate` 失败，实装后 4 passed。
- `python -m pytest tests/test_excel_draft_gate.py tests/test_run_excel_draft_gate_cli.py tests/test_draft_visibility.py tests/test_draft_runner.py -q` → 21 passed。
- `python scripts\run_excel_draft_gate.py captures\valid_object_tenant5_870088_20260720_095019\training_chain\draft_payloads\payloads\AE-CSP-Auto-Test.json --out captures\excel_draft_gate_dry --limit 1` → `ok=true/status=dry_run_passed/live=false`。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:40 — 1688 工作台命名与上架入口

**任务**：前端 `/1688-auto-upload` 无上架入口；将「1688 采购矩阵」改为「1688 工作台」，并在矩阵页加入自动上架入口。

**改动**：
- 路由拆分：`/1688-auto-upload` 为工作台矩阵页；`/1688-auto-upload/listing` 及 `/listing/agent/:id` 为自动上架链路。
- `MatrixSection1688.vue` 顶部新增「进入自动上架」卡片，跳转 listing 子路径。
- `platformMatrixConfigs` / 导航 / i18n：标题改为「1688 工作台」，上架页文案改为「1688 自动上架」。
- `auto-upload.vue` / `AgentWorkspace.vue` 修正 1688 路径与返回工作台按钮。

**涉及文件**：
- `commander-web-t260220-main/.../src/router/index.js`
- `commander-web-t260220-main/.../src/views/platform-entrance/components/matrix/MatrixSection1688.vue`
- `commander-web-t260220-main/.../src/views/platform-entrance/auto-upload.vue`
- `commander-web-t260220-main/.../src/views/platform-entrance/components/auto-upload/AgentWorkspace.vue`
- `commander-web-t260220-main/.../src/config/platformMatrixConfigs.js`
- `commander-web-t260220-main/.../src/config/adminPlatformNav.js`
- `commander-web-t260220-main/.../src/styles/matrix-placeholder.css`
- `commander-web-t260220-main/.../src/locales/zh-CN.json`
- `commander-web-t260220-main/.../src/locales/en.json`

## 2026-07-20 10:46 — CSP 图片替换门禁完成

**任务**：继续执行 CSP Excel 到上架闭环计划的图片上传/替换门禁，防止新商品 payload 沿用 seed 样本图片 URL。

**改动**：
- 新增 `image_payload_rewriter`：识别 `main_images/detail_images/sku_list[].image` 中的本地路径，缺少上传映射时返回 `image_upload_required`。
- `generate_draft_payload` 新增 `uploaded_images` 参数；有映射时替换 `mainImage[*].url` 与 `sku[*].skuImage.url`，无映射时拒绝本地图片 payload。
- 更新 Gate 2 测试边界：已上传 URL 可继续 dry-run；本地图片会停在 payload 阶段，不再误报 payload ready。
- 更新 CSP 需求文档、闭环计划和根目录交接文档。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/image_payload_rewriter.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_generator.py`
- `aliexpress-csp-auto-upload/tests/test_image_payload_rewriter.py`
- `aliexpress-csp-auto-upload/tests/test_payload_generator.py`
- `aliexpress-csp-auto-upload/tests/test_excel_payload_pipeline.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_image_payload_rewriter.py tests/test_payload_generator.py -q`：先因缺少 `uploader.csp_uploader.image_payload_rewriter` 失败，实装后通过。
- `python -m pytest tests/test_image_payload_rewriter.py tests/test_payload_generator.py tests/test_payload_batch.py -q` → 14 passed。
- `python -m pytest tests/test_excel_payload_pipeline.py tests/test_payload_batch.py tests/test_draft_run_batch.py -q` → 11 passed。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:39 — CSP Gate 2 Excel payload dry-run 完成

**任务**：继续执行 CSP Excel 到上架闭环计划的 Gate 2，把已校验 Excel 转成 draft payload dry-run 链路，并明确失败停止边界。

**改动**：
- 新增 `excel_payload_pipeline`：Excel 先走 `validate_listing_excel`，全部行通过后才写 `products.json`、调用 seed profile 生成 `draft_payloads/`，再写 `draft_runs/` dry-run 报告。
- 新增 CLI `scripts/build_excel_draft_payloads.py`，要求显式 `--dry-run`；未传 dry-run 或 Excel 任意行失败均不会触发 live draft。
- 新增测试覆盖成功链路、Excel 失败即停止、CLI 退出码；同步更新 CSP 需求文档、闭环计划和根目录交接文档。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/excel_payload_pipeline.py`
- `aliexpress-csp-auto-upload/scripts/build_excel_draft_payloads.py`
- `aliexpress-csp-auto-upload/tests/test_excel_payload_pipeline.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_excel_payload_pipeline.py -q`：先因缺少 `uploader.csp_uploader.excel_payload_pipeline` 失败，实装后 3 passed。
- `python -m pytest tests/test_excel_payload_pipeline.py tests/test_payload_batch.py tests/test_draft_run_batch.py -q` → 10 passed。
- `python scripts\build_excel_draft_payloads.py inbox\sample_batch\listing.xlsx --profiles-dir captures\valid_object_tenant5_870088_20260720_095019\training_chain\template_profiles --out captures\excel_gate2_sample --dry-run` → `ok=false/status=excel_validation_failed`，`excel_ok=2 excel_failed=3 payload_count=0`，符合样例坏行停止边界。
- 未触发真实保存草稿、未发布商品、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:34 — CSP Gate 1 Excel 标准输入合同完成

**任务**：继续执行 CSP Excel 到上架闭环计划的 Gate 1，让 Excel 校验通过的行输出后续 payload 可消费的标准 CSP product JSON。

**改动**：
- `validate_listing_excel` 对 OK 行新增标准字段：`source_id`、`cat_id`、`title_en`、`material`、`main_images`、`detail_images`、`sku_list`。
- 新增测试 `test_validate_listing_excel_outputs_csp_product_contract`，先验证缺字段失败，再实现映射转绿。
- 更新 CSP 需求文档、闭环计划和根交接文档：Gate 1 已完成；下一步进入 Gate 2（Excel product JSON -> draft payload dry-run）。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/ops_excel.py`
- `aliexpress-csp-auto-upload/tests/test_ops_excel_validate.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_ops_excel_validate.py::test_validate_listing_excel_outputs_csp_product_contract -q`：先失败于 `KeyError: 'source_id'`，实现后通过。
- `python -m pytest tests/test_ops_excel_validate.py tests/test_ops_jobs.py -q` → 8 passed。
- `python scripts\ops_validate_excel.py inbox\sample_batch\listing.xlsx --out exports\validate_gate1.json` → `ok=2 fail=3`，失败行为样例内故意坏数据。
- 未触发真实发布、未保存草稿、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:34 — CSP Excel 到上架闭环实施规划

**任务**：规划后续如何让 AliExpress CSP 从 Excel 输入到草稿/发布形成真正闭环，并明确不能把 dry-run 误判为上架能力。

**改动**：
- 新增实施计划 `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`，按 Gate 拆分为 Excel 标准化、payload dry-run、图片替换、单行真实草稿、小批量草稿、显式发布和 runbook。
- 更新根目录交接文档，新增“闭环定义”：必须通过 Excel 校验、payload 净化、真实草稿可见、小批量清理台账、发布后队列可见，才能称为 Excel 到上架闭环。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- 只新增/更新规划文档；未改业务代码，未触发真实发布，未部署服务器，未写入 cookie/Token/密码。

## 2026-07-20 10:26 — CSP Excel 直连上架边界确认

**任务**：确认当前能力是否已经支持 Excel 直接上架产品，并明确后续边界，避免把 seed payload dry-run 误判为生产发布能力。

**改动**：
- 在 CSP 需求文档新增“Excel 直连上架能力边界”：当前只验证到 `真实可见草稿 capture -> seed training sample -> seed profile -> draft payload -> dry-run`，未验证 Excel 多行、图片替换、真实保存草稿、发布与审核队列可见性。
- 更新根目录交接文档：明确现在不能直接承诺 Excel 自动上架；Excel 下一步只能作为标准商品数据输入，先跑 payload dry-run 和单行真实保存草稿。
- 明确后续不应一味补样本，而应按 Gate 推进：Excel schema -> payload dry-run -> 单行真实草稿 -> 小批量草稿 -> 显式确认发布。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- 只改文档；未触发真实发布、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:16 — CSP 可见草稿 seed 训练链路

**任务**：基于真实可见草稿对象继续做 payload 训练样本和模板灌入链路，不再把问题归因到“保存未落库”。

**改动**：
- `aliexpress-csp-auto-upload` 新增 `visible_draft_training_chain`：从 `record_valid_object_chain` capture 校验 `draft_visible`，提取 `publish.render`，必要时用同次 `draft.submit.post_data.jsonBody` 回填 render 空字段，生成 raw sample、sample pool、seed profile、draft payload 并 dry-run。
- `template_profile` 新增 `allow_single_sample_seed`，把真实可见草稿单样本显式标记为 `single_visible_draft_seed`，可继续生成 payload，但保留“需要第二样本提升 diff 置信度”的 warning。
- `payload_batch` 支持现有 UI 录制模板格式：`category_id/title/skus/extras.cat_property.材质` 会归一成 `cat_id/title_en/sku_list/material`。
- 更新 CSP 需求文档与根目录交接文档，明确当前已生成 `visible_draft_payload_ready`，下一步风险是第二样本和图片替换策略。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/visible_draft_training_chain.py`
- `aliexpress-csp-auto-upload/scripts/build_visible_draft_training_chain.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/template_profile.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_batch.py`
- `aliexpress-csp-auto-upload/tests/test_visible_draft_training_chain.py`
- `aliexpress-csp-auto-upload/tests/test_template_profile.py`
- `aliexpress-csp-auto-upload/tests/test_payload_batch.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `交接文档.md`
- `开发文档.md`

**验证**：
- `python -m pytest tests/test_template_profile.py tests/test_payload_generator.py tests/test_payload_batch.py tests/test_batch_replay_pipeline.py tests/test_visible_draft_training_chain.py tests/test_draft_runner.py tests/test_draft_run_batch.py -q` → 28 passed
- `python scripts\build_visible_draft_training_chain.py captures\valid_object_tenant5_870088_20260720_095019 products\template_100005539.json --out captures\valid_object_tenant5_870088_20260720_095019\training_chain` → `ok=true/status=visible_draft_payload_ready`，`sample_total=1/profile_count=1/payload_count=1/dry_run_passed_count=1`
- 未触发真实发布、未部署服务器、未写入 cookie/Token/密码。

## 2026-07-20 10:15 — 1688 采集图优上架 MVP 实现（Task 1–7）

**任务**：按 Subagent-Driven 计划落地 1688（国内）采集图优再上架，与速卖通严格隔离。

**改动**：
- **Server**：`listing_1688_error.go` 枚举 + 副图上限 9；`listing_1688_jobs` 审计表；`RunListing1688` 旁路（Onebound 采集 → **grsai** 图生图 → Agent）；业务口 `produce_images` / `AiEditImage`；**禁止** Lyncr；`AgentProductIssue` 支持 `peer_url` 表单提交。
- **Web**：`/1688-auto-upload` 改走真实自动上架页；`PeerLinkUpload` 粘贴同行链接；错误码映射 `listing1688Error.js`。
- **Agent**：注册 `Platform1688` → `AlibabaFactory`；`RestyMiaoshou1688Utils` + 1688 导入 xlsx + `alibaba_collect_box/processImportCopyV2`（非 AE API）。

**涉及文件**（主要）：
- `commander-server-.../internal/constants/listing_1688_error.go`
- `commander-server-.../internal/tasks/proto_listing_1688.go`、`listing_1688_collect.go`
- `commander-web-.../src/views/platform-entrance/components/auto-upload/PeerLinkUpload.vue`
- `commander-agent-.../backend/internal/factory/alibaba/`、`resty_miaoshou_1688.go`
- `docs/superpowers/plans/2026-07-20-1688-listing-img2img.md`

**验证**：
- Server: `go test ./internal/tasks/ ./internal/constants/` → pass
- Agent: `go test ./backend/internal/factory/alibaba/` → pass
- 端到端妙手 1688 导入需联调抓包核对（见 `commander-agent-.../docs/miaoshou-1688-import-notes.md`）

**未 commit/push**（待用户明确要求）。

## 2026-07-20 10:06 — 根目录交接文档断点规则加固

**任务**：按要求把交接文档落到根目录，并明确每走完一步都要更新交接文档，避免长会话断点导致逻辑丢失。

**改动**：
- 重写根目录 `交接文档.md` 为断点恢复优先结构：顶部新增“断点恢复硬规则”，明确每个改变事实状态、代码状态、线上状态或关键判断的步骤都必须同步更新。
- 在交接文档中补入 AliExpress CSP 当前主线状态：有效对象 `1005012770682998` 已保存草稿并在真实 `draft` tab 可见；上一轮 `draft_visibility_not_found` 已确认为检查器 tab 误判。
- 补充 CSP 下一步、关键脚本、验收口径和安全禁忌，方便下一轮直接接手。

**涉及文件**：
- `交接文档.md`
- `开发文档.md`

**验证**：
- 文档行数未达到归档阈值；未改业务代码；未写入 cookie、Token、密码或配置密钥。

## 2026-07-20 10:05 — 1688 采集图优上架：决策锁定 + 实施计划

**任务**：确认 1688 MVP 三项产品决策，与速卖通严格隔离，并写出可执行实施计划。

**改动**：
- 锁定决策：① MVP **必须**打通妙手 **1688** 导入；② 入口优先粘贴同行链接；③ 副图图优上限默认 **9**。
- 同步更新设计说明、需求、PRD、技术分析、文档包 README；明确 `PUBLISH_UNSUPPORTED` 不可作为 MVP 验收通过口径。
- 硬性隔离：1688（国内）≠ 速卖通 AliExpress（国外）；禁止混用工厂 / AE Resty / 导入 API。
- 新增实施计划：`docs/superpowers/plans/2026-07-20-1688-listing-img2img.md`（Server 旁路 `RunListing1688` + Web 链接入口 + Agent `AlibabaFactory` 妙手 1688）。

**涉及文件**：
- `docs/superpowers/specs/2026-07-20-1688-listing-img2img-design.md`
- `docs/superpowers/specs/1688-listing-img2img/*`
- `docs/superpowers/plans/2026-07-20-1688-listing-img2img.md`

**验证**：文档决策表与计划 Global Constraints 已对齐；未改业务代码；未 commit/push（待用户指定执行方式）。

## 2026-07-20 10:02 — CSP 草稿可见性误判修正

**任务**：继续追查“保存草稿未落库”问题，确认当前账号/channel 的真实草稿可见性与有效对象保存链路。

**改动**：
- 修正 `check_draft_visibility.py`：解析 `render.list` URL 中 `data.jsonBody.tab` 的真实 tab，并主动点击目标列表 tab，避免把默认 `online_product` 响应误标为 `draft`。
- 扩展 `draft_visibility` 报告：新增 `sample_rows_by_tab`，并在脚本层过滤非目标 tab 响应。
- 新增 `capture_existing_draft_save_chain.py`：从真实草稿 tab 选择草稿、打开编辑页、点击保存并抓取 mtop/DOM/可见性，用作保存链路基准。
- 复核结论：上一轮 `draft_visibility_not_found` 是检查器误判；修正后 `1005012763843765`、`1005012770666191`、新建 `1005012770682998` 均在真实 `draft` tab 可见。
- 更新 CSP 模板回放需求文档，改正“未落库”结论，明确真实验收口径是解析真实 tab 后的列表可见性。

**涉及文件**：
- `aliexpress-csp-auto-upload/scripts/check_draft_visibility.py`
- `aliexpress-csp-auto-upload/scripts/capture_existing_draft_save_chain.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_visibility.py`
- `aliexpress-csp-auto-upload/tests/test_check_draft_visibility_cli.py`
- `aliexpress-csp-auto-upload/tests/test_draft_visibility.py`
- `aliexpress-csp-auto-upload/tests/test_capture_existing_draft_save_chain_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`

**验证**：
- `python -m pytest tests/test_capture_existing_draft_save_chain_cli.py tests/test_record_valid_object_chain_cli.py tests/test_draft_visibility.py tests/test_check_draft_visibility_cli.py tests/test_publish_chain.py tests/test_publish_chain_runner.py tests/test_run_publish_chain_cli.py -q` → 38 passed
- `python scripts\check_draft_visibility.py --account tenant5 --channel-id 870088 --product-id 1005012770666191 ...` → `visible/tab=draft`
- `python scripts\record_valid_object_chain.py --account tenant5 --channel-id 870088 --product products\template_100005539.json --headless --wait-ms 7000 --fill-timeout-ms 180000 --skip-marketing-images --save-draft --verify-draft-visibility` → `ok=true`，`switchId=1005012770682998`，`save_draft.status=draft_visible`
- `python scripts\capture_existing_draft_save_chain.py --account tenant5 --channel-id 870088 --product-id 1005012770682998 --headless --wait-ms 7000` → `existing_draft_saved_visible`

## 2026-07-20 09:41 — CSP 有效对象保存可见性校验

**任务**：基于当前 `tenant5/channelId=870088` 继续录“速卖通生成并 render 成功的有效商品对象”链路，并验证模板灌入后保存草稿是否真正落库。

**改动**：
- 增强 `record_valid_object_chain.py`：新增 `--save-draft --verify-draft-visibility` 严格模式，保存后必须通过商品管理列表 ID/标题搜索可见才算 `draft_visible`。
- 增强 `check_draft_visibility.py` 与 `draft_visibility` helper：支持 `draftId`、标题匹配，输出 `sample_rows`，并默认触发列表页查询按钮后再解析 `render.list`。
- 实测两轮当前账号/channel：模板灌入均 `missing_critical=[]`，速卖通生成的 `switchId` 可回渲染；但保存后列表搜索仍 `not_found`，最终状态记为 `draft_visibility_not_found`，不能把 `draftId` URL 或 HTTP 200 视为草稿成功。
- 更新 CSP 模板回放需求文档，明确区分 `valid_object_rendered` 与 `draft_visible` 两个验收层级。

**涉及文件**：
- `aliexpress-csp-auto-upload/scripts/record_valid_object_chain.py`
- `aliexpress-csp-auto-upload/scripts/check_draft_visibility.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_visibility.py`
- `aliexpress-csp-auto-upload/tests/test_record_valid_object_chain_cli.py`
- `aliexpress-csp-auto-upload/tests/test_check_draft_visibility_cli.py`
- `aliexpress-csp-auto-upload/tests/test_draft_visibility.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`

**验证**：
- `python -m pytest tests/test_record_valid_object_chain_cli.py tests/test_draft_visibility.py tests/test_check_draft_visibility_cli.py -q` → 17 passed
- `python scripts\record_valid_object_chain.py --account tenant5 --channel-id 870088 --product products\template_100005539.json --headless --wait-ms 8000 --fill-timeout-ms 180000 --skip-marketing-images --save-draft --verify-draft-visibility` → `draft_visibility_not_found`，`switchId=1005012770666191`

## 2026-07-20 09:10 — CSP 当前 channel 有效对象录制

**任务**：基于当前账号/channel 录制“速卖通生成并 render 成功的有效商品对象”链路，再做模板灌入。

**改动**：
- 新增 `scripts/record_valid_object_chain.py`：打开当前 channel 新建页，监听 `publish.render/switch.submit`，用页面链路获取速卖通生成的 `switchId`，灌入产品模板后重新打开编辑页做只读 render 复核。
- 新增脚本测试，覆盖 create URL、`switchId` 提取、`PUB_TRANSFER_ERROR` 识别、`data.data` 结构的 render 成功识别。
- 实测 `tenant5 + channelId=870088`：生成 `switchId=1005012763723712`，模板灌入 `missing_critical=[]`，编辑页 render 返回 `SUCCESS::调用成功`；未点击保存/提交，因此列表可见性检查为 `not_found`。
- 更新 CSP 模板回放需求文档，记录本轮录制路径、命令和边界：本脚本不发布商品，只证明当前账号/channel 下 ID 有效且 render 可成功。

**涉及文件**：
- `aliexpress-csp-auto-upload/scripts/record_valid_object_chain.py`
- `aliexpress-csp-auto-upload/tests/test_record_valid_object_chain_cli.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`

**验证**：
- `python -m pytest tests/test_record_valid_object_chain_cli.py tests/test_publish_chain.py tests/test_publish_chain_runner.py tests/test_run_publish_chain_cli.py tests/test_draft_visibility.py tests/test_check_draft_visibility_cli.py -q` → 27 passed
- `python scripts/record_valid_object_chain.py --account tenant5 --channel-id 870088 --product products\template_100005539.json --headless --wait-ms 12000 --fill-timeout-ms 180000 --skip-marketing-images` → `valid_object_rendered`
- `python scripts/check_draft_visibility.py --account tenant5 --channel-id 870088 --product-id 1005012763723712 ...` → `not_found`（未保存/未提交的预期边界）
- `python -m pytest -q` → 92 passed 后仍遇到既有失败 `tests/test_account_config.py::test_yaml_overrides_defaults`，未在本轮修复。

## 2026-07-17 17:53 — CSP publish ID 生命周期与 render 前置校验

**任务**：按“先拿速卖通承认的商品 ID，再进入发布后状态流转”的判断，修正 CSP 全托管接口回放的成功判定。

**改动**：
- 收紧 `publish.submit` 成功判定：发布阶段不再把 `switchId` 回退当作 `productId` 使用；若响应没有明确 `productId/itemId/id/data`，直接输出 `publish_missing_product_id`，不再进入列表可见性查询。
- 在 `publish.submit` 后、商品列表查询前增加只读 `publish.render` 校验；若编辑页 render 返回 `PUB_TRANSFER_ERROR` 等后端转换错误，输出 `publish_transfer_failed`，判定速卖通未承认该商品对象。
- 扩展 payload 净化：递归移除 `jsonBody` 内残留的 `productId/itemId/draftId/switchId`，并在 `request_summary.payload_sanitizer` 记录清理字段。
- 补充测试覆盖：缺失商品 ID 不查列表、render 失败不查列表、发布响应只带 `switchId` 不视为商品 ID、嵌套业务 ID 会被净化。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain_runner.py`

**验证**：
- `python -m pytest tests/test_publish_chain.py tests/test_publish_chain_runner.py tests/test_run_publish_chain_cli.py tests/test_draft_visibility.py tests/test_cleanup_strategy.py tests/test_check_draft_visibility_cli.py -q` → 26 passed
- `python -m pytest -q` → 92 passed 后遇到既有失败 `tests/test_account_config.py::test_yaml_overrides_defaults`，该失败读取到默认登录 URL，疑似来自本轮外已有 `conf.py` 工作树改动；未在本轮修复。

## 2026-07-17 17:36 — CSP publish chain 受控 live runner

**任务**：按“run_publish_chain.py + 显式确认 + 发布后可见性校验”落地下一轮 CSP 全托管接口回放能力。

**改动**：
- 新增 `run_live_publish_chain_payload_file`，按 `switch.submit -> publish.submit` 顺序执行，先取本轮新鲜 `switchId`，再材料化 `publish.submit.itemId`。
- live 执行必须显式 `confirm_publish=True`；CLI 层必须同时传 `--live --confirm-publish`，否则写出 `confirm_publish_required`，不打开浏览器、不发真实请求。
- 发布成功后复用商品管理列表页监听，检查 `pending_approval,online_product,violation_qc_failed,offline_product,draft`；接口成功但列表不可见时输出 `publish_passed_visibility_not_found`，不算完整成功。
- 新增 `scripts/run_publish_chain.py`，默认 dry-run 只做 payload 预检，真实发布需显式确认。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/scripts/run_publish_chain.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain_runner.py`
- `aliexpress-csp-auto-upload/tests/test_run_publish_chain_cli.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `docs/superpowers/specs/aliexpress-csp-mvp/技术分析文档.md`

## 2026-07-17 17:21 — CSP 发布成功链路 payload 生成器

**任务**：根据已找到的真实发布成功录制，落地 `switch.submit -> publish.submit` 的两步接口回放 payload 抽取与净化能力。

**改动**：
- 新增 `publish_chain` 模块，从成功录制的 `mtop_hits.json` 抽取 `mtop.item.product.self.publish.switch.submit` 与 `mtop.item.product.pop.publish.submit`，识别 `switchId/productId`，并把 `ret=SUCCESS` 但含业务错误字段的响应判为失败。
- 新增两步 payload 生成能力：`publish.submit.itemId` 使用 `{{switchId}}` 占位，真实执行前必须替换为本轮新鲜 `switchId`；同时清理旧商品 `id/itemId/productId/scItemInfo/skuTicketAudit` 等 SKU 身份、绑定和审核字段。
- 新增只生成不发布的命令入口 `scripts/build_publish_chain_payload.py`，并补充文档说明真实成功链路不是旧的空 `itemId draft.submit` 假设。
- 本轮只做 payload 生成与净化，不触发真实发品。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/publish_chain.py`
- `aliexpress-csp-auto-upload/scripts/build_publish_chain_payload.py`
- `aliexpress-csp-auto-upload/tests/test_publish_chain.py`
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `docs/superpowers/specs/aliexpress-csp-mvp/技术分析文档.md`

## 2026-07-17 17:10 — CSP live 草稿回放复跑与假成功识别

**任务**：按“开始”执行下一轮，刷新/复跑 `tenant5` 的 sanitized `draft.submit`，并完成草稿可见性与清理闭环校验。

**改动**：
- 修复 live runner 的 mtop token 过期处理：当首次返回 `FAIL_SYS_TOKEN_EXOIRED` 时，在同一浏览器上下文中重试一次，并在触发刷新或成功后回写 storage_state。
- 修复 `draft.submit` 响应分类：`ret=SUCCESS` 但 `data.errorCode/globalError/error/models.globalMessage` 存在时，判为 `draft_failed`，避免把平台业务失败误判为成功。
- 实测 `tenant5`：`ae_cli.py check --account tenant5` 可进入后台，说明不是整站登录失效；首次 live 成功走通 token retry。
- 实测 sanitized live 两次返回 API 成功 ID：`1005012758684812`、`1005012758936064`；但二者在 5 个列表页均 `not_found`，且编辑页 `publish.render` 均返回 `THD_IC_ERR_F_IC_SERVICE_QUERY_002::Product not found`。
- 结论：当前录制/固化的 `mtop.item.product.pop.publish.draft.submit` 来自编辑态样本，不能证明可用于“新建可见草稿”；下一步必须重新录制“新建商品保存草稿”的真实页面链路，不能继续把接口 `SUCCESS` 当成草稿创建成功。

**验证**：
- `python -m pytest tests/test_payload_sanitizer.py tests/test_draft_visibility.py tests/test_check_draft_visibility_cli.py tests/test_cleanup_strategy.py tests/test_payload_generator.py tests/test_draft_runner.py tests/test_auth_helpers.py tests/test_cookie_import.py tests/test_listed_product_samples.py tests/test_live_response_capture.py tests/test_capture_render_samples_cli.py tests/test_render_sample_merge.py tests/test_template_profile_batch.py -q` → 46 passed
- `python scripts/run_draft_payload.py ... --live --account tenant5 --channel-id 870088 --headless ...` → API `draft_passed`, `product_id=1005012758936064`
- `python scripts/check_draft_visibility.py --account tenant5 --channel-id 870088 --product-id 1005012758936064 ...` → `not_found`
- 编辑页 `product_publish?switchId=1005012758936064&from=SELF&channelId=870088` 监听 `mtop.item.product.self.publish.render` → `Product not found`

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_runner.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner.py`
- `docs/superpowers/specs/aliexpress-csp-mvp/技术分析文档.md`
- `开发文档.md`

## 2026-07-17 16:54 — CSP payload 净化器、草稿可见性校验与清理策略

**任务**：按下一轮要求直接实现 “payload 净化器 + 草稿可见性校验 + 清理策略”，只针对 CSP 全托管页面接口回放链路。

**改动**：
- 新增 `payload_sanitizer`，在生成 `draft.submit` payload 时重建 `sku/logisticSku`，移除已上架商品残留的 `skuId/scItemId/scInvId/productBarcode` 等身份、绑定、审核与锁定字段，并写入 `sanitized_fields` 审计信息。
- live `draft.submit` 增加 `--channel-id`，确保 mtop 请求携带当前店铺发品链路的 `__channel-id__` 上下文。
- 新增只读草稿可见性校验脚本，按 `draft/pending_approval/violation_qc_failed/offline_product/online_product` 标签页监听 `mtop.global.merchant.self.product.manager.render.list` 并反查 productId。
- 新增非破坏性清理策略脚本：只生成手工删除/撤回/复核建议，不自动删除、撤回或下架任何商品。
- 实测旧草稿 `product_id=1005012758404676` 在 5 个列表页均未找到；本轮净化后的 live 试跑因当前 cookie mtop token 过期返回 `FAIL_SYS_TOKEN_EXOIRED`，未创建新草稿，需刷新登录态后复跑可见性。

**验证**：
- `python -m pytest tests/test_payload_sanitizer.py tests/test_draft_visibility.py tests/test_check_draft_visibility_cli.py tests/test_cleanup_strategy.py tests/test_payload_generator.py tests/test_draft_runner.py tests/test_auth_helpers.py tests/test_cookie_import.py tests/test_listed_product_samples.py tests/test_live_response_capture.py tests/test_capture_render_samples_cli.py tests/test_render_sample_merge.py tests/test_template_profile_batch.py -q` → 44 passed
- `python scripts/check_draft_visibility.py --account tenant5 --channel-id 870088 --product-id 1005012758404676 --tabs draft,pending_approval,violation_qc_failed,offline_product,online_product --headless --wait-ms 8000 ...` → `not_found`
- `python scripts/run_draft_payload.py ...draft_payload_100005539_sanitized.json --live --account tenant5 --channel-id 870088 --headless ...` → `FAIL_SYS_TOKEN_EXOIRED`

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_sanitizer.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_visibility.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/cleanup_strategy.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_generator.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_runner.py`
- `aliexpress-csp-auto-upload/scripts/check_draft_visibility.py`
- `aliexpress-csp-auto-upload/scripts/plan_cleanup_strategy.py`
- `aliexpress-csp-auto-upload/scripts/run_draft_payload.py`
- `aliexpress-csp-auto-upload/tests/test_payload_sanitizer.py`
- `aliexpress-csp-auto-upload/tests/test_draft_visibility.py`
- `aliexpress-csp-auto-upload/tests/test_check_draft_visibility_cli.py`
- `aliexpress-csp-auto-upload/tests/test_cleanup_strategy.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner.py`
- `docs/superpowers/specs/aliexpress-csp-mvp/技术分析文档.md`

## 2026-07-17 16:42 — CSP 已上架商品训练样本真实链路打通

**任务**：继续按 spec 将已上架商品转为训练样本，并验证 CSP 全托管页面接口回放的草稿试跑链路。

**改动**：
- 新增外部 cookie 导入工具，将 CrossHub/速卖通 cookie JSON 规范化为本项目 Playwright storage_state；本次仅本地导入 `tenant5`，不提交 cookie。
- 修正真实 GSP 商品列表解析：支持 `itemDesc`、`productCategory`、`subDataSource`，可从 `mtop.global.merchant.self.product.manager.render.list` 提取已上架商品行。
- 新增只读 `publish.render` 抓取脚本，按已上架 `productId/switchId` 打开编辑页，归档 render 表单体并构建训练样本池。
- 新增 render/list hybrid 样本合并：真实 render 的 `value.sku` 为空时，用列表行 SKU 证据补齐，并在 `_sample_source.sku_source=listed_product_row` 标记来源。
- 修正 CSP 后台鉴权误判：后台页面包含“登录/验证”类文案时，优先识别“商品管理/商品列表/草稿箱/全托管”等工作台成功信号。
- 增强 `draft.submit` 结果 ID 提取，支持 `data.data` 和 `data.models` 中的 productId/draftId。
- 真实验证结果：`tenant5` 抓取 10 条已上架商品，生成 10 条 `sample_normalized`；`100005539|multi_sku` 与 `200075142|multi_sku` 生成 `profile_ready`；`100005539` 样例 dry-run 通过，live `draft.submit` 通过并返回 `product_id=1005012758404676`（未调用发布接口）。

**验证**：
- `python -m pytest tests/test_auth_helpers.py tests/test_cookie_import.py tests/test_listed_product_samples.py tests/test_live_response_capture.py tests/test_capture_render_samples_cli.py tests/test_render_sample_merge.py tests/test_draft_runner.py tests/test_template_profile_batch.py tests/test_payload_generator.py -q` → 32 passed
- `python scripts/capture_listed_samples.py --account tenant5 --out captures\tenant5_gsp_listed_20260717_162636 --headless --wait-ms 20000 --url https://gsp.aliexpress.com/m_apps/choiceProductManage/list-manage?channelId=870088` → `product_count=10`
- `python scripts/capture_render_samples.py --account tenant5 --channel-id 870088 --out captures\tenant5_render_hybrid_20260717_163317 --headless --wait-ms 20000 --raw-sample-dir captures\tenant5_gsp_listed_20260717_162636\raw_samples ...` → `sample_normalized=10`
- `python scripts/build_ready_template_profiles.py captures\tenant5_render_hybrid_20260717_163317\sample_pool --out captures\tenant5_render_hybrid_20260717_163317\template_profiles` → `profile_count=2`
- `python scripts/run_draft_payload.py captures\tenant5_render_hybrid_20260717_163317\draft_payload_100005539.json --live --account tenant5 --headless --out captures\tenant5_render_hybrid_20260717_163317\draft_run_100005539_live_2.json` → `draft_passed`, `product_id=1005012758404676`

**涉及文件**：
- `aliexpress-csp-auto-upload/scripts/import_external_cookie.py`
- `aliexpress-csp-auto-upload/scripts/capture_render_samples.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/cookie_import.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/listed_product_samples.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/live_response_capture.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/render_sample_merge.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/auth.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_runner.py`
- `aliexpress-csp-auto-upload/tests/test_cookie_import.py`
- `aliexpress-csp-auto-upload/tests/test_listed_product_samples.py`
- `aliexpress-csp-auto-upload/tests/test_live_response_capture.py`
- `aliexpress-csp-auto-upload/tests/test_capture_render_samples_cli.py`
- `aliexpress-csp-auto-upload/tests/test_render_sample_merge.py`
- `aliexpress-csp-auto-upload/tests/test_auth_helpers.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner.py`
- `docs/superpowers/specs/aliexpress-csp-mvp/技术分析文档.md`

## 2026-07-17 16:22 — CSP 批量 payload 增加场景级 profile 匹配

**任务**：继续修正“同类目不等于同模板”的逻辑漏洞，避免批量 payload 生成只按 `cat_id` 选 profile。

**改动**：
- `payload_batch` 增加 profile 选择器：单 profile 仍直接使用；多 profile 时优先匹配产品显式 `scenario_key`。
- 产品未提供 `scenario_key` 时，按 `sku_list` 数量推断 `single_sku` / `multi_sku`，用 `cat_id + sku_shape` 缩小匹配范围。
- 若推断后仍命中多个 profile，返回 `ambiguous_profile:scenario_key`；若同类目但没有相应 SKU 形态，保留 `ambiguous_profile:cat_id`。
- payload 批量报告里的成功项补充 `scenario_key`，便于追溯每个商品套用了哪个模板画像。

**验证**：
- `python -m pytest tests/test_payload_batch.py tests/test_payload_batch_cli.py -q` → 6 passed
- `python -m pytest tests/test_batch_replay_pipeline_cli.py tests/test_batch_replay_pipeline.py tests/test_draft_run_batch_cli.py tests/test_draft_run_batch.py tests/test_payload_batch_cli.py tests/test_payload_batch.py tests/test_template_profile_batch_cli.py tests/test_template_profile_batch.py tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 62 passed
- `git diff --check -- uploader/csp_uploader/payload_batch.py tests/test_payload_batch.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_batch.py`
- `aliexpress-csp-auto-upload/tests/test_payload_batch.py`
- `开发文档.md`

## 2026-07-17 16:13 — 串联 CSP 离线批量模板回放流水线

**任务**：继续推进已上架商品训练样本链路，把样本池、ready profile、批量 payload、批量 draft dry-run 串成一个离线总入口。

**改动**：
- 新增 `batch_replay_pipeline` 总编排模块，复用既有 `build_listed_sample_pool`、`build_ready_template_profiles`、`build_draft_payload_batch`、`run_draft_payload_batch` 四段能力。
- 新增 CLI `run_batch_replay_pipeline.py`，输入 raw samples/capture 目录与标准商品 JSON 数组，输出完整离线批量产物。
- 总入口会生成 `sample_pool/`、`template_profiles/`、`draft_payloads/`、`draft_runs/`，并在根输出 `batch_replay_report.json` 汇总 artifacts、counts、errors。
- `--live` 仍返回 `live_batch_not_supported`，当前只允许离线 dry-run，避免批量误触发真实 `draft.submit`。

**验证**：
- `python -m pytest tests/test_batch_replay_pipeline.py tests/test_batch_replay_pipeline_cli.py -q` → 4 passed
- `python -m pytest tests/test_batch_replay_pipeline_cli.py tests/test_batch_replay_pipeline.py tests/test_draft_run_batch_cli.py tests/test_draft_run_batch.py tests/test_payload_batch_cli.py tests/test_payload_batch.py tests/test_template_profile_batch_cli.py tests/test_template_profile_batch.py tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 60 passed
- `git diff --check -- uploader/csp_uploader/batch_replay_pipeline.py scripts/run_batch_replay_pipeline.py tests/test_batch_replay_pipeline.py tests/test_batch_replay_pipeline_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/batch_replay_pipeline.py`
- `aliexpress-csp-auto-upload/scripts/run_batch_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_batch_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_batch_replay_pipeline_cli.py`
- `开发文档.md`

## 2026-07-17 16:06 — 增加 CSP 批量 draft dry-run 入口

**任务**：继续推进 CSP 模板回放，将已批量生成的 `draft.submit` payload 目录转换为可审计的批量草稿试跑结果。

**改动**：
- 新增 `draft_run_batch` 模块，批量读取 `payloads/*.json`，逐条复用现有 `run_draft_payload` 做 dry-run preflight 校验。
- 新增 CLI `run_draft_payload_batch.py`，强制显式传入 `--dry-run` 或 `--live`；当前只开放批量 dry-run，批量 live 返回 `live_batch_not_supported`，避免误触发真实接口。
- 批量试跑会输出 `runs/*.json` 和 `draft_run_batch_report.json`，汇总 `payload_count`、`passed_count`、`failed_count`、每条 payload/run 路径与错误。
- 新增测试覆盖批量 dry-run 成功/失败混合、空 payload 目录、CLI 写报告、CLI 运行模式闸门。

**验证**：
- `python -m pytest tests/test_draft_run_batch.py tests/test_draft_run_batch_cli.py -q` → 4 passed
- `python -m pytest tests/test_draft_run_batch_cli.py tests/test_draft_run_batch.py tests/test_payload_batch_cli.py tests/test_payload_batch.py tests/test_template_profile_batch_cli.py tests/test_template_profile_batch.py tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 56 passed
- `git diff --check -- uploader/csp_uploader/draft_run_batch.py scripts/run_draft_payload_batch.py tests/test_draft_run_batch.py tests/test_draft_run_batch_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_run_batch.py`
- `aliexpress-csp-auto-upload/scripts/run_draft_payload_batch.py`
- `aliexpress-csp-auto-upload/tests/test_draft_run_batch.py`
- `aliexpress-csp-auto-upload/tests/test_draft_run_batch_cli.py`
- `开发文档.md`
## 2026-07-17 16:00 — 增加 CSP 批量 draft payload 生成入口

**任务**：继续推进 CSP 模板回放，把新商品标准 JSON 自动匹配 ready profile 并批量生成 `draft.submit` payload。

**改动**：
- 新增 `payload_batch` 模块，扫描 profiles 目录下的 `template_profile.json`，只使用 `profile_ready` 模板。
- 新增 CLI `build_draft_payload_batch.py`，支持输入单个产品 JSON 或产品数组，输出 `payloads/*.json` 与 `draft_payload_batch_report.json`。
- 第一版按 `cat_id` 唯一匹配 profile；找不到模板返回 `profile_missing:cat_id`，同类目多个 ready profile 返回 `ambiguous_profile:cat_id`，避免猜错场景。
- 生成结果复用现有 `generate_draft_payload`，继续保留危险字段清理、类目匹配和商品必填字段校验。

**验证**：
- `python -m pytest tests/test_payload_batch_cli.py tests/test_payload_batch.py tests/test_template_profile_batch_cli.py tests/test_template_profile_batch.py tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 52 passed
- `git diff --check -- uploader/csp_uploader/payload_batch.py scripts/build_draft_payload_batch.py tests/test_payload_batch.py tests/test_payload_batch_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_batch.py`
- `aliexpress-csp-auto-upload/scripts/build_draft_payload_batch.py`
- `aliexpress-csp-auto-upload/tests/test_payload_batch.py`
- `aliexpress-csp-auto-upload/tests/test_payload_batch_cli.py`
- `开发文档.md`

## 2026-07-17 15:57 — 增加 CSP ready 场景批量模板画像

**任务**：继续推进 CSP 模板回放，把样本池中已满足覆盖门闸的场景批量生成 `template_profile.json`。

**改动**：
- 新增 `template_profile_batch` 模块，读取 `sample_pool/manifest.json` 与 `coverage.json`。
- 只为 `coverage_status=ready_for_diff` 的场景生成模板画像；`low_confidence`、`sample_raw`、`sample_unusable` 仅记录到跳过列表，不生成可用 profile。
- 新增 CLI `build_ready_template_profiles.py`，按 `scenario_key` 建子目录并输出每个场景的 `template_profile.json`。
- 输出 `template_profile_batch_report.json`，记录生成画像数量、路径、跳过场景与原因，方便后续 payload 生成器挑选可用模板。

**验证**：
- `python -m pytest tests/test_template_profile_batch_cli.py tests/test_template_profile_batch.py tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 48 passed
- `git diff --check -- uploader/csp_uploader/template_profile_batch.py scripts/build_ready_template_profiles.py tests/test_template_profile_batch.py tests/test_template_profile_batch_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/template_profile_batch.py`
- `aliexpress-csp-auto-upload/scripts/build_ready_template_profiles.py`
- `aliexpress-csp-auto-upload/tests/test_template_profile_batch.py`
- `aliexpress-csp-auto-upload/tests/test_template_profile_batch_cli.py`
- `开发文档.md`

## 2026-07-17 15:55 — 增加 CSP 已上架样本池覆盖报告

**任务**：继续推进 CSP 模板回放，把 live 采集目录或 raw sample 目录一键转成训练样本池与覆盖状态报告。

**改动**：
- 新增 `listed_sample_pipeline` 编排模块，自动识别 `capture_dir/raw_samples` 或直接输入的 raw sample 目录。
- 新增 CLI `build_listed_sample_pool.py`，输出 `manifest.json`、`coverage.json`、`listed_sample_report.json`。
- 报告汇总 `status_counts`、`coverage_status_counts` 和 `ready_scenarios`，用于判断哪些类目/场景已满足至少 2 个 `sample_normalized` 样本，可进入模板差异分析。
- 缺少 raw samples 时返回 `raw_samples_missing`，避免生成空样本池后误判覆盖状态。

**验证**：
- `python -m pytest tests/test_listed_sample_pipeline_cli.py tests/test_listed_sample_pipeline.py tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 44 passed
- `git diff --check -- uploader/csp_uploader/listed_sample_pipeline.py scripts/build_listed_sample_pool.py tests/test_listed_sample_pipeline.py tests/test_listed_sample_pipeline_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/listed_sample_pipeline.py`
- `aliexpress-csp-auto-upload/scripts/build_listed_sample_pool.py`
- `aliexpress-csp-auto-upload/tests/test_listed_sample_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_listed_sample_pipeline_cli.py`
- `开发文档.md`

## 2026-07-17 15:53 — 增加 CSP 已上架商品 live 响应采集入口

**任务**：继续推进 CSP 模板回放，把真实浏览器里抓到的已上架商品响应直接落到 raw sample 输入目录。

**改动**：
- 新增 `live_response_capture` 归档模块，接收浏览器 response 的 URL、状态、headers 和文本，只在能识别商品行时保存原始响应和 raw samples。
- 新增 CLI `capture_listed_samples.py`，复用本地账号 cookie 打开 CSP 发品/商品管理候选页，监听商品相关响应并输出 `responses/`、`raw_samples/`、`capture_summary.json`。
- 默认候选商品管理 URL 会继承账号 `publish_url` 里的 `channelId/from/switchId`，避免缺上下文导致页面或接口不可用。
- 新增测试覆盖响应归档、非商品响应跳过、默认 URL 上下文保留；尚未用真实速卖通账号执行 live 抓取。

**验证**：
- `python -m pytest tests/test_capture_listed_samples_cli.py tests/test_live_response_capture.py tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 40 passed
- `git diff --check -- uploader/csp_uploader/live_response_capture.py scripts/capture_listed_samples.py tests/test_live_response_capture.py tests/test_capture_listed_samples_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/live_response_capture.py`
- `aliexpress-csp-auto-upload/scripts/capture_listed_samples.py`
- `aliexpress-csp-auto-upload/tests/test_live_response_capture.py`
- `aliexpress-csp-auto-upload/tests/test_capture_listed_samples_cli.py`
- `开发文档.md`

## 2026-07-17 15:49 — 增加 CSP 已上架商品 raw 样本抽取入口

**任务**：继续推进 CSP 模板回放，把已上架商品接口响应整理成可进入训练样本池的 raw sample 文件。

**改动**：
- 新增 `listed_product_samples` 数据抽取模块，可从嵌套 CSP/mtop JSON 响应中识别商品行并按商品 ID 输出一商品一份 raw JSON。
- 新增 CLI `extract_listed_samples.py`，支持从抓到的列表/详情响应导出 raw samples 与 `index.json`，作为 `build_training_sample_pool.py` 的前置输入。
- 训练样本归一化器增加 `skuList/skuInfoList/skuInfos/skuDataList` 等 SKU 别名识别，让详情/列表接口结构能被判定为 `sample_raw` 覆盖，而不是误判不可用。
- 样本池读取跳过 raw 导出目录里的 `index.json`，避免把导出索引当成一个坏样本。

**验证**：
- `python -m pytest tests/test_listed_product_samples_cli.py tests/test_listed_product_samples.py tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 37 passed
- `git diff --check -- uploader/csp_uploader/listed_product_samples.py uploader/csp_uploader/training_samples.py scripts/extract_listed_samples.py tests/test_listed_product_samples.py tests/test_listed_product_samples_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/listed_product_samples.py`
- `aliexpress-csp-auto-upload/uploader/csp_uploader/training_samples.py`
- `aliexpress-csp-auto-upload/scripts/extract_listed_samples.py`
- `aliexpress-csp-auto-upload/tests/test_listed_product_samples.py`
- `aliexpress-csp-auto-upload/tests/test_listed_product_samples_cli.py`
- `开发文档.md`

## 2026-07-17 15:46 — 增加 CSP 模板回放 live 流水线入口

**任务**：继续推进 CSP 模板回放，将已串联的 dry-run 流水线扩展为显式 live 草稿试跑入口。

**改动**：
- `replay_pipeline` 新增 `run_live_replay_pipeline`，复用样本池、模板画像和 payload 生成阶段，再调用已封装的 `run_live_draft_payload_file`。
- CLI `run_template_replay_pipeline.py` 增加 `--live --account <店铺代号>`，并保留 `--dry-run` / `--live` 二选一、live 必填账号的安全闸门。
- live 编排会输出同一套 `sample_pool/`、`template_profile.json`、`draft_payload.json`、`draft_run.json` 和 `replay_report.json` 工件，便于失败定位和审计。
- 新增 live 编排单元测试与 CLI 参数闸门测试；当前仍使用 monkeypatch 假 live runner，尚未对真实速卖通账号执行 `draft.submit`。

**验证**：
- `python -m pytest tests/test_replay_pipeline_cli.py tests/test_replay_pipeline.py tests/test_draft_runner_cli.py tests/test_draft_runner.py tests/test_payload_generator_cli.py tests/test_payload_generator.py tests/test_template_profile_cli.py tests/test_template_profile.py tests/test_training_sample_pool_cli.py tests/test_training_sample_pool.py tests/test_training_samples.py tests/test_training_sample_cli.py -q` → 32 passed
- `git diff --check -- uploader/csp_uploader/replay_pipeline.py scripts/run_template_replay_pipeline.py tests/test_replay_pipeline.py tests/test_replay_pipeline_cli.py` → 通过

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/replay_pipeline.py`
- `aliexpress-csp-auto-upload/scripts/run_template_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_replay_pipeline_cli.py`
- `开发文档.md`

## 2026-07-17 15:42 — 串联 CSP 模板回放 dry-run 流水线

**任务**：继续推进 CSP 模板回放，将样本池、模板画像、payload 生成和 dry-run 校验串成一条命令。

**改动**：
- 新增 `replay_pipeline` 编排模块，按顺序生成 `sample_pool/manifest.json`、`template_profile.json`、`draft_payload.json`、`draft_run.json`。
- 输出总报告 `replay_report.json`，记录各阶段工件路径、最终状态和错误列表。
- 新增 CLI `run_template_replay_pipeline.py`，当前只开放 `--dry-run`，用于真实 live 前的完整本地预检。
- 新增端到端测试，覆盖原始样本目录 + 标准商品 JSON 一次性产出 dry-run 全链路工件。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/replay_pipeline.py`
- `aliexpress-csp-auto-upload/scripts/run_template_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_replay_pipeline.py`
- `aliexpress-csp-auto-upload/tests/test_replay_pipeline_cli.py`
- `开发文档.md`

## 2026-07-17 15:39 — 接入 CSP 草稿试跑 live 安全入口

**任务**：继续推进 CSP 模板回放 M3，把 `draft.submit` dry-run runner 接到现有登录态 mtop 请求层。

**改动**：
- `draft_runner` 新增 `run_live_draft_payload_file`，复用 `cookie_auth`、`open_request_context`、`fetch_mtop_step` 调用 `mtop.item.product.pop.publish.draft.submit`。
- CLI `run_draft_payload.py` 增加显式 `--live --account <店铺代号>` 安全闸门；未指定 `--dry-run` 或 `--live` 时拒绝执行。
- live 分支仍复用本地 preflight 校验，只有 payload 合格才会创建请求上下文。
- 测试通过 monkeypatch 注入假登录态和假 mtop 响应，覆盖成功 draft id 解析；尚未对真实速卖通账号执行 live 调用。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_runner.py`
- `aliexpress-csp-auto-upload/scripts/run_draft_payload.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner_cli.py`
- `开发文档.md`

## 2026-07-17 15:36 — 增加 CSP 草稿试跑 dry-run 校验

**任务**：继续推进 CSP 模板回放 M3，为生成的 `draft.submit` payload 增加试跑前校验与响应解析。

**改动**：
- 新增 draft runner 模块，可读取 `draft_payload.json` 并执行本地 dry-run 校验。
- 校验内容包括外层 `catId`、空 `itemId/draftId`、`jsonBody.categoryPath.id`、`jsonBody.sku` 等关键条件。
- 增加 `draft.submit` 响应分类器，可识别 `SUCCESS::`、提取 `draftId/productId`，并把平台错误消息整理为字段级失败原因。
- 新增 CLI `run_draft_payload.py`，当前只开放 `--dry-run`，避免未接登录态时误触实站。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/draft_runner.py`
- `aliexpress-csp-auto-upload/scripts/run_draft_payload.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner.py`
- `aliexpress-csp-auto-upload/tests/test_draft_runner_cli.py`
- `开发文档.md`

## 2026-07-17 15:33 — 生成 CSP draft.submit 请求体

**任务**：继续推进 CSP 模板回放 M2，根据 `template_profile.json` 与新商品标准 JSON 生成草稿接口 payload。

**改动**：
- 新增 draft payload 生成模块，从 `profile_ready` 模板画像还原 `jsonBody` 基础结构。
- 支持覆盖新商品字段：`cat_id`、`title_en`、`material`、`sku_list[0].price/stock/sku_code`。
- 生成 `mtop.item.product.pop.publish.draft.submit` 外层请求体，强制 `itemId`、`draftId` 为空，并删除 `$.id` 等危险字段。
- 对低置信画像、类目不匹配、缺少商品必填字段输出可审计失败原因，不进入草稿试跑。
- 新增 CLI，可从模板画像和商品 JSON 输出 `draft_payload.json`。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/payload_generator.py`
- `aliexpress-csp-auto-upload/scripts/build_draft_payload.py`
- `aliexpress-csp-auto-upload/tests/test_payload_generator.py`
- `aliexpress-csp-auto-upload/tests/test_payload_generator_cli.py`
- `开发文档.md`

## 2026-07-17 15:30 — 生成 CSP 模板差异画像

**任务**：继续推进 CSP 模板回放 M1，从训练样本池生成 `template_profile.json`。

**改动**：
- 新增模板画像模块，读取样本池 `manifest.json` 中的 `sample_normalized` 样本。
- 对同一 `scenario_key` 下的归一化 `jsonBody` 做扁平路径差异分析，输出 `fixed_fields`、`variable_fields`、`dangerous_fields` 与 `missing_coverage`。
- 当同场景至少 2 个归一化样本时标记 `profile_ready`；只有 1 个样本时标记 `low_confidence` 并提示样本不足。
- 新增 CLI，可从样本池 manifest 直接生成 `template_profile.json`。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/template_profile.py`
- `aliexpress-csp-auto-upload/scripts/build_template_profile.py`
- `aliexpress-csp-auto-upload/tests/test_template_profile.py`
- `aliexpress-csp-auto-upload/tests/test_template_profile_cli.py`
- `开发文档.md`

## 2026-07-17 15:25 — 批量生成 CSP 已上架商品训练样本池

**任务**：继续根据 CSP 模板回放 spec，将多个已上架/录制商品 JSON 批量转成训练样本池。

**改动**：
- 在训练样本模块中新增批量样本池构建能力，支持读取目录内单对象 JSON 与列表 JSON。
- 输出 `samples/`、`manifest.json`、`coverage.json`，按 `scenario_key` 聚合 `sample_normalized`、`sample_raw`、`sample_unusable` 数量。
- 新增覆盖状态：至少 2 个归一化样本的场景标记为 `ready_for_diff`，用于衔接后续模板差异分析。
- 新增批量 CLI，可直接从原始商品 JSON 目录生成样本池摘要。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/training_samples.py`
- `aliexpress-csp-auto-upload/scripts/build_training_sample_pool.py`
- `aliexpress-csp-auto-upload/tests/test_training_sample_pool.py`
- `aliexpress-csp-auto-upload/tests/test_training_sample_pool_cli.py`
- `开发文档.md`

## 2026-07-17 15:18 — 将已上架商品 JSON 转为 CSP 训练样本

**任务**：根据 CSP 模板回放 spec，开始把已上架商品数据落成可训练/可诊断样本。

**改动**：
- 新增训练样本归一化模块，支持从外层 `jsonBody` 或直接草稿 JSON 中提取可回放样本。
- 将草稿形态样本标记为 `sample_normalized`，将只有 `categoryId` 等详情页形态但缺少 `categoryPath.id` 的数据标记为 `sample_raw`，避免误当作可回放模板。
- 新增命令行脚本，可把单个已上架商品/草稿 JSON 输出为训练样本 JSON。
- 新增单元测试与 CLI 测试，覆盖单 SKU、多 SKU、不可用样本、详情页原始样本分类。

**涉及文件**：
- `aliexpress-csp-auto-upload/uploader/csp_uploader/training_samples.py`
- `aliexpress-csp-auto-upload/scripts/build_training_sample.py`
- `aliexpress-csp-auto-upload/tests/test_training_samples.py`
- `aliexpress-csp-auto-upload/tests/test_training_sample_cli.py`
- `开发文档.md`


## 2026-07-17 15:11 — 审查并修正速卖通 CSP 模板回放文档逻辑

**任务**：复核速卖通 CSP 模板回放相关文档是否存在逻辑漏洞，并直接修正不通之处。

**改动**：
- 将旧 MVP 文档中的 `publish` 口径标记为早期最小发品链验证，避免与当前“只做 `draft.submit` 草稿回放”的阶段目标混用。
- 补充样本采集与归一化门闸，明确已上架商品详情/编辑结构不能直接当作草稿 payload。
- 拆分技术可行性验收与单场景可用验收，避免“1 个类目草稿成功”被误读为“所有类目可批量使用”。
- 修正录制 EXE 索引中过期的“自带 Chromium”描述，改为 Patchright driver 驱动本机 Chrome。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/README.md`
- `aliexpress-csp-auto-upload/docs/requirements.md`
- `aliexpress-csp-auto-upload/docs/技术分析文档.md`
- `开发文档.md`

## 2026-07-17 15:00 — 落地速卖通 CSP 模板回放需求文档

**任务**：将“模板差异分析 + payload 生成器 + 草稿试跑校验”的需求分析沉淀为项目文档。

**改动**：
- 新增速卖通 CSP 模板扩展与接口回放需求文档，明确第一版只做 CSP 全托管接口回放、只生成草稿、不自动发布。
- 在录制/回放项目文档索引中补充新文档入口。

**涉及文件**：
- `aliexpress-csp-auto-upload/docs/csp-template-replay-requirements.md`
- `aliexpress-csp-auto-upload/docs/README.md`
- `开发文档.md`

> 工作区：`D:\dev\workspace`
> 最新开发流水写在本文档顶部；长期架构、端口、启动约定见 [`交接文档.md`](./交接文档.md)。
> 历史归档位于 `docs/handover/`。

## 2026-07-17 14:24 — 修复录制器速卖通登录非法请求

**任务**：审查录制包打开速卖通登录页后点击登录提示「非法请求」的问题，判断是否由浏览器携带参数触发风控。

**改动**：
- 定位到录制器手工打开 `login.aliexpress.com/user/seller/login?...return_url=http://csp...`，不是浏览器启动参数或服务器 `access_token` 被带到速卖通。
- 将录制器登录入口改为直接打开 `https://csp.aliexpress.com/` 或带本店 `channelId` 的 CSP 发品目标页，由速卖通自己生成登录跳转，避免手写 `return_url` 被判非法。
- 扩展旧配置检测：本地账号 yaml 中只要出现手工 `login.aliexpress.com` 登录地址，新版会强制重写；已同步修复本机 `%LOCALAPPDATA%/YotoAeCspRecorder/accounts/admin.yaml`。
- 已重新打包 `ae_csp_recorder_app.zip`，并更新服务器动态录制器目录与静态下载 zip。

**涉及文件**：
- `aliexpress-csp-auto-upload/operator_launcher.py`
- `aliexpress-csp-auto-upload/tests/test_operator_launcher_urls.py`
- `aliexpress-csp-auto-upload/dist/ae_csp_recorder_app.zip`
- `开发文档.md`

## 2026-07-17 14:08 — 速卖通录制工具按钮改为单包动态下载并部署

**任务**：将 Web「下载录制工具」按钮改为直接下载一个已注入账号配置的完整压缩包，避免 app 包和配置文件分开下载。

**改动**：
- Web 下载逻辑改为调用 `/api/v1/ae_csp/template/recorder_bundle`，通过 `access_token` 查询参数触发浏览器原生下载。
- Server 动态 ZIP 从 Store 改为 Deflate 压缩，单包约 73.44MB；包内包含 `server_config.json`、`YotoAeCspRecorder.exe`、Patchright driver，不包含 `ms-playwright` 或 Chrome/Chromium exe。
- 已重新部署 Web 静态资源、录制器 onedir 目录与 Server Linux 二进制到 `www.yoto.work`。
- 线上校验：Web 资产旧静态 zip 引用 0、旧 `recorder_config` 引用 0、`recorder_bundle` 引用 1；动态下载包结构校验通过。

**涉及文件**：
- `commander-web-t260220-main/commander-web-t260220-main/src/api/modules/aeCsp.js`
- `commander-web-t260220-main/commander-web-t260220-main/scripts/verify-ae-csp-recorder-download.mjs`
- `commander-server-t260220/internal/services/ae_csp_recorder_zip.go`
- `commander-server-t260220/internal/services/ae_csp_recorder_zip_test.go`
- `commander-server-t260220/internal/store/auth_token.go`
- `commander-server-t260220/internal/middleware/default.go`
- `commander-server-t260220/internal/utils/jwt.go`
- `commander-server-t260220/ioc/providers.go`
- `开发文档.md`

## 2026-07-17 13:27 — 部署速卖通录制器 slim 包到服务器

**任务**：将重新打包后的速卖通录制器 slim zip 部署到服务器公网下载目录。

**改动**：
- 使用 `scripts/_ssh-probe/upload-ae-csp-recorder-static.js` 上传本地 `aliexpress-csp-auto-upload/dist/ae_csp_recorder_app.zip`。
- 替换服务器 `/opt/1panel/www/sites/www.yoto.work/index/downloads/ae_csp_recorder_app.zip`。
- 服务器侧校验 zip：包含 `YotoAeCspRecorder.exe`、`patchright/driver/node.exe`、`patchright/driver/package/cli.js`；不包含 `ms-playwright` 或 Chrome/Chromium exe。

**涉及文件**：
- `aliexpress-csp-auto-upload/dist/ae_csp_recorder_app.zip`
- `scripts/_ssh-probe/upload-ae-csp-recorder-static.js`
- `开发文档.md`

## 2026-07-17 13:24 — 速卖通录制器 slim 包补齐 Patchright driver 并重新打包

**任务**：复核运营录制压缩包缺失资源，一次性重新打出“不内置 Chrome、但内置 Patchright driver”的可运行 slim 包。

**改动**：
- `YotoAeCspRecorder.spec` 改为收集 `patchright` data，补齐 `patchright/driver/node.exe` 与 `patchright/driver/package/cli.js`。
- `scripts/build_operator_exe.py` 增加产物审计：缺主 exe、缺 Patchright driver、fat 包缺浏览器时直接失败。
- 新增打包资源回归测试，先复现 `datas=[]` 漏打 driver，再验证修复。
- 已重新生成 `aliexpress-csp-auto-upload/dist/ae_csp_recorder_app.zip`，产物为 slim 包，不包含 `ms-playwright` 或 Chrome/Chromium exe。

**涉及文件**：
- `aliexpress-csp-auto-upload/YotoAeCspRecorder.spec`
- `aliexpress-csp-auto-upload/scripts/build_operator_exe.py`
- `aliexpress-csp-auto-upload/tests/test_operator_bundle_packaging.py`
- `aliexpress-csp-auto-upload/dist/ae_csp_recorder_app.zip`

## 2026-07-17 13:24 — 开发文档归档

**任务**：按文档长度阈值归档旧 `开发文档.md`，重建活跃开发流水。

**改动**：
- 旧文档已移动到 `docs/handover/开发文档-归档-20260717.md`。
- 新建当前活跃 `开发文档.md`，保留归档索引与最近上下文摘要。

**涉及文件**：
- `开发文档.md`
- `docs/handover/开发文档-归档-20260717.md`

## 归档索引

| 文件名 | 归档时间 | 条目约数 |
| --- | --- | --- |
| `docs/handover/开发文档-归档-20260616.md` | 2026-06-16 | 历史开发流水 |
| `docs/handover/开发文档-归档-20260717.md` | 2026-07-17 13:24 | 50+ 条 / 1022 行 |

## 最近上下文摘要

- 2026-07-14：CSP MVP 推进到 publish/API 骨架，真实发品仍需录制 capture 固化 endpoints。
- 2026-07-14：补充 AliExpress CSP 自动上架实现计划和四份设计/验收文档。
- 2026-07-14：修订 CSP 设计，强调 filtered request 保留完整 body、RefreshCookie 成功才写盘。
- 2026-07-14：建立速卖通全托管 CSP 独立 MVP 方案，明确 login/capture/publish 的阶段边界。
- 2026-07-13：落地 AliExpress 方案 B MVP CLI 骨架与 capture 请求日志能力。
