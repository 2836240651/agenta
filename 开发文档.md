# Commander 开发文档

> 工作区：`D:\dev\workspace`  
> **规则**：每次任务完成并有实质改动时，在本文档**顶部**追加一条记录（最新在上）。  
> 格式与**归档拆分**阈值见 [`AGENTS.md`](./AGENTS.md)「开发文档与交接」。  
> 历史归档：`docs/handover/`

## 2026-06-17 11:10 — product_issue 图生图保真链路 + 部署上线

**任务**：表单原图仅作 AI 参考、100% 保真产品形态；本地验证通过后推送 GitHub 并部署线上。

**改动**：
- **原型图视觉分析**：生图前 `analyzeProductVision` 解析 `PRODUCT_TYPE/MATERIAL/SHAPE/KEY_FEATURES/DO_NOT_CHANGE`，锁定参考图产品身份。
- **标题/场景输入拆分**：`buildProductIssueTitleQuery`（类目+尺寸+备注）、`buildProductIssueSceneQuery`（仅备注）；场景 LLM 以视觉 brief 为最高优先级。
- **保真 prompt**：每张轮播图 prompt 追加 `MUST KEEP from reference` + `STRICT reference-image fidelity` 条款。
- **禁止文生图回退**：`lyncr_product_carousel.go` 移除 `GenerateImage` 纯文生图回退（避免产品被替换）。
- **场景约束收紧**：生活方式/环境/细节场景明确要求「必须是参考图中同一产品」。
- **图生图质量**：`/images/edits` 请求 `quality` 由 `low` 升为 `high`。
- **多行标签解析**（同批）：`parseTaggedLines3` 支持 `PROMPT:` 标签与值分行。

**本地验证**：
- `go test ./internal/tasks/... -count=1` 通过。
- `RUN_LYNCR_LIVE_TEST=1` + 桌面 `批量上货表单 (8)(1).xlsx`：5 张轮播图全部 success；视觉分析锁定「两件并排橄榄绿飞德饵笼」；对比目录 `.local/fidelity-test/`。

**涉及文件**：
- `commander-server-t260220/internal/constants/prompt.go`
- `commander-server-t260220/internal/tasks/product_issue_llm_lyncr.go`
- `commander-server-t260220/internal/tasks/product_issue_llm_lyncr_test.go`
- `commander-server-t260220/internal/tasks/proto_product_issue.go`
- `commander-server-t260220/internal/tasks/lyncr_product_carousel.go`
- `commander-server-t260220/internal/utils/lyncr.go`
- `开发文档.md`

## 2026-06-17 09:57 — 修复 product_issue 场景 LLM 多行标签解析

**任务**：修复轮播场景 LLM 输出标签与值分行时 `parseProductIssueScene` 报「缺少 PROMPT: 行」的问题。

**改动**：
- `parseTaggedLines3`：标签行值为空时，合并后续非空行直至遇到下一个已知标签前缀（`PROMPT:` / `TITLE_ZH:` / `TITLE_EN:` 等）。
- 新增 `collectTaggedValue` 辅助函数，保持同行 `TAG: value` 解析行为不变。
- 单测：`TestParseProductIssueScene_MultilineTagValue`、`TestParseProductIssueScene_SameLineTagValue`。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr_test.go`
- `开发文档.md`

**验证**：`go test ./internal/tasks/... -count=1` 通过。

## 2026-06-17 09:38 — Image link 空链修复闭环 + 线上重部署

**任务**：继续上一轮 `Image link cannot be empty` 修复，补跑 live 测试、用含 `96fa564` 的最新 Server 二进制重部署线上，并修正部署脚本重启逻辑。

**改动**：
- **Server**（`commander-server-t260220` @ `96fa564`）：下发 Agent 前 `validateVariantHTTPURLs`，变体缺公网图 URL 直接 fail，不再下发。
- **Agent**（`b795a44`）：`ProductIssue.go` 先归一化轮播图 URL，再构建 `variationList`，避免 `thumbUrl` 为空。
- **部署脚本**：`restore-config-and-deploy.js` 将 `docker start` 改为 `docker restart`，确保已运行容器也加载新二进制。
- **本地 live 测试**：`TestProductIssueLive_FromFormRow` 用桌面 xlsx，`113.55s` 通过，5 张轮播图全部 `success`。
- **线上**：上传新编译 Linux 二进制（来自 `commander-server-t260220`），`docker restart` 后 `health_http=200`，`StartedAt` 已更新。

**涉及文件**：
- `commander-server-t260220/internal/tasks/proto_product_issue.go`
- `commander-agent-t260220-main/.../backend/internal/factory/dxm/ProductIssue.go`
- `scripts/_ssh-probe/restore-config-and-deploy.js`
- `开发文档.md`

**待你侧验证**：Web 提交一条新 `product_issue` 任务，Agent 需为 **4.0.2**（`https://www.yoto.work/downloads/Agent_windows_amd64_4.0.2.zip`），确认店小秘 `batchPublish` 不再报 `Image link cannot be empty`。

## 2026-06-16 17:36 — 修复识图/生图链路卡顿与错误兜底

**任务**：修复产品发布链路中识图未生效、图片 MIME 判断越界风险、DISPIMG 串图风险，以及生图部分失败仍继续下发的问题；并执行 150 秒内单测验证。

**改动**：
- `internal/utils/lyncr.go`：
  - 将 `lyncrChatIncludeVisionImage` 改为 `true`，恢复多模态识图输入。
  - 将 `grsaiPollTimeout` 从 `6m` 调整为 `90s`，避免单次轮询过长导致任务长时间挂起。
- `internal/pkg/imageinput.go`：
  - 修复 GIF 判断条件优先级问题，避免短字节切片越界 panic。
- `internal/utils/xlsx.go`：
  - DISPIMG 回退策略改为仅在“整表仅 1 处 DISPIMG”时才使用首图兜底，降低多商品串图概率。
- `internal/tasks/proto_product_issue.go`：
  - 生图后增加结果门禁：全部失败直接失败；部分失败直接失败并给出成功/失败数量，不再继续下发 Agent。
- 测试：
  - `go test ./internal/tasks -count=1 -timeout 150s` 通过（live 测试关闭）。
  - `RUN_LYNCR_LIVE_TEST=1` + `LYNCR_FORM_XLSX_PATH="C:/Users/Administrator/Desktop/批量上货表单 (8)(1).xlsx"` 下，`TestProductIssueLive_FromFormRow` 在 `114.26s` 内通过（总耗时 `114.691s`，满足 150 秒要求）。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/utils/lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/pkg/imageinput.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/utils/xlsx.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/proto_product_issue.go`
- `开发文档.md`

## 2026-06-16 17:32 — product_issue 生图改为并发执行

**任务**：解决 `product_issue` 任务在“正在进行图片生成...”阶段串行等待过长的问题，将轮播图生图改为并发执行。

**改动**：
- `internal/tasks/lyncr_product_carousel.go`：将逐张串行生图改为 goroutine + `sync.WaitGroup` 并发执行。
- 保留原有单张处理逻辑（先图生图，失败后回退文生图；失败时写入对应 `status/message`），仅调整执行模型为并发。
- 验证：`go test ./internal/tasks -run TestRunLyncrProductCarousel -count=1` 通过（包可编译，无回归报错）。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/lyncr_product_carousel.go`
- `开发文档.md`

## 2026-06-16 17:00 — 对话模型切换为 gpt-5.5

**任务**：按用户要求，将当前开发环境的对话模型改为 `gpt-5.5`，沿用已配置的 GRSAI `url/api_key`。

**改动**：
- `etc/config/config-dev.yaml`：将 `lyncr.model_chat` 从 `agnes-2.0-flash` 调整为 `gpt-5.5`。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config-dev.yaml`
- `开发文档.md`

## 2026-06-16 16:58 — 接入 GRSAI 生图配置与后端兼容

**任务**：将 GRSAI 的生图 `url/api_key/model` 加入项目，并让现有后端生图链路兼容 `POST /v1/api/generate`。

**改动**：
- `internal/utils/lyncr.go`：
  - 新增对 GRSAI 域名的识别（`lyncr.url` 含 `grsai` 时走 GRSAI 协议）
  - 文生图/图生图统一兼容 `POST /api/generate`，请求体包含 `model/prompt/images/aspectRatio/replyType`
  - 新增 GRSAI 响应解析与状态处理（`succeeded/running/failed/violation`）
- `etc/config/config-dev.yaml`：
  - 生图配置改为 `url: https://grsaiapi.com`
  - `api_key` 设置为用户提供的测试 key
  - `model_images` 设置为 `gpt-image-2`
- 验证：`go test ./internal/utils` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/utils/lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config-dev.yaml`
- `开发文档.md`

## 2026-06-16 16:45 — 改图建议 prompt：原图锁外形，remark 仅作抽象补充

**任务**：LLM 生成改图/轮播图提示词时，不再根据 `remark` 盲猜产品结构；`remark` 仅补充抽象细节，禁止改变原图结构外观形变。

**改动**：
- `internal/constants/prompt.go`：强化 `LLM_PRODUCT_ISSUE_SCENE_SYSTEM`：
  - 明确 `remark` 仅作抽象细节补充（光线/背景/质感/构图/使用场景等）
  - 明确禁止“根据 remark 猜产品类型/编造结构细节”，禁止任何结构/轮廓/比例变化
- `internal/tasks/product_issue_llm_lyncr.go`：场景 LLM user 文本同步调整为同口径约束
- 验证：`go test ./...` 通过

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `开发文档.md`

## 2026-06-16 16:42 — 开发文档归档拆分（触发阈值）

**任务**：`开发文档.md` 达到归档阈值（≥800 行），按规则归档并重建活跃文档。

**改动**：
- 将旧 `开发文档.md` 归档为 `docs/handover/开发文档-归档-20260616.md`
- 重建新的 `开发文档.md`，保留归档索引与最近摘要，后续继续在本文件顶部追加

**涉及文件**：
- `docs/handover/开发文档-归档-20260616.md`
- `开发文档.md`

## 2026-06-16 16:42 — Lyncr 生图能力单元测试（本地 mock）

**任务**：增加单元测试，验证 `lyncr` 可生成图片（至少返回非空图片 URL），且不依赖真实外网/Key。

**改动**：
- `internal/utils/lyncr_test.go`：使用 `httptest` 模拟 Lyncr OpenAI 兼容接口：
  - `/v1/images/generations`：覆盖一次“先 500 后成功”的重试路径，并断言返回非空 URL
  - `/v1/images/edits`：校验 multipart 中包含 `image` parts，并断言返回 URL
  - 补充 `normalizeLyncrImageSize`、`resolveLyncrV1Base` 的基础行为测试
- 验证：`go test ./...` 通过

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/utils/lyncr_test.go`
- `开发文档.md`

### 归档索引

| 归档文件 | 归档时间 | 说明 |
|----------|----------|------|
| `docs/handover/开发文档-归档-20260616.md` | 2026-06-16 16:42 | 触发阈值（≥800 行），归档并重建活跃文档 |

---

### 最近摘要（来自最新归档）

- **2026-06-16 16:20**：生图文本输入改为仅依赖 `remark`
- **2026-06-16 16:32**：Lyncr 图生图失败回退文生图（保证可出图）
- **2026-06-16 16:35**：场景生图 prompt 调整：原图锁形状，remark 仅给抽象改进
- **2026-06-16 16:13**：回滚生图链路近期改动（按 GitHub 对照）
- **2026-06-16 16:10**：修复 product_issue 失败链路“错误被吞”问题

## 2026-06-16 16:35 — 场景生图 prompt 调整：原图锁形状，remark 仅给抽象改进

**任务**：按用户要求调整场景生图提示词：表单原图仅用于图生图锁定形状/结构；根据 remark 判断商品类型但只给抽象改进建议，避免导致产品变形。

**改动**：
- `internal/constants/prompt.go`：强化 `LLM_PRODUCT_ISSUE_SCENE_SYSTEM` 约束：
  - 原图为形状/结构唯一约束（no deformation / no geometry change / same silhouette）
  - remark 仅用于抽象改进方向（光线、背景、构图、质感、场景），禁止引导结构变化
- `internal/tasks/product_issue_llm_lyncr.go`：场景 LLM user 文本补充“原图使用规则”，明确不得变形。
- 验证：`go test ./internal/tasks/...` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `开发文档.md`

## 2026-06-16 16:13 — 回滚生图链路近期改动（按 GitHub 对照）

**任务**：用户要求将近期“生图稳定版 prompt / 配置开关 / server 端失败拦截”改回 GitHub 之前代码路径。

**改动**：
- 回滚 `internal/constants/prompt.go`：
  - 移除 `*_SCENE_SYSTEM_*_STABLE`
  - 移除 `ProductIssueSceneSystemLegacy` 与 `ProductIssueSceneSystemByVersion`
  - `ProductIssueSceneSystem` 恢复为旧常量返回
- 回滚 `internal/types/server.go`：移除 `Lyncr.ScenePromptVersion`
- 回滚 `internal/tasks/product_issue_llm_lyncr.go`：恢复 `ProductIssueSceneSystem(ozon)` 调用
- 回滚配置：
  - `etc/config/config-dev.yaml` 删除 `lyncr.scene_prompt_version`
  - `etc/config/config.yaml` 删除 `lyncr.scene_prompt_version`
- 回滚 `internal/tasks/proto_product_issue.go` 新增的失败聚合拦截逻辑（恢复此前流程）
- 验证：`go test ./internal/tasks/...` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/types/server.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/proto_product_issue.go`
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config-dev.yaml`
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config.yaml`
- `开发文档.md`

## 2026-06-16 16:10 — 修复 product_issue 失败链路“错误被吞”问题

**任务**：用户反馈“仍失败且像是整体逻辑没对上”；排查后修复 Server 在生图失败时仍下发 Agent，导致真实错误被 Agent 统一文案覆盖的问题。

**改动**：
- 复盘任务 `5577d1d1-4345-474c-845a-f6083ca75a62`：`send.images[1..5]` 全部 `status=failed`，message 为 `图生图失败（已尝试4次）`，内含上游 `utf-8 codec can't decode byte 0x89`。
- 在 `internal/tasks/proto_product_issue.go` 增加 `ensureProductIssueCarouselSuccess(images)`：
  - 生图后先在 Server 侧校验 5 张是否全部成功（`status=success` 且 `url` 非空）。
  - 若失败，直接返回聚合错误（示例：第1张/第2张失败原因），不再继续下发 Agent。
- 新增 `minInt` 辅助函数用于摘要输出。
- 验证：`go test ./internal/tasks/...` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/proto_product_issue.go`
- `开发文档.md`

## 2026-06-16 16:01 — 新增 scene prompt 配置开关（stable/legacy）

**任务**：按用户要求增加配置开关，支持在不改代码的情况下切换生图场景系统提示词版本。

**改动**：
- 新增配置项：`lyncr.scene_prompt_version`（支持 `stable` / `legacy`，默认按空值与 `stable` 处理）。
- `internal/types/server.go`：`ServerConfigLyncr` 增加字段 `ScenePromptVersion`。
- `internal/constants/prompt.go`：新增 `ProductIssueSceneSystemByVersion(ozon, version)`，统一做版本选择与兜底。
- `internal/tasks/product_issue_llm_lyncr.go`：场景系统提示改为读取配置开关。
- 更新配置文件示例：
  - `commander-server.../etc/config/config-dev.yaml`
  - `commander-server.../etc/config/config.yaml`
- 验证：`go test ./internal/tasks/...` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/types/server.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config-dev.yaml`
- `commander-server-t260220-main/commander-server-t260220-main/etc/config/config.yaml`
- `开发文档.md`

## 2026-06-16 16:00 — 生图场景 Prompt 升级为稳定版（保留旧版）

**任务**：按用户要求将“更稳的生图 prompt”落到代码，且旧版 prompt 不删除，便于回滚与 AB 对照。

**改动**：
- 在 `internal/constants/prompt.go` 新增：
  - `LLM_PRODUCT_ISSUE_SCENE_SYSTEM_STABLE`
  - `LLM_PRODUCT_ISSUE_SCENE_SYSTEM_OZON_STABLE`
- 默认入口 `ProductIssueSceneSystem(ozon bool)` 切换为返回 stable 版本。
- 保留旧版并新增 `ProductIssueSceneSystemLegacy(ozon bool)` 用于回滚/对照（旧常量 `LLM_PRODUCT_ISSUE_SCENE_SYSTEM*` 未删除）。
- 运行 `go test ./internal/tasks/...` 通过。

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `开发文档.md`

## 2026-06-16 16:05 — Lyncr 图生图链路一键验证脚本

**任务**：用户要求停止逐次报错排查，改为脚本一次性验证「xlsx 取图 + Lyncr /images/edits 提交」关键链路，并输出可对比结论。

**改动**：
- 新增 `scripts/validate-lyncr-edits.ps1`
- 脚本能力：
  - 自动发现桌面最新 `.xlsx`（优先匹配 `*(8)(1)*`）
  - 直接从 `xl/media/*` 提取首图并打印文件头（用于验证取图）
  - 读取 `config-dev.yaml` 中 `lyncr.url/api_key/model_images`
  - 同时验证 4 组请求：`raw+image`、`raw+image[]`、`png+image`、`png+image[]`
  - 打印 HTTP 状态与响应摘要，避免再走 UI 点重试
- 运行结果：4 组均返回 `400`，错误分别为 `utf-8 decode byte 0xff/0x89`，说明当前异常不由字段名或图片格式单点导致，链路问题可稳定复现。

**涉及文件**：
- `scripts/validate-lyncr-edits.ps1`
- `开发文档.md`

## 2026-06-16 16:35 — Temu 生图链路视觉优先优化（本地）

**任务**：优化 Temu 产品发布生图链路，先做原型图视觉分析；轮播场景仅按视觉 brief + remark + 尺寸生成，移除 categoryName 语义参与；图生图失败不再回退纯文生图。同步更新模板说明文案。不部署服务器。

**改动**：
- `prompt.go` 新增 `LLM_PRODUCT_ISSUE_VISION_SYSTEM` 与 `ProductIssueVisionSystem()`，场景 prompt 增加「视觉分析优先」约束
- `product_issue_llm_lyncr.go` 新增：
  - `analyzeProductVision()`（原型图前置视觉分析）
  - `parseProductVisionBrief()`（结构化解析）
  - `buildProductIssueTitleQuery()` / `buildProductIssueSceneQuery()`（标题与轮播输入拆分）
- `proto_product_issue.go` 编排改为：先 `analyzeProductVision`，再调用 `runProductIssueLyncrLLM(titleQuery, sceneQuery, ...)`
- `lyncr_product_carousel.go` 去掉 `GenerateImage` 纯文生图回退；图生图失败直接标记 failed
- 新增测试 `product_issue_llm_lyncr_test.go`（视觉 brief 解析 + sceneQuery 不含类目）
- 模板说明脚本 `scripts/generate-temu-upload-template.mjs` 更新为：
  - B 列类目仅上架映射
  - 轮播语义依赖原型图视觉分析 + remark
  - 重新生成 `public/Temu批量上货表单.xlsx`

**验证**：
- `go test ./internal/tasks/...` 通过
- `pnpm run generate:temu-template` 成功，模板已覆盖输出

**涉及文件**：
- `commander-server-t260220-main/commander-server-t260220-main/internal/constants/prompt.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/proto_product_issue.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/lyncr_product_carousel.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/tasks/product_issue_llm_lyncr_test.go`
- `scripts/generate-temu-upload-template.mjs`
- `commander-web-t260220-main/commander-web-t260220-main/public/Temu批量上货表单.xlsx`

## 2026-06-16 15:20 — Agent WebSocket 切换到本地后端

**任务**：修复 Agent WebSocket 连接仍指向生产域名的问题；按本地联调要求改为连接 `ws://localhost:34206/api/v1/agent/online`。

**改动**：
- Agent 常量：`backend/internal/constants/agent.go`
  - `WsProtocol`：`wss` -> `ws`
  - `BaseServerAddress`：`www.yoto.work` -> `localhost:34206`
- 重启 `wails dev` 后，服务器侧 `Agent17` 在线状态正常。

**涉及文件**：
- `commander-agent-t260220-main/commander-agent-t260220-main/backend/internal/constants/agent.go`

## 2026-06-16 15:30 — Agent 4.0.2 编译：申报价按分提交

**任务**：用户反馈任务 JSON 中 `price:2365` 仍显示 ¥0.23；继续完成 Agent 侧修复并发版。

**结论**：
- Server 下发正确（2365 分 = 23.65 元），问题在 **Agent 4.0.1 仍执行 `supplierPrice/100`**
- 店小秘 `add` 接口 `supplierPrice` 单位为「分」；误传 23.65 会被当成 0.24 元

**改动**：
- Agent：`ProductIssue.go` 直接提交 `v.Price`（分），并打印 `supplierPrice(分)=… (约 … 元)` 便于核对
- 本地 `wails build` 产出 `build/bin/Agent.exe` 与 `Agent_windows_amd64_4.0.2.zip`
- `version.yaml` 新增 4.0.2（`force_update: true`），待上传静态包与同步线上

**涉及文件**：
- `commander-agent-.../backend/internal/factory/dxm/ProductIssue.go`
- `commander-server-.../etc/config/version.yaml`
- `开发文档.md`

**操作**：各端完全退出旧 Agent → 运行新 `Agent.exe` → **重新上架**（已错价商品须店小秘侧调价或下架重发）。

## 2026-06-16 15:00 — 修复 Agent 显示版本仍为 4.0.1

**任务**：用户反馈下载 `Agent_windows_amd64_4.0.2.zip` 后，程序内仍显示 4.0.1。

**根因**：
- Agent 当前版本号来自常量 `backend/internal/constants/agent.go`，发布包编译时仍写死为 `4.0.1`。

**改动**：
- 将 `AgentVersion` 从 `4.0.1` 更新为 `4.0.2`。
- 重新 `wails build` 并重打 `Agent_windows_amd64_4.0.2.zip`。
- 覆盖上传线上 `https://www.yoto.work/downloads/Agent_windows_amd64_4.0.2.zip`。

**涉及文件**：
- `commander-agent-t260220-main/commander-agent-t260220-main/backend/internal/constants/agent.go`
- `开发文档.md`

**线上同步（2026-06-16 14:26）**：
- 已上传 `Agent_windows_amd64_4.0.2.zip` → `www.yoto.work/downloads/`
- 已同步 `version.yaml` → `124.223.27.98:/data/commander/config/version.yaml`
- 校验：`curl -I` zip → HTTP 200；`GET /api/v1/openapi/agent_version` → `4.0.2` `force_update: true`

## 2026-06-16 14:00 — 修复 Temu 上架申报价缩小 100 倍

**任务**：表单填 28.98/29.98 元，店小秘申报价显示 ¥0.28/¥0.29；停服排查并修复后重新部署 Server。

**根因**：
- Server 解析 Excel 后 `variants.price` 已是「分」（如 2898=28.98 元），线上任务 `0195820d` 可复证
- 6/15 Agent 误将 `supplierPrice` 再 `/100` 后以「元」提交，店小秘 API 按「分」解析 → 28.98 被当成 0.28 元
- 部署过程中发现 `/data/commander/config/config.yaml` 被 `version.yaml` 内容覆盖，导致容器无法启动（已恢复）

**改动**：
- Agent：`dxm/ProductIssue.go` 提交 `supplierPrice` 直接使用 Server 下发的分，不再 `/100`
- Server：恢复损坏的 `internal/utils/xlsx.go`；`agent_product_issue.go` 元→分改用 `math.Round` 避免浮点截断
- 线上：已停服 → 恢复 `config.yaml` → 热更新 Server 二进制 → `health_http=200`，Agent 已重连

**涉及文件**：
- `commander-agent-.../backend/internal/factory/dxm/ProductIssue.go`
- `commander-server-.../internal/services/agent_product_issue.go`
- `commander-server-.../internal/utils/xlsx.go`
- `scripts/_ssh-probe/restore-config-and-deploy.js`（部署脚本）
- `开发文档.md`

**后续**：本地 Agent 需 `wails generate module` + `wails build` 发新版，否则仍走旧逻辑。


**任务**：生成带下拉校验的 Temu 上货 Excel 模板，本地 public 目录供下载；暂不部署线上。

**改动**：
- 新增 `src/constants/temuCategoryNames.js`（24 个合法类目，与 Server `category.go` 对齐）
- 新增 `scripts/generate-temu-upload-template.mjs`：生成 `上货数据` / `选项`（隐藏）/ `填写说明` 三 Sheet；B/C/D 列数据验证（类目、父体/子体、编号 1–100）
- 产出 `public/Temu批量上货表单.xlsx`（含 1 父体 + 5 子体示例行）
- `package.json` 增加 `pnpm run generate:temu-template`
- `ExcelUpload.vue`：Temu 工作区下载新模板；AliExpress/Ozon 显示「该平台模板开发中」

**涉及文件**：
- `commander-web-t260220-main/.../src/constants/temuCategoryNames.js`
- `scripts/generate-temu-upload-template.mjs`
- `commander-web-t260220-main/.../public/Temu批量上货表单.xlsx`
- `commander-web-t260220-main/.../src/views/platform-entrance/components/auto-upload/ExcelUpload.vue`
- `commander-web-t260220-main/.../src/views/platform-entrance/components/auto-upload/AgentWorkspace.vue`

## 2026-06-16 — Web / Agent 推送 GitHub

**任务**：将 Web 端、Agent 端本地代码同步到 GitHub。

**改动**：
- **Web** `hyhacct/commander-web-t260220`：本地目录原无 `.git`，已 `git init` 并对齐 `origin/main`；提交 `d8a2c56` 并推送 `main`
  - 认证卡宽度、营销弹层 `box-sizing` 修复
  - `zh-CN` 公司名「台州」→「泰州」
  - 删除已废弃的 `src/views/login/*`（由 auth 流程替代）
- **Agent** `hyhacct/commander-agent-t260220`：工作区与 `origin/main` 一致（`ad033b7`），无实质 diff；`git push` 确认 **Everything up-to-date**
- Agent 工作区大量 `M` 为 CRLF 换行噪声，已 `git restore .` 清理

**涉及文件**：
- `commander-web-t260220-main/.../src/locales/zh-CN.json`
- `commander-web-t260220-main/.../src/views/auth/auth-theme.css`
- `commander-web-t260220-main/.../src/views/marketing/marketing-theme.css`
- `commander-web-t260220-main/.../src/views/login/**`（删除）

## 2026-06-16 — AGENTS.md：禁止远程部署 + 任务完成必推 GitHub

**任务**：补充 Agent 硬性规则——禁止自行将本地代码部署到远程服务器；每次有实质改动的任务收工前必须 commit + push GitHub。

**改动**：
- 「远程服务器」新增「远程部署 — Agent 硬性禁止」：禁止 scp/rsync/upload 脚本、远程构建重启、改线上目录；仅允许 SSH 只读排查
- 「Git 仓库 → 推送规则」：由「仅用户要求才 push」改为「实质改动后收工前必须 commit + push」
- 「每次任务必做」增加推送 GitHub 步骤；开发文档禁止项增加「有改动却不 push、改以 scp 更新服务器」
- 「Agent 版本发布」步骤 5/6 及静态包上传注明须用户明确要求，Agent 禁止自行上传

**涉及文件**：
- `AGENTS.md`
- `开发文档.md`

## 2026-06-16 10:30 — 4.0.1 更新时间改为 6 月 16 日

**任务**：Agent 4.0.1 版本记录的 `update_time` 应显示为 2026-06-16。

**改动**：
- `version.yaml` 中 4.0.1 的 `update_time` → `1781539200`（2026-06-16 00:00 北京时间）
- 已同步线上 `/data/commander/config/version.yaml`

**涉及文件**：
- `commander-server-.../etc/config/version.yaml`

## 2026-06-16 10:25 — 4.0.1 下载改公网托管（GitHub 私有库 404）

**任务**：Web 点击 4.0.1 下载出现 GitHub Not Found。

**根因**：
- 截图 URL 误为 `commander-agent-t250220`（正确为 **t260220**）
- 仓库私有，未登录访问 `releases/download/...` 一律 404，不能作 `download_url`

**改动**：
- 本地打包 `Agent_windows_amd64.zip`（约 8.1 MB）上传至 `www.yoto.work/downloads/Agent_windows_amd64_4.0.1.zip`（HTTP 200）
- `version.yaml` 的 `download_url` 改为 `https://www.yoto.work/downloads/Agent_windows_amd64_4.0.1.zip`；`message` 保留 GitHub Release 页链接作溯源
- 更新 `AGENTS.md`：私有库禁止 GitHub 直链、推荐 yoto.work/downloads 托管

**涉及文件**：
- `commander-server-.../etc/config/version.yaml`
- `AGENTS.md`
- 线上 `/data/commander/config/version.yaml`、`/opt/1panel/www/sites/www.yoto.work/index/downloads/`

## 2026-06-16 10:20 — 4.0.1 下载指向 GitHub Release + AGENTS.md 规则

**任务**：将 `version.yaml` 中 4.0.1 的 `download_url` 指向 GitHub Release，并写入 Agent 版本发布规则。

**改动**：
- `4.0.1.download_url` → `https://github.com/hyhacct/commander-agent-t260220/releases/download/4.0.1/Agent_windows_amd64.zip`（Release 页：`.../releases/tag/4.0.1`）
- `AGENTS.md` 新增「Agent 版本发布（version.yaml）」：本地/线上路径、发布步骤、同步与校验
- 已同步至线上 `/data/commander/config/version.yaml`

**涉及文件**：
- `commander-server-.../etc/config/version.yaml`
- `AGENTS.md`

## 2026-06-16 10:15 — 修复 Agent 版本列表为空（线上缺 version.yaml）

**任务**：Web 管理后台「Agent 下载列表」与 Agent 端「更新日志」均显示暂无记录。

**根因**：线上 Server 容器挂载目录 `/data/commander/config/` 仅有 `config.yaml`，缺少 `version.yaml`；`/api/v1/openapi/agent_version` 返回 403「读取版本配置文件失败」。

**改动**：
- 将本地 `etc/config/version.yaml` 上传至服务器 `/data/commander/config/version.yaml`
- 接口恢复 `code: 0`，返回 26 条版本记录
- 顺带将 4.0.1 更新说明改为生产文案（下载链接待后续替换为 4.0.1 包）

**涉及文件**：
- `commander-server-.../etc/config/version.yaml`（本地）
- 线上 `/data/commander/config/version.yaml`
- `scripts/_ssh-probe/upload-version-yaml.js`

## 2026-06-16 10:05 — 登录页中英文卡片宽度对齐

**任务**：中文登录页白框与英文界面保持相同尺寸。

**改动**：
- 根因：`.auth-page__center` 使用 `align-items: center` 时卡片按文案内容收缩，中文约 308px、英文约 391px
- `.auth-card` 改为固定 `width: min(540px, calc(100vw - 32px))`，`.auth-page__center` 补 `width: 100%`
- 已构建并部署至 `www.yoto.work`

**涉及文件**：
- `commander-web-.../src/views/auth/auth-theme.css`
- `commander-web-.../src/views/marketing/marketing-theme.css`

## 2026-06-16 09:35 — 台州改泰州（Web 文案 + 线上部署）

**任务**：检查并修正 Web 首页、登录页及线上 `www.yoto.work` 中误写的「台州」为「泰州」。

**改动**：
- `zh-CN.json` 的 `marketing.pageTitle` / `companyName` / `copyright` 三处「台州亿拓…」改为「泰州亿拓…」（登录页 `MarketingBrand` 与首页共用该文案）
- 本地 `pnpm build` 后同步 `dist/` 至服务器 `/opt/1panel/www/sites/www.yoto.work/index/`
- 删除线上旧 bundle `index-DXX3J8IG.js`（仍含「台州」且已不再被 `index.html` 引用）

**涉及文件**：
- `commander-web-t260220-main/commander-web-t260220-main/src/locales/zh-CN.json`
- 线上 `www.yoto.work` 静态资源

## 2026-06-16 09:30 — Agent 4.0.1 推送 GitHub 并发布 Release

**任务**：将 Agent 端代码提交到 `hyhacct/commander-agent-t260220`，同步创建 GitHub Release。

**改动**：
- 提交 `ad033b7`：生产环境默认连接 `wss` + `www.yoto.work`
- 推送 `main` 与 tag `4.0.1`
- `wails build -platform windows/amd64` 编译并上传 `Agent_windows_amd64.zip`（约 8.1 MB）
- Release 说明涵盖：店小秘类目/轮播图/Token 二次探针、失败重试、WebSocket runAt、usererr 错误文案等（相对 2.0.3）

**Release**：https://github.com/hyhacct/commander-agent-t260220/releases/tag/4.0.1

**说明**：本机仅构建 Windows 包；macOS arm64 需在 Mac 上执行 `publish.sh` 补传。

**涉及文件**：
- `commander-agent-t260220-main/.../backend/internal/constants/agent.go`
- `开发文档.md`

## 2026-06-15 21:35 — 三端代码推送 GitHub（hyhacct）

**任务**：将 Server / Web / Agent 本地代码上传到对应 GitHub 仓库。

**改动**：
- `commander-server-t260220` → `b851f2a`（product_issue 修复、task_list 净化、Lyncr 对齐等）
- `commander-web-t260220` → `b78fc74`（批量上货 UI、welcome 拆分、taskError 等）
- `commander-agent-t260220` → `9b7abdf`（店小秘上架/WebSocket 等）；**停止跟踪** `agent/config.yaml`
- 本地 `.gitignore` 补充：`config-dev.yaml`、`agent/config.yaml`、`etc/logs/`、`*.exe`

**未提交**：`config-dev.yaml`、`agent/config.yaml`（含账号密钥）

**涉及文件**：
- 三端 `.gitignore`
- `开发文档.md`

## 2026-06-15 21:07 — 修复生图完成后卡住（product_issue）

**任务**：任务 `99fcabd9` 界面显示「生图完成，共 5 张」后长时间无进展。

**根因**：
- 生图后同步 `ProductIssuePrecheck` 阻塞等 Agent 45s+，且向 Agent 下发含 `imageRawUrl`（~400KB）的完整 payload，WebSocket 易堵塞
- Agent 上架只需轮播公网 URL，不需要 Excel 内嵌主图

**改动**：
- 移除 `RunProductIssue` 生图后同步预检（retask / Agent 内仍有探针）
- `AsyncSend` 前清空 `ImageRawUrl`，减小 WebSocket 载荷
- 下发后更新 message 为「已提交 Agent，正在店小秘上架...」
- 若 5 张轮播图已为 `success` 且含 https URL，跳过 Lyncr 重复生图（便于 retask / 进程恢复）

**验证**：
- Server 重启后 `RecoverFromStore` 自动续跑该任务，因 Agent17 离线快速失败：`Agent 未连接`（不再卡在「生图完成」）
- 需先启动 Agent（`wails dev` 显示 ACTIVE）再对 `99fcabd9` 点重试

**涉及文件**：
- `internal/tasks/proto_product_issue.go`
- `开发文档.md`

## 2026-06-15 20:48 — 修复 task_list 巨包导致 Web 无响应

**任务**：用户再次上架后 Web 请求后端无反应。

**根因**：
- `task_list` 返回每条任务的完整 `send.imageRawUrl`（约 400KB base64），多任务合计 ~800KB+，序列化/传输需 30s 级，前端像卡死
- 新上架时 Agent 正忙（生图/上架），同步预检 15s 超时，任务 `e4d90d06` 报「上架前预检失败」

**改动**：
- 列表接口剥离 `imageRawUrl`；支持按 `agent_id` / `platform` 筛选
- Agent 预检 `SyncSend` 超时 15s → 45s

**涉及文件**：
- `internal/pkg/task_list_sanitize.go`（新建）
- `internal/services/agent_task_list.go`
- `internal/types/request/agent.go`
- `internal/tasks/proto_product_issue_precheck.go`
- `开发文档.md`

## 2026-06-15 20:30 — 对齐 Lyncr dev 配置并修复 agnes 生图

**任务**：用户提供新 Lyncr API Key，要求恢复上架。

**根因**：
- `ENV=dev` 读 `config-dev.yaml`，仍指向 `www.lyncr.cn` + 旧 Key/模型；新 Key 仅在 `api.lyncr.cn` + `agnes-*` 模型可用
- `agnes-image-2.0-flash` 不支持 `response_format`；`/images/edits` 对二进制参考图报 UTF-8 解析错误

**改动**：
- `config-dev.yaml`：`lyncr.url` → `https://api.lyncr.cn`，模型与 `config.yaml` 对齐
- `lyncr.go`：去掉 `response_format`，规范化 `1K`→`1024x1024`
- `lyncr_product_carousel.go`：图生图失败时回退文生图

**涉及文件**：
- `commander-server-.../etc/config/config-dev.yaml`
- `commander-server-.../internal/utils/lyncr.go`
- `commander-server-.../internal/tasks/lyncr_product_carousel.go`
- `开发文档.md`

## 2026-06-15 20:20 — 纠正「无效 Token」误报：实为 Lyncr API Key 失效

**任务**：用户 `retask` 任务 `0eb9642f` / `35bc2b25` 仍显示「Temu 授权 Token 无效」，称模型 API 已切换正常。

**根因**：
- Server 日志：`listing 标题 LLM` / `场景 N LLM: chat/completions 失败: {"error":{"code":"invalid_token","message":"无效 Token","param":"Authorization"}}` —— 为 **Lyncr 鉴权失败**，非店小秘 Temu Token
- `humanize_error` 凡含「无效 Token」一律映射为 Temu 授权文案，误导排查
- `etc/config/config-dev.yaml` 中 `lyncr.api_key` 经 `curl` 实测仍返回 `invalid_token`
- 店小秘预检 `product_issue_precheck` 与 Temu `add` 探针均通过；任务 `0eb9642f` 已被软删除，`retask` 返回「任务不存在」

**改动**：
- `humanize_error` / `upstream_error`：优先识别 Lyncr `chat/completions` + `authentication_error`，展示「更新 lyncr.api_key」
- `WorkerManager`：`scopedProgressWriter` + `runAt` 收尾校验，拒绝 `runAt==0` 的过期 Agent 回包

**涉及文件**：
- `commander-server-.../internal/pkg/humanize_error.go`
- `commander-server-.../internal/pkg/upstream_error.go`
- `commander-server-.../internal/manager/WorkerManager.go`
- `开发文档.md`

## 2026-06-15 19:50 — 修复 retask 过期回包覆盖与重试脏数据

**任务**：用户反复 `retask` 任务 `71fb4b27` 仍显示「无效 Token」，但本地同 payload 直连店小秘可成功。

**根因**：
- `retask` 复用同一 `taskId`，多次点击会产生多轮 Agent 执行；**较早一轮的失败回包晚到**，覆盖新一轮结果，界面长期显示旧的「无效 Token」
- `product_issue` 在 `AsyncSend` 后 Server 即标记「执行完成」，与 Agent 异步回包竞态
- 重试未清空 `send.images` / 变种 `url`，可能跳过重新生图

**改动**：
- Server：`AgentMessage` 增加 `runAt`（与 `queued_at` 对齐）；`Update` 忽略过期回包
- Server：`product_issue` 不再在 `AsyncSend` 后标记成功，最终状态仅由 Agent 回包决定
- Server：`RerunTask` 清空 `images` 与变种 `url`，强制重新走 Lyncr + 上架
- Agent：回传 `runAt` 字段（随消息原样带回）

**涉及文件**：
- `commander-server-.../internal/manager/WorkerManager.go`
- `commander-server-.../internal/types/agent.go`
- `commander-server-.../internal/tasks/proto_product_issue.go`
- `commander-server-.../internal/services/agent_retask.go`
- `commander-agent-.../backend/types/task.go`
- `commander-server-.../internal/pkg/humanize_error.go`（修复 switch 语法）
- `开发文档.md`

## 2026-06-15 19:35 — 修复 Temu 上架误报「无效 Token」

**任务**：新任务 `861f6ede` 在预检通过后仍失败；用户要求修好整条上架链路。

**根因**：
- 预检在提交瞬间通过，但 Server 生图（Lyncr）排队数分钟后 Agent 才真正调用店小秘 `add`；期间未再次校验 Token/Cookie
- Excel 子体行内嵌图被写成 `data:image/...` 塞进变种 `thumbUrl`（单任务 JSON 约 400KB），与店小秘/Temu 实际发布链路不一致
- 店小秘对部分失败场景仍返回「无效 Token」，前端统一映射后误导排查方向
- 本地用同 payload 复现：`add` 接口可 `code:0` 成功，说明 Token 本身多数时候有效

**改动**：
- Agent：轮播图仅取 `success` 的公网 URL；变种缩略图强制替换为轮播图，禁止 `data:` URL 进入上架 body
- Agent：真正 `add` 前再次 `ProbeTemuListingToken`；Cookie 固定从 `dianxiaomi.com` 标签页读取
- Agent：Temu 供货价 `supplierPrice` 由「分」换算为「元」（÷100）
- Server：Temu 不再把 Excel 子体图写入 `variant.url`；生图完成后所有变种统一用 AI 轮播公网图
- Server：下发 Agent 前对 Temu 再跑一次 `product_issue_precheck`（覆盖生图等待窗口）
- 错误文案：补充「图片空间不足」「轮播图未生成」等真实原因

**涉及文件**：
- `commander-agent-.../backend/internal/utils/dxm_images.go`（新建）
- `commander-agent-.../backend/internal/factory/dxm/ProductIssue.go`
- `commander-agent-.../backend/internal/utils/resty_aliexpress.go`
- `commander-server-.../internal/tasks/proto_product_issue.go`
- `commander-server-.../internal/services/agent_product_issue.go`
- `commander-server-.../internal/pkg/humanize_error.go`
- `开发文档.md`

## 2026-06-15 19:10 — Temu Token 探针修复与失败任务不再自动重试

**任务**：用户更新店小秘授权后重试仍报「无效 Token」；排查并修复预检误报与自动重试循环。

**改动**：
- Agent：`probeTemuListingToken` 改为调用真实上架接口 `pddkjProduct/add` 最小探针（缺变种即停，不发布有效商品）；旧 `categoryAttrMatch` 探针对 shopId/Token 不敏感已废弃
- Agent：`GetByDxmCookies` 优先从任意已打开标签页取 Cookie，避免无店小秘标签时新开页即关导致会话不稳定
- Agent：`UploadProductIssue` 移除 multipart 同请求多余的 `SetBody(body)`
- Server：手动「重试」`retask` 前先执行 `product_issue_precheck`
- Server：关闭 `TaskRestart` 对失败 `product_issue` 的每 3 分钟自动重试（避免 Token 类错误被反复触发）
- 实测：Agent 浏览器内同类目（32498 鱼钩）+ 店铺 8381218 上架接口 `code:0` 成功，说明当前 Token 已可用

**涉及文件**：
- `commander-agent-t260220-main/.../backend/internal/utils/dxm_biz.go`
- `commander-agent-t260220-main/.../backend/internal/utils/resty_dxm.go`
- `commander-agent-t260220-main/.../backend/internal/utils/resty_aliexpress.go`
- `commander-server-t260220-main/.../internal/services/agent_retask.go`
- `commander-server-t260220-main/.../internal/crontabs/task_restart.go`
- `开发文档.md`

## 2026-06-15 18:45 — Temu 上架预检接口与失败文案人性化

**任务**：新增上架前预检 API，避免任务失败原因以整段 JSON 展示给前端。

**改动**：
- Agent：新增协议 `product_issue_precheck`，检查店小秘登录、店铺绑定、Temu Token（类目属性探针）
- Agent：统一店小秘业务响应解析 `CheckDxmBizResponse`，dispatcher 回包 message 经 `usererr.Normalize`
- Server：新增 `POST /api/v1/agent/product_issue_precheck`；`product_issue` 提交前强制预检
- Server：`HumanizeTaskMessage` 写入任务 message，剥离 JSON 并映射「无效 Token」等常见错误
- Web：提交 Temu 批量上货前先调预检；任务列表失败态用 `formatTaskFailureMessage` 展示可读原因

**涉及文件**：
- `commander-agent-t260220-main/commander-agent-t260220-main/backend/internal/utils/dxm_biz.go`
- `commander-agent-t260220-main/commander-agent-t260220-main/backend/internal/factory/dxm/ProductIssuePrecheck.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/services/agent_product_issue_precheck.go`
- `commander-server-t260220-main/commander-server-t260220-main/internal/pkg/humanize_error.go`
- `commander-web-t260220-main/commander-web-t260220-main/src/utils/taskError.js`
- `开发文档.md`

## 2026-06-15 18:10 — 店小秘上架 Token 错误信息增强

**任务**：排查 Temu 上架 `无效 Token` 场景，并在 Agent 侧补充可执行的错误提示。

**改动**：
- 调整 `UploadProductIssue`：在 HTTP 200 场景下新增店小秘业务响应 `code/msg` 解析，避免“HTTP 成功但业务失败”被误判为成功
- 当店小秘返回 `无效 Token` 时，返回更明确提示，明确需同时检查 Temu 店铺的“商品发布授权”与“库存/订单 token”
- 保持原有请求链路与参数不变，仅增强失败识别与可读报错

**涉及文件**：
- `commander-agent-t260220-main/commander-agent-t260220-main/backend/internal/utils/resty_dxm.go`
- `开发文档.md`

### 归档索引

| 归档文件 | 归档时间 | 说明 |
|----------|----------|------|
| （暂无） | — | 当前为第一份活跃文档 |

---

## 2026-06-15 17:07 — Welcome 工作台拆分（阶段 0-4）

**任务**：实施 Welcome 工作台可维护性拆分方案，保持 `/welcome` 单页与既有交互不变。

**改动**：
- `welcome/index.vue` 改为薄壳（provide + 7 个子组件），并引入 `src/styles/welcome.css`
- 新增 `welcome/sections/*` 与 `components/UserReportReminderModal.vue`，保留原有 6 区块功能
- 新增 `src/composables/welcomeCtxKey.js`、`src/composables/welcome/useWelcomePage.js` 及 welcome 领域拆分文件（hero/agent/reptile/userReport/tickets）
- 将工单与 Agent 逻辑拆到 `src/composables/admin/useAdminTickets.js`、`src/composables/admin/useAdminAgent.js`；`useAdminPage.js` 改为组合层并复用共享工具
- 抽出并复用共享工具：`src/utils/ticketHelpers.js`、`src/utils/saleFormatters.js`、`src/utils/saleExportExcel.js`、`src/composables/usePlatformNavDisplay.js`
- 样式从 `welcome/index.vue` 外提到 `src/styles/welcome.css`，清理残留 admin 样式导致的 CSS 破损
- 构建验证：`pnpm build` 通过

**涉及文件**：
- `commander-web-t260220-main/commander-web-t260220-main/src/views/welcome/index.vue`
- `commander-web-t260220-main/commander-web-t260220-main/src/views/welcome/sections/*`
- `commander-web-t260220-main/commander-web-t260220-main/src/views/welcome/components/UserReportReminderModal.vue`
- `commander-web-t260220-main/commander-web-t260220-main/src/composables/welcome/*`
- `commander-web-t260220-main/commander-web-t260220-main/src/composables/admin/*`
- `commander-web-t260220-main/commander-web-t260220-main/src/composables/useAdminPage.js`
- `commander-web-t260220-main/commander-web-t260220-main/src/styles/welcome.css`
- `commander-web-t260220-main/commander-web-t260220-main/src/styles/admin.css`
- `开发文档.md`

---

## 2026-06-15 17:30 — 编写工作区根目录 README.md

**任务**：在 `D:\dev\workspace` 根目录编写详细的 `README.md` 项目总览文档。

**改动**：
- 新增 `README.md`：系统架构、三端能力、技术栈、环境变量、一键/手动启动、端口约定、目录结构、子项目说明、开发约定、文档索引、Git 与安全禁忌、常见问题

**涉及文件**：
- `README.md`
- `开发文档.md`

---

## 2026-06-15 16:10 — 登录页卡片加宽

**任务**：登录页中央卡片过窄，与通白背景不协调，适当加宽。

**改动**：
- `auth-theme.css`：`.auth-card` `max-width` 440px → 540px；内边距 32/36/28 → 36/44/32，登录与注册页共用

**涉及文件**：
- `commander-web-t260220-main/commander-web-t260220-main/src/views/auth/auth-theme.css`
- `开发文档.md`

---

## 2026-06-15 15:32 — 修复 Agent 每 5 秒 WebSocket 重连与 Vite EPIPE

**任务**：排查三端启动后 Agent 约每 5 秒重连、Vite HMR `EPIPE` 崩溃。

**改动**：
- 根因：`wails dev` 产物为 `Agent-dev.exe`，`restart-commander.ps1` 仅杀 `Agent.exe`，残留多实例争抢同一 `agent id` 的 WebSocket
- `scripts/restart-commander.ps1`：`Stop-CommanderAgent` 增加终止 `Agent-dev`、`wails` 及对应 CIM 进程
- `frontend/vite.config.ts`：stdout/stderr `EPIPE` 忽略 + `customLogger` 包装，避免 HMR 日志写管道崩溃
- 已清理残留 PID `2732`，当前仅保留单实例 WS 连接

**涉及文件**：
- `scripts/restart-commander.ps1`
- `commander-agent-t260220-main/commander-agent-t260220-main/frontend/vite.config.ts`
- `开发文档.md`

---

## 2026-06-15 — AGENTS.md 增加 smart-agent Git 拉取/推送规则

**任务**：将 `https://github.com/2836240651/smart-agent` 的最优 clone / pull / push 约定写入 `AGENTS.md`。

**改动**：
- `AGENTS.md`：新增「Git 仓库（smart-agent）」章节（HTTPS 认证、首次克隆、拉取 rebase、推送前检查、禁止项、故障排查）
- `交接文档.md`：新增「Git 主仓库」章节并引用 `AGENTS.md`

**涉及文件**：
- `AGENTS.md`
- `交接文档.md`
- `开发文档.md`

---

## 2026-06-15 — 登记远程服务器信息

**任务**：将当前项目使用的云主机 SSH 信息写入 `AGENTS.md`。

**改动**：
- `AGENTS.md`：新增「远程服务器（当前项目）」章节（`124.223.27.98` / `root`）
- `交接文档.md`：新增「远程服务器」章节（凭据引用 `AGENTS.md`，不写明文密码）

**涉及文件**：
- `AGENTS.md`
- `交接文档.md`
- `开发文档.md`

---

## 2026-06-15 15:00 — 开发文档归档拆分规则

**任务**：`AGENTS.md` 增加文档达量归档规则，避免单文件过深。

**改动**：
- `AGENTS.md`：新增「文档归档与拆分」章节（阈值、归档路径、操作步骤）
- `开发文档.md`：增加归档索引表
- `交接文档.md`：补充归档目录说明

**涉及文件**：
- `AGENTS.md`
- `开发文档.md`
- `交接文档.md`

---

**任务**：项目根目录创建交接文档；`AGENTS.md` 增加「任务完成必写开发文档」规则。

**改动**：
- 新增 `交接文档.md`（项目全貌、端口、营销页、启动方式）
- 新增 `开发文档.md`（本文件，开发变更流水账）
- 更新 `AGENTS.md`：新增「开发文档与交接」硬性规则

**涉及文件**：
- `交接文档.md`（新建）
- `开发文档.md`（新建）
- `AGENTS.md`

---

## 2026-06-15 14:28 — 三端启动脚本同步 Agent 端口

**任务**：`scripts` 下启动脚本与 env 脚本同步 Agent dev 端口 `34115`，避免顶掉 Web `5173`。

**改动**：
- `restart-commander.ps1`：增加 `$AgentDevPort = 34115`；重启前释放该端口；状态输出与提示文案更新
- `env/scripts/verify.ps1`、`start-services.ps1`：端口检查增加 `:34115`
- `AGENTS.md`：端口表与一键启动说明

**涉及文件**：
- `scripts/restart-commander.ps1`
- `D:\dev\env\scripts\verify.ps1`
- `D:\dev\env\scripts\start-services.ps1`
- `AGENTS.md`

---

## 2026-06-15 14:20 — Agent 前端 dev 端口固定为 34115

**任务**：限制 Agent Vite dev server 端口，不与 Web 5173 冲突。

**改动**：
- `commander-agent-.../frontend/vite.config.ts`：`server.port = 34115`，`strictPort: true`，`host: 127.0.0.1`

**涉及文件**：
- `commander-agent-t260220-main/commander-agent-t260220-main/frontend/vite.config.ts`

---

## 2026-06-15 14:18 — 修复营销页平台图标空白

**任务**：TEMU、Shein、JD.com、拼多多 Iconify 图标缺失导致空白。

**改动**：
- 下载四个平台 favicon 至 `public/assets/platforms/`
- `platforms.js` 上述平台改用 `image` 字段
- `marketing/index.vue` 支持 `image` / Iconify 双模式渲染

**涉及文件**：
- `commander-web-.../public/assets/platforms/*.png`
- `commander-web-.../src/views/marketing/data/platforms.js`
- `commander-web-.../src/views/marketing/index.vue`
- `commander-web-.../src/views/marketing/marketing-theme.css`

---

## 2026-06-15 14:15 — 营销落地页与登录页品牌区

**任务**：按 `baseLinePng/营销落地页.png` 开发未登录首页；登录/注册继承营销页左上角品牌；品牌图标来自 `icons/`。

**改动**：
- 新增营销页模块：`src/views/marketing/`（页面、头部、语言切换、技术支持弹窗、样式）
- 品牌图标复制至 `public/assets/brands/`
- 路由 `/` 指向营销页；`App.vue` 使用 `isPublicShell` 独立布局
- `AuthShell.vue` 复用 `MarketingBrand` 左上角品牌
- 中英文文案：`locales/zh-CN.json`、`locales/en.json` 增加 `marketing` 段

**涉及文件**：
- `commander-web-.../src/views/marketing/**`
- `commander-web-.../public/assets/brands/**`
- `commander-web-.../src/router/index.js`
- `commander-web-.../src/App.vue`
- `commander-web-.../src/views/auth/AuthShell.vue`
- `commander-web-.../src/locales/zh-CN.json`
- `commander-web-.../src/locales/en.json`
