# Commander 工作区交接文档

> 工作区根目录：`D:\dev\workspace`
> 最后更新：**2026-07-20 11:59 CSP 单行 live 草稿可见**

本文档用于长会话断点恢复。逐次开发流水见同目录 [`开发文档.md`](./开发文档.md)。

---

## 0. 断点恢复硬规则

- 每走完一个会改变事实状态、代码状态、线上状态或关键判断的步骤，必须同步更新本文件。
- 本文件写“当前可接手状态”：做到哪一步、下一步是什么、哪些结论已经被推翻、哪些风险不能碰。
- `开发文档.md` 写详细时间线；`交接文档.md` 写最新状态和接力入口，避免长会话断开后逻辑丢失。
- 涉及 cookie、账号、密钥、Token、`config.yaml`、`.env` 时，只记录“存在/用途/禁止提交”，不得写入具体敏感值。
- 发布、上架、服务器部署等真实外部动作，必须有用户本轮明确指令；否则只做本地验证、文档和 GitHub 推送。

---

## 1. 当前主线：AliExpress CSP 全托管接口回放

| 项 | 当前状态 |
|----|----------|
| 本地项目 | `D:\dev\workspace\aliexpress-csp-auto-upload` |
| 当前目标 | 只针对“CSP 全托管页面接口回放”，把已上架/已保存商品沉淀为训练样本，再做模板灌入、payload dry-run 与草稿校验 |
| 当前账号/channel | 使用本机已配置的 `tenant5` / `channelId=870088`；cookie 仅本地使用，禁止提交 |
| 当前可靠验收口径 | 生成速卖通承认的对象 -> render 成功 -> 模板灌入 `missing_critical=[]` -> 保存草稿 -> 真实 `draft` tab 可见 |
| 最新有效对象 | 单行 live gate 已生成草稿 `product_id/draft_id=1005012765475947`，`visibility.tab=draft`，`matched_by=draft_id` |
| 最新 seed 链路 | `captures/valid_object_tenant5_870088_20260720_095019/training_chain` 已生成 `visible_draft_payload_ready` |
| Excel 上架结论 | 当前已证明单行 Excel/payload 可生成真实可见草稿；仍不能承诺 Excel 批量上架，因为小批量草稿 gate 和发布后可见性尚未 live 验证 |
| 闭环计划 | `aliexpress-csp-auto-upload/docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md` |
| 闭环 Gate 状态 | Gate 2、图片替换门禁、单行 live 草稿 gate、清理台账均已通过；3-5 行小批量草稿 gate 和显式发布 gate 仍只完成非 live 外壳 |
| 已推翻结论 | 之前的 `draft_visibility_not_found` 不是速卖通未落库，而是检查器误判 tab |
| 修正点 | `check_draft_visibility.py` 必须解析 `data.jsonBody.tab`，并主动点击目标 tab，不能只看 URL 或默认响应 |
| 最新验证 | `run_excel_draft_gate.py --live --confirm-live` -> `ok=true/status=draft_visible/live=true`，`new_product_id=1005012765475947`，真实 `draft` tab 可见，清理台账已生成 |
| 操作手册 | `aliexpress-csp-auto-upload/docs/excel-to-csp-listing-runbook.md` |
| live 前置审计 | `aliexpress-csp-auto-upload/docs/csp-live-validation-readiness.md` |
| 最新 live 输出 | `aliexpress-csp-auto-upload/captures/excel_draft_gate_live_AE-CSP-Auto-Test_20260720_115841`（本机忽略目录，不提交 git） |

### CSP 下一步

1. 下一步是 3-5 行小批量草稿 gate；执行前仍需用户明确授权 `--live --confirm-live --limit 3`。
2. 如 Excel 使用本地图片，必须先补图片上传能力并生成 `uploaded_images` 映射；没有映射时 payload 阶段会停在 `image_upload_required`。
3. 小批量每个通过行都必须从真实 `draft` tab 查到 ID 或标题，并写出行级 `cleanup_ledger.jsonl`。
4. 小批量通过后，才能从清理台账挑选一个 `draft_visible` 草稿显式发布。
5. 真实发布必须 `--live --confirm-publish --draft-id --account --channel-id`，且只认 `pending_approval/online_product` 为成功；不得批量发布。

### CSP 闭环定义

只有同时满足以下条件，才能说“Excel 到上架闭环”成立：

- Excel 行已转成标准 CSP product JSON，且本地字段/图片校验通过。
- draft payload 不携带旧 `itemId/draftId/productId`、旧 SKU 身份或 seed 图片 URL。
- 单行真实 `draft.submit` 生成新速卖通对象，并在真实 `draft` tab 可见。
- 3-5 行同类目小批量草稿试跑有清理台账。
- 显式确认发布后，商品进入 `pending_approval` 或 `online_product`；若进入 `violation_qc_failed`，必须能输出失败原因。

### CSP 关键脚本

| 脚本 | 用途 |
|------|------|
| `scripts/record_valid_object_chain.py` | 当前账号/channel 新建速卖通有效对象，灌入模板，render/保存/草稿可见性校验 |
| `scripts/check_draft_visibility.py` | 检查商品在 `draft/pending/online/offline` 等真实列表 tab 中是否可见 |
| `scripts/capture_existing_draft_save_chain.py` | 从真实草稿 tab 打开已有草稿，点击保存并验证可见性，作为保存链路基准 |
| `scripts/build_visible_draft_training_chain.py` | 从 `draft_visible` capture 生成 raw sample、seed profile、draft payload，并做 dry-run |
| `scripts/build_excel_draft_payloads.py` | 从运营 Excel 生成标准 product JSON、draft payload 和本地 dry-run 报告；Excel 任意 error 即停止 |
| `uploader/csp_uploader/image_payload_rewriter.py` | 本地图片上传映射后的 payload 图片 URL 替换器；缺映射时返回 `image_upload_required` |
| `scripts/run_excel_draft_gate.py` | 单行 payload 的受控草稿 gate；默认 dry-run，真实保存必须 `--live --confirm-live --limit 1` |
| `scripts/run_excel_batch_draft_gate.py` | 3-5 行小批量草稿 gate；默认 dry-run，live 必须 `--live --confirm-live`，同账号/channel 带本地锁 |
| `scripts/run_excel_publish_gate.py` | 单个已可见草稿的显式发布 gate；真实发布必须 `--live --confirm-publish --draft-id`，只认 `pending_approval/online_product` 为成功 |
| `uploader/csp_uploader/cleanup_ledger.py` | 真实草稿可见后的清理台账；记录 row/account/channel/title/product_id/tab，便于人工清理 |
| `scripts/run_publish_chain.py` | 受控发布链路 runner；真实发布必须 `--live --confirm-publish` |
| `docs/excel-to-csp-listing-runbook.md` | 操作 runbook；包含 6 步 gate 和硬停止条件 |
| `docs/csp-live-validation-readiness.md` | live 前置审计；记录候选 payload、已存在产物、授权口径和下一条 live 命令 |

---

## 2. Commander 项目组成

| 子项目 | 路径 | 技术栈 | 说明 |
|--------|------|--------|------|
| **Web** | `commander-web-t260220-main/commander-web-t260220-main` | Vue3 + Vite + TDesign | 管理端网站：营销落地页、登录注册、工作台 |
| **Server** | `commander-server-t260220-main/commander-server-t260220-main` | Go + Gin + GORM + PostgreSQL | 后端 API + Agent WebSocket |
| **Agent** | `commander-agent-t260220-main/commander-agent-t260220-main` | Wails v2 + Go + Vue3 | 桌面客户端（非浏览器站点） |
| **AliExpress CSP 工具** | `aliexpress-csp-auto-upload` | Python + Playwright/Patchright | 速卖通 CSP 页面录制、接口回放、模板灌入、草稿校验 |

环境工具链根目录：`D:\dev\env`（`FISHAGENT_ENV_ROOT`）

---

## 3. 端口约定（勿混用）

| 服务 | 端口 | 访问方式 |
|------|------|----------|
| Web 管理端 / 营销页 | **5173** | 浏览器 `http://localhost:5173` 或 `http://127.0.0.1:5173` |
| Agent 前端 dev（`wails dev`） | **34115** | 仅 Wails 内嵌 WebView 加载，不要用浏览器当网站打开 |
| Server API | **34206** | `http://localhost:34206` |
| PostgreSQL | 5432 | 本地数据目录 `D:\dev\env\data\postgresql` |

常见误区：`wails dev` 与 `pnpm dev`（Web）若都占 5173，浏览器访问 `localhost` 可能误进 Agent 界面。Agent 前端已固定 **34115**，Web 独占 **5173**。

---

## 4. 一键启动

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\dev\workspace\scripts\restart-commander.ps1
```

会依次处理 Server / Web / Agent。手动启动顺序见 [`AGENTS.md`](./AGENTS.md) 的“启动顺序”章节。

---

## 5. 关键路径速查

```text
D:\dev\workspace\
├── AGENTS.md                 # Agent 工作规则
├── 交接文档.md               # 本文件：最新接手状态
├── 开发文档.md               # 逐次开发流水
├── aliexpress-csp-auto-upload\
├── scripts\restart-commander.ps1
├── commander-agent-t260220-main\
├── commander-server-t260220-main\
└── commander-web-t260220-main\
```

---

## 6. Git 与提交状态

- 根仓库当前用于文档和项目管理，推送远端为 `origin`。
- `aliexpress-csp-auto-upload` 是独立本地 Git 仓库，当前无远端；代码提交只能本地提交，不能声称已推送该子仓库。
- 有实质改动时，只暂存本次相关文件；禁止 `git add .`。
- 提交前必须检查 `git status`、`git diff`、`git diff --staged`、`git log -3 --oneline`。

---

## 7. 远程服务器

远程服务器连接信息只在 [`AGENTS.md`](./AGENTS.md) 内部规则中维护，本文件不写密码或敏感配置。

允许只读排查；上传、部署、重启、替换线上文件必须用户在本轮明确要求。

---

## 8. 安全禁忌

- 禁止提交 cookie、Token、密码、`.env`、真实 `config.yaml` 密钥。
- 禁止把 CSP 账号 cookie 内容写入文档或日志。
- 禁止把接口 HTTP 200 当成发布成功；必须以后端业务返回和列表可见性为准。
- 禁止在没有显式确认时执行真实发布、上架或远程部署。

---
