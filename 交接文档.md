> **2026-07-28 草稿门禁（方案 A）已落地**：`POST .../form/save-draft` → Agent `listing1688_seller_save_draft`（仅填已验证 title/images + 保存草稿）→ 回写 `draft_saved`；`form/validate` 会清 `draft_saved`；`publish` 须 `ready + draft_saved`，Agent 须 `draft_verified=true` 且复用已打开 offer-new 页提交。未做：无 binding 属性盲填、企业店 live 发品、Web 按钮。
> **2026-07-25 类目同步**：`POST .../category/inspect` 仍可用；只读同步 required_fields。
> **下一刀（可选）**：Web Step4 接「保存草稿」按钮；企业店账号 live；更多 field_bindings。
# Commander 工作区交接文档

> 工作区根目录：`D:\dev\workspace`
> 最后更新：**2026-07-28 13:49 — 1688 方案 A 草稿门禁闭环**

本文档用于长会话断点恢复。逐次开发流水见同目录 [`开发文档.md`](./开发文档.md)。

---

## 0. 断点恢复硬规则

- **每走完一步就更新本文件**（用户硬性要求）：会改变事实状态、代码状态、线上状态或关键判断的步骤，必须同步写入，避免刷新记忆后从 0 开始。
- **每次上架任务（新 JSON / 新类目 / worker 停点）必须同步更新**：
  - 专项：`aliexpress-csp-auto-upload/docs/handover/上架流程-阻力分析与改进方案.md` §1 当前停点
  - 本文件 §1 摘要（3–5 行即可）
- 本文件写“当前可接手状态”：做到哪一步、下一步是什么、哪些结论已经被推翻、哪些风险不能碰。
- `开发文档.md` 写详细时间线；`交接文档.md` 写最新状态和接力入口。
- 涉及 cookie、账号、密钥、Token、`config.yaml`、`.env` 时，只记录“存在/用途/禁止提交”，不得写入具体敏感值。
- 发布、上架、服务器部署等真实外部动作，必须有用户本轮明确指令；否则只做本地验证、文档和 GitHub 推送。

---

## 1. 当前主线（活跃）：JSON + 浏览器填表上架

> 与下方「接口 replay 批量」是两条并行能力；**此刻优先做本条**。
> **专项交接（阻力根因 + 改进方案 + 每次上架必写规则）**：[`aliexpress-csp-auto-upload/docs/handover/上架流程-阻力分析与改进方案.md`](./aliexpress-csp-auto-upload/docs/handover/上架流程-阻力分析与改进方案.md)

| 项 | 当前状态 |
|----|----------|
| 本地项目 | `D:\dev\workspace\aliexpress-csp-auto-upload` |
| **当前停点** | **产品表单样本批次已生成**（7 品 / 3 CSP 类目）；Browser 实跑暂停，待用户确认按类目认证顺序执行 |
| 产品表单 | `C:\Users\Administrator\Desktop\产品表单.xlsx` — **511 商品 / 1722 SKU**（10 sheet） |
| 样本批次 | `aliexpress-csp-auto-upload/inbox/product_form_samples/` — 校验 **7/7 通过** |
| 已打通样本 | 钓钩 `HOOK-023`、`TPL-DEMO-001` → `submitted_success`（**仅证明钓钩类目**） |
| 浏览器账号 | **`demo`** + `channelId=2078391`；JSON 勿写 `tenant5` |
| 运营路径 | `ops_validate_json.py` → `ops_enqueue_json.py` → `ops_worker.py` |
| **核心结论** | JSON 字段可复用；**销售属性填法按类目完全不同**（钓钩=comma_batch，钓鱼工具=color_rows）。新类目必须先 probe/认证，不能复制 JSON 就 expect 闭环 |

### 1.1 已打通（不要再怀疑）

| 环节 | 结论 |
|------|------|
| JSON 校验/入队 | ✅ `ops_json.py` + CLI |
| 类目选择 | ✅ 钓钩路径 |
| 商品属性 + 高关注化学品 | ✅ |
| 主图 + 营销图 | ✅ |
| 是否原箱 / 物流属性 | ✅ |
| PC 详描 | ✅ |
| 供货价多行 | ✅ 三行 `18.9/20.9/23.9` 能写入 |
| 销售属性多值 | ✅ `comma_batch`：`8,6,4` → color_count 0→5 |
| SKU 图第 1 行 | ✅（2–3 行仍可再核 capture） |
| **HOOK-023 浏览器提交** | ✅ `ab54ab90…` → `submitted_success`，productId=`1005012779918826` |

### 1.2 仍卡住 / 已破解

| 卡点 | 状态 | 结论 |
|------|------|------|
| **海关监管属性** | ✅ 已破解（2026-07-21 复跑 click3） | 「去设置」→ `.hscode-drawer` → 填 `种类(Kind)=其他(Other)` → 点 **`确 认`**（不是确定）→ tag=`已配置`。`_complete_hscode_drawer` + 下拉等待已加固。产物：`exports/customs_click3.json`（HANDLER ok=True） |
| **多 SKU 第 2/3 个销售属性** | ✅ 选择器结论已对齐 | **没有**「添加自定义名称」按钮；颜色 select search 常 **disabled**；逐次 Enter 只会覆盖第 1 个值。正确做法：一次填入 `8,6,4` + Enter（probe `sale_separators.json`：count→5）。代码：`ensure_sale_prop_skus` 已改为 `comma_batch` 模式 |

### 1.3 代码已改、未 commit/push

| 文件 | 改了什么 |
|------|----------|
| `uploader/csp_uploader/publish_form.py` | `_complete_hscode_drawer`（种类+确认）；下拉选项等待；`ensure_sale_prop_skus` **comma_batch**；销售属性限「选择主色系」 |
| `scripts/debug_customs_click3.py` | 点击后调用 `_handle_customs_attributes` 验证 drawer |
| `scripts/probe_sale_add_second.py` / `probe_sale_color_select.py` / `probe_sale_size_multi.py` / `probe_sale_separators.py` | 多 SKU 探测脚本 |
| `scripts/ops_worker.py` | 海关未 ok 阻断提交 |
| `templates/HOOK-023.json` | `account=demo`；`sale_name`=`8/6/4` |

**未 push GitHub**（未明确要求不主动 commit/push）。

### 1.4 最新草稿 / capture（本机）

- **成功提交**：job=`ab54ab90-cba2-4d88-adc9-b6908934f9f5`，productId=`1005012779918826`，status=`submitted_success`
- capture：`captures/job_ab54ab90-…_20260721_105050`
- 海关产物：`exports/customs_click3.json`（HANDLER ok）；多 SKU：`exports/sale_separators.json`

### 1.5 Skill 沉淀（2026-07-21）

| 项 | 路径 |
|----|------|
| Skill | `.cursor/skills/ae-csp-browser-listing/SKILL.md` |
| 坑位 | `.cursor/skills/ae-csp-browser-listing/references/pitfalls.md` |
| 规则 | `.cursor/rules/ae-csp-browser-listing.mdc` |
| 用户注册 | `%USERPROFILE%\.cursor\skills\ae-csp-browser-listing\` → 项目 skill（junction） |
| **运营 JSON 模板** | `aliexpress-csp-auto-upload/templates/ops_listing_template.json`（多 SKU）+ `_single.json` |
| **运营说明（喂大模型）** | `aliexpress-csp-auto-upload/templates/README-运营上架.md` |
| **模板实跑** | job=`5daa6bc8…` → `submitted_success`，productId=`1005012774311145`（`TPL-DEMO-001`） |

触发：CSP/速卖通全托管上架、ops_worker、海关、多 SKU、HOOK-023。

**模板链路硬修复（2026-07-21）**：
- `job_to_publish_product` 透传 `cat_property` / `detail_images`
- 供货价按「供货价」列表头写，禁止写到「货品编码」
- 写入 `skus[].outer_sku_id` 到货品编码列（不可用价格当编码）
- 绑定弹窗需二次确认；成功看 `pop.publish.submit` / 成功页

### 1.6 下一步（接手即做）

1. **产品表单样本**：按 `inbox/product_form_samples/README.md` — 每 CSP 类目 Browser 认证 2–3 个，勿 511 个逐个 Browser
2. 钓钩 `cat_100005539_samples.json` → enqueue → worker（comma_batch 已通，优先 Replay 验证）
3. 鱼饵 / 钓鱼工具样本 → probe + worker（钓鱼工具用 color_rows）
4. Excel DISPIMG 主图需运营导出后替换 `images/<row_id>/`
5. 用户要求时再 commit/push

### 1.7 接手命令速查

```powershell
cd D:\dev\workspace\aliexpress-csp-auto-upload

# 海关（已通过，必要时复验）
python scripts/debug_customs_click3.py

# 多 SKU 分隔符复验
python scripts/probe_sale_separators.py

# 样本重跑
python scripts/ops_validate_json.py templates/HOOK-023.json
python scripts/ops_enqueue_json.py templates/HOOK-023.json
python scripts/ops_worker.py --job-id <uuid>
```

### 1.8 关键判断（已写入，勿重复踩坑）

1. 页面自动化必须用 `demo` + 带 `channelId` 的 `publish_url`。
2. 销售属性输入必须在「选择主色系」内；全局 `placeholder*=自定义` 会命中「型号」。
3. `sale_name` 不要带 `#`；**钓钩**多 SKU 用逗号一次提交（`8,6,4`）；**钓鱼工具**用逐行「选择主色系 + 自定义名称」（见 probe_tool_progressive）。
4. 海关：抽屉 `.hscode-drawer`，确认按钮文案是 **`确 认`**；Kind 选 **其他(Other)**。
5. Worker：**海关不过不提交**。
6. **`选择主色系 N/128` 的 N 是字符计数，不是 chip 数**——勿用它判断多 SKU 成功。

---

## 1B. 并行能力（次要）：CSP 接口 replay 批量（Excel/payload）

| 项 | 状态摘要 |
|----|----------|
| 目标 | Excel/product JSON → draft/publish payload → ID-only 命中 `pending_approval/online_product` |
| 批量硬规则 | `batch_operational_guard.py`：默认 5 行、同类目、`--confirm-live/--confirm-publish` |
| 最新 3 行 batch | R1=`1005012766587831`、R2=`1005012773670015`、R3=`1005012766721648` → `pending_approval` |
| 计划/手册 | `docs/superpowers/plans/2026-07-20-csp-excel-to-listing-closure.md`；`docs/excel-to-csp-listing-runbook.md` |
| 说明 | **本轮不优先**；页面自动化（§1）与 replay 可并存，勿混账号配置 |

### CSP 关键脚本（replay）

| 脚本 | 用途 |
|------|------|
| `scripts/run_excel_batch_publish_gate.py` | 3-5 行小批量 replay 发布 |
| `uploader/csp_uploader/batch_operational_guard.py` | 运营批量硬规则 |
| `scripts/ops_validate_json.py` / `ops_enqueue_json.py` / `ops_worker.py` | **JSON 页面填表主路径（§1）** |
| `scripts/debug_customs_click3.py` | 海关去设置网络+DOM |
| `scripts/probe_sale_add_second.py` | 多 SKU 第 2 销售属性选择器 |

---

## 2. Commander 项目组成

| 子项目 | 路径 | 技术栈 | 说明 |
|--------|------|--------|------|
| **Web** | `commander-web-t260220-main/commander-web-t260220-main` | Vue3 + Vite + TDesign | 管理端网站：营销落地页、登录注册、工作台 |
| **Server** | `commander-server-t260220-main/commander-server-t260220-main` | Go + Gin + GORM + PostgreSQL | 后端 API + Agent WebSocket |
| **Agent** | `commander-agent-t260220-main/commander-agent-t260220-main` | Wails v2 + Go + Vue3 | 桌面客户端（非浏览器站点） |
| **AliExpress CSP 工具** | `aliexpress-csp-auto-upload` | Python + Playwright/Patchright | 速卖通 CSP 页面录制、接口回放、模板灌入、草稿校验 |

环境工具链根目录：`D:\dev\env`（`FISHAGENT_ENV_ROOT`）

---

## 3. 端口约定（勿混用）

| 服务 | 端口 | 访问方式 |
|------|------|----------|
| Web 管理端 / 营销页 | **5173** | 浏览器 `http://localhost:5173` 或 `http://127.0.0.1:5173` |
| Agent 前端 dev（`wails dev`） | **34115** | 仅 Wails 内嵌 WebView 加载，不要用浏览器当网站打开 |
| Server API | **34206** | `http://localhost:34206` |
| PostgreSQL | 5432 | 本地数据目录 `D:\dev\env\data\postgresql` |

常见误区：`wails dev` 与 `pnpm dev`（Web）若都占 5173，浏览器访问 `localhost` 可能误进 Agent 界面。Agent 前端已固定 **34115**，Web 独占 **5173**。

---

## 4. 一键启动

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File D:\dev\workspace\scripts\restart-commander.ps1
```

会依次处理 Server / Web / Agent。手动启动顺序见 [`AGENTS.md`](./AGENTS.md) 的“启动顺序”章节。

---

## 5. 关键路径速查

```text
D:\dev\workspace\
├── AGENTS.md                 # Agent 工作规则
├── 交接文档.md               # 本文件：最新接手状态
├── 开发文档.md               # 逐次开发流水
├── aliexpress-csp-auto-upload\
├── scripts\restart-commander.ps1
├── commander-agent-t260220-main\
├── commander-server-t260220-main\
└── commander-web-t260220-main\
```

---

## 6. Git 与提交状态

- 根仓库当前用于文档和项目管理，推送远端为 `origin`。
- `aliexpress-csp-auto-upload` 是独立本地 Git 仓库，当前无远端；代码提交只能本地提交，不能声称已推送该子仓库。
- 有实质改动时，只暂存本次相关文件；禁止 `git add .`。
- 提交前必须检查 `git status`、`git diff`、`git diff --staged`、`git log -3 --oneline`。

---

## 7. 远程服务器

远程服务器连接信息只在 [`AGENTS.md`](./AGENTS.md) 内部规则中维护，本文件不写密码或敏感配置。

允许只读排查；上传、部署、重启、替换线上文件必须用户在本轮明确要求。

---

## 8. 安全禁忌

- 禁止提交 cookie、Token、密码、`.env`、真实 `config.yaml` 密钥。
- 禁止把 CSP 账号 cookie 内容写入文档或日志。
- 禁止把接口 HTTP 200 当成发布成功；必须以后端业务返回和列表可见性为准。
- 禁止在没有显式确认时执行真实发布、上架或远程部署。

---
